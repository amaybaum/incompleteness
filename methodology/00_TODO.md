# TODO — priority queue (updated 2026-07-13, §B.2.696ol)

## P0 — HIGHEST PRIORITY (updated §B.2.696ol — consolidation items #1,#2 DONE)
0. **Willow arc CLOSED (oc) + OI-DF-1 CLOSED (ok)**: OI-DF-1 was a tabletop instrument
   on the spectral-measure fork; oi resolved that fork LOCAL -> D3 puts the lab floor on
   the 'invisible forever' branch (no-retreat discharged). NOT a live pivot. Revives only
   if 'local' is overturned by the boundary-lattice computation (item 1).
1. **[DONE ok — LOCAL]** Boundary-lattice spectral computation: promoted
   local Clausius-argument -> THEOREM (consolidates measure/gravity sector) OR returns
   non-local (reopens bend/LV/substratum-sign/OI-DF-1). Gates everything downstream.
   Machinery built: oh Result-1 sketch + code/spectral_phase2_B2_696oi/.
1b. [EXECUTED 2026-07-24 — outcome O2, derived-conditional (§sp)] **GME: YES**, conditional
   on the constraint-slaving input (GR C1). Binding commitment + active falsification clause
   (decisive BMV null ⇒ gravity sector falsified). Derivation archived pre-experiment:
   notes/GME_derivation_v1_2026-07-24.md. REMAINING (◯): quantitative φ_ij; counterfactual
   Γ law; radiative-sector analysis = the surviving OI-specific-divergence candidate.

2. **[DONE ol]** DESI Y5 pre-registration package built (notes/DESI_Y5_preregistration.md):
   w=-1/nu~0 frozen, co-signature battery {LV,w-anatomy}, no-retreat clauses; ok cited as backing.
3. **MILC A^2=2-vs-0.34 + M_X dC2** (owner-hardware): sharpest falsifier + ONLY channel
   that can move the correctness band. Jumps to #1 if optimizing falsification exposure.
   [CORRECTED 2026-07-16 — see item #1 below: ladder HALTED 2026-07-14 (pj-pk), verdict OPEN,
   structurally mismatched pipeline diagnosed (pn-po); redesign path = m_b/m_tau template.]
4. **Owner-gated corpus sync -> 2.5.2 batch**: oe D4 depth-face; oc Willow grade-II;
   differentiator-map contraction; GR-412 ok-upgrade; gdot_check.py 0.093->0.93 typo fix.
   NEXT LIVE ITEM [STALE — corrected 2026-07-16]: was "#3 (MILC/M_X) — the only remaining open
   front"; superseded by the 2026-07-14 ladder halt (pj-pk). Current front = the CONDENSATE GATE
   (see corrected item #1).


## AUDIT #3 COMPLETE (§B.2.696of/og/oh/oi) — spectral-measure fork RESOLVED = LOCAL
- of: anchored-constants policy forced (theorem). og: entropy census survives.
- oh/oi: boundary spectral measure = LOCAL. Brick-wall steelman defeated by the
  stretched-horizon Clausius computation (nu_sub ~ (Heps)^2, not log-uniform 1e-4).
- NET: bend/floor/LV-escape ALL closed on the local branch; framework maximally
  rigid; DESI Y5 clean binary SHARPENED (bend-seen-and-LV-null now adverse).
- Only the FINITENESS fork (QC ceiling / box, nq-nv) remains genuinely open.
- 696oi/oj: GR §412 upgrade APPLIED to v2.5.0 (owner approved); GR.{md,tex,pdf}
  rebuilt; repo shipped (incompleteness-2.5.0_B2_696oi.zip). Four older held memos
  (Koide/A1/tier_c/linearity) verified ALREADY MERGED in v2.5.0 — flagged superseded.

## P1 — queued
- §7.3 substratum nu sign derivation — now only relevant on the INTRINSIC-NONLOCAL branch (oi: local branch gives nu~1e-122, no DESI-quadrant signal; a DESI-quadrant detection REQUIRES non-locality + correlated LV). 4-part pre-registration package before DESI Y5 unchanged.
- M_X statistics/emergent-side question (controls R1 sign, nl).
- Ref [34] conflation check (nn flag).
- Fold DES-Y6/Lyman-alpha update into nh odds ledger (P(f|W) 0.35 -> higher).
- Formal pre-registered falsification-map document for corpus (now incl. QC gate + decoder test); deck fourth-gate line.
- QC-gate P(fire) primary pulls: vendor logical-qubit roadmap numbers (nt inputs are memory-class).
- Primary pulls: modern LLR/MESSENGER Gdot bounds; best atomic-clock alpha-dot/alpha.
- Double-slit toy model ("stripes from a bijection"); A²/ΔC₂ ladder when owner's lattice run completes.
- Malament–Hogarth: landed in §4.6 one-liner; optional expansion to formal corollary.

## RESEARCH LEADS (speculative)
- 696os: METHOD-BORROW (Correctness-relevant IF it lands, higher-effort): adapt Bianconi's
  discrete network-geometry / higher-order-network machinery (arXiv:2408.14391 lineage) as a
  METHOD for the substratum-graph -> smooth-spacetime emergence step — OI's weakest
  derivational link. OI does the work; credit 'method adapted from [Bianconi]'. NOT
  citation-as-proof. Subordinate to item #3 (MILC/M_X), which remains the real front.
- 696os: [APPLIED v2.5.5] related-work paragraph (GR §8.6) + DESI competitor line — owner-gated;
  Consistency/credibility only, cannot move Correctness (self-check passed).
- 696pa: §8.7 kinematic-emergence (2a-2e ladder) [APPLIED v2.5.6]. Open research: (i) 4S dof normalization from microstructure; (ii) nonlinear GR + Lorentzian; (iii) entropy/Compton-phase id. All subordinate to item #3.

## ===== HIGHEST-PRIORITY INTERNAL ITEMS (re-evaluated §B.2.696pe) =====
0. [NEW 2026-07-19 — EXTERNAL REVIEW LIVE] Main submitted to SciPost Physics (§sf). This is
   the standing Correctness channel, now open. IMMEDIATE: (A) CLOSED 2026-07-20 — papers/main_repro built,
   tested (ALL PASS ×4 scripts), merged to main, released as 2.6.0, concept DOI confirmed serving
   it (§sg); (B) fix Zenodo metadata
   (creator name/ORCID/description/type); (C) manuscript frozen as submitted; (D) EXECUTED 2026-07-20: MISMATCH FOUND (3 pts,
   §5 out-claims Abstract); staged fix + response language in notes/ITEM_D_s5... (§sh). Use
   verbatim in first response. Response posture pre-registered in
   CHANGES_2026-07-19 §PRE-REGISTERED — binding. On first report: log verbatim, respond to the
   strongest reading.

1. [CORRECTED 2026-07-16; was "IN PROGRESS ~97%" — STALE vs the journal's own pj-pk markers]
   Item #3 = A2 Koide harness B2 486. STATUS: LADDER HALTED 2026-07-14, VERDICT OPEN (frozen
   rule; NOT falsified, NOT confirmed, NO A^2 ever computed — analysis/ empty at halt).
   SEQUENCE CLEAN (no-retreat clause not implicated): halt was triggered by METHOD gates
   pre-data — anti-GMOR trend (m 0.05->0.03 RAISED m_pi 0.868->0.898, plaq 0.625->0.649,
   opposite the pre-registered m->m_c trajectory) + decisive rho gate cutoff-null at every
   smearing radius, GEVP-confirmed (pl); owner halted with a PREREG ADDENDUM before releasing
   m=0.02/0.01. Textbook pre-registration discipline (pk).
   DIAGNOSED CAUSE (pn/po): the beta=0 det(D^dagD)^6 measure suppresses near-zero Dirac modes ->
   chirally symmetric -> no confinement, no light pion, no rho, no m_c in the low-m direction;
   the ladder (hadron spectroscopy + m->m_c chiral extrapolation) presupposes confinement =
   STRUCTURALLY MISMATCHED to OI's own predicted near-identity measure. Double mismatch: wrong
   PHASE (condensate-free, where the A^2 enhancement cannot occur) and wrong OBSERVABLE.
   CORRECT PATH (po): the framework's own m_b/m_tau template — weak-coupling Haar QCD
   (beta~11.1, chirally broken, Sigma~0.20) + the A^2 matching factor (Z_S analog) + L->inf;
   no ladder, no rho plateau, proven pipeline. Redesign is a NEW pre-registration (the frozen
   ladder rule stays frozen and unexecuted; do not retrofit it).
   GATING QUESTION (the new de facto #1; convergence of THREE independent threads — po's
   sharpened residual, LB5 grading link L6, and item #2's interacting-kernel residual):
   does OI's PHYSICAL induced (Z/qZ) measure carry the chiral condensate? Recon bears on it
   empirically: near-identity-leaning (<P>=0.62-0.65; supports bridge L5) but condensate-
   suppressing (L6 caution made empirical). If NO: A^2=2 AND the m_b/m_tau-style mass sector
   are jointly in jeopardy — this is the honest stake.
   [LB5 grading action B — owner approved, RECONTEXTUALIZED]: <P> + condensate measurements
   are first-class observables of ANY successor run (the condensate is now the gate itself,
   not merely a disambiguator). Interpretation ledger only; no frozen rule touched.
2. [DISCHARGED at actual-kernel grade, 2026-07-16 — owner approved] Coarse-graining-map locality /
   boundary spectral-measure characterization. Computed from the ACTUAL traced-out kernel (exact
   surface GF of the half-space trace-out, linear map): exponent p = +4.00 (analytic +4; +1/+2
   controls pass), flat over 30 orders of tau_S/tau_B, stable under quenched disorder; log-uniform
   nowhere. Hub items (a),(b),(c) conditionally discharged; nu-null a fortiori; DESI Y5 binary
   frozen. RESIDUAL: interacting (condensate-dressed) kernel = terminal step — NOTE 2026-07-16: this residual is the SAME question as the condensate gate in corrected item #1 (one problem, three threads). Manuscript edits
   applied (GR 412, SM 125, SM 716 bridge grade, ch19 229). Kit: work/grr_spectral_measure.py,
   notes: grr_actual_kernel_spectral_note.md, LB5_measure_bridge_A3_grading.md.
3. Derive delta_0 + C2-dependent A*B thresholds from first-principles lattice PT -> upgrades the 3
   gauge couplings at M_Z from FITS (delta_0,A,B matched) to strict predictions. 2-loop failed
   (delta_0->0 at large N); needs 3-loop or a structural mechanism (delta_0=10.02~Tr[Y^2]=10).
4. Close the SPIN gap in QM emergence: spin is the one component supplied by the dilation, not
   read off the substratum; unify with the staggered-to-Dirac lattice spin structure (Main §3.4).
5. MOND rich-cluster residual (1.0-1.5x Perseus/A2029/A1689): entropy-displacement relaxation-kernel
   MOND-to-CDM crossover — framework's self-identified highest-leverage dark-sector computation.
Full reasoning + what-dropped (d=3, external review, substratum->geometry ladder) in
notes/top5_internal_reeval.md.
## ====================================================================
