# The phase-source propagation round — the manuscripts at the sourced boundary

Owner-called, publication-only, written from `main` at `05e290b`, the merge of the phase-source
audit. The kernel states the boundary: the sourced baseline is `SourcedOI`, the theory generated by
the stated substratum and observer access; `DerivedOI` is `SourcedOI` with one further conjunct,
`PhasesAvailable`, which the stated substratum `(S, φ)` and observer access do not derive
(`PHASE-SOURCE-AUDIT.md`, `onesFixing_not_phasesAvailable`, `permClass_onesFixing`,
`substratumClass_not_onesFixing`). This round makes the manuscripts state that boundary, and does
nothing else. Preregistered here and committed alone before any manuscript is touched.

## The three items, fixed in advance

The round is bounded to three items, the owner's list, together with the mirrors §A.25 requires.

**Item 1, the two normalization sentences.** Under the migrated implementation semantics of
`INSTRUMENT-MIGRATION-AUDIT.md`, an intervention is available when it is realized by one protocol
built from admissible operators, and probability normalization in aggregate is a theorem of that
realization (`instAvail_trace`), not a clause of the generation. Two sentences of `[GR §3.3]`
state the clause:

| site | current text |
|---|---|
| GR §3.3, *Primitive-source form*, principle 1 | "Operational interventions are generated by finite families of admissible implementation operators with aggregate probability normalization; admissibility is invariant under relabelling and under adjoining an uncoupled spectator." |
| GR §3.3, the paragraph after the boxed equivalence | "the normalization half of operational validity is explicit in implementation generation" |

The target reading: interventions are those realized by one protocol built from admissible
implementation operators, an admissible isometric step, a register readout with admissible
projectors, classical coarse-graining of the outcome, sequential composition with
outcome-dependent continuation, and the discard of a uniformly attached ancilla, the branches of
an intervention arising together as the outcomes of one protocol; aggregate normalization is a
consequence of that realization. No manuscript names the kernel predicate.

**Item 2, the file attributions moved by the migration.** `control_of_lieRank` and
`inverseAccessibility_of_lieRank` are stated in `OIBridge/MicroscopicReversibility.lean`;
`carrier_general_oiPlusPos` and `oiPlusPos_iff_qm` are stated in `OIBridge/PositivePackage.lean`;
`universalReachability_of_lieRank_positive` stays in `OIBridge/PositiveReachability.lean`. Four
sentences attribute the moved names to `OIBridge/PositiveReachability.lean`:

| site | names attributed to `PositiveReachability.lean` |
|---|---|
| GR §3.3, OI⁺ principle 2, *Reversible richness* | `control_of_lieRank` (cited "from `universalReachability_of_lieRank_positive`", which stays) |
| GR §3.3, *Primitive-source form*, the opening sentence | `carrier_general_oiPlusPos`, `oiPlusPos_iff_qm` |
| GR §3.3, *Primitive-source form*, principle 1 | `control_of_lieRank` (beside `universalReachability_of_lieRank_positive`, which stays) |
| Main §3.4, the primitive-source summary | `carrier_general_oiPlusPos`, `inverseAccessibility_of_lieRank` |

The target: each name attributed to the module that states it, every other name and path
unchanged.

**Item 3, the substratum-source sentence.** `[GR §3.3]`, *Substratum-source form*, states: "The
concrete OI substratum — finite states, bijective microscopic dynamics (Axiom 2), and the phase
structure — supplies the structural part in full." The phase-source audit found the phrase "the
phase structure" underdetermined: it names no operation the observer performs, and every
operation the manuscripts state is ones-fixing and sources no quarter phase. The target reading:
the relative-phase access is stated explicitly as an assumption, an intervention principle on the
substratum class, and not as a consequence of `(S, φ)` with the stated observer access. The
wording distinction of the phase-source scope amendment survives intact, in both parts:

- *necessary*: an access that supplies the phases has an admissible operator, selectable at every
  level, that moves the all-ones vector off its ray, since no ones-fixing class supplies a quarter
  phase;
- *sufficient, as tested*: the quarter-phase intervention itself, a member of the substratum class
  as stated (`phase_monomial`); moving the all-ones ray is not shown sufficient, and the
  manuscript sentence does not say that any ray-moving operator supplies the phases.

The sentence that follows the boxed substratum endpoint, "The controllability resource is not
entailed by A1–A6; it is an empirical extension of the current substratum, entering as a
hypothesis on the extended architecture", is completed by the same assumption on the phase
intervention, so that the left side of the box, "current OI substratum", reads as the substratum
class with the phase intervention stated.

**The mirrors, per §A.25.** Five sites restate the substratum-source form in one summary sentence,
"the concrete OI substratum supplies the structural architecture — closure under composition and
coarse-graining, stability under an uncoupled spectator, relabelling invariance, reversibility,
the exchanges of distinguishable states, and the phases — but, under finite bijective read-write
dynamics, not the continuously tunable state-mixing operation": Main §3.4, the Explainer, book
chapter 1, book chapter 19, and the two occurrences in the book's full source. Each receives the
same minimal clause qualifying the phases as a stated intervention principle rather than a
consequence of the finite states and the bijective dynamics; nothing else in those sentences
changes. The summary sentence of the primitive-source form that `R7-MSP` pins is not touched.

## The constraints, fixed in advance

- No theorem is added, changed or removed; the kernel is not touched. `PHASE-SOURCE-AUDIT.md`,
  `INSTRUMENT-MIGRATION-AUDIT.md` and every other frozen note are not edited.
- Nothing about the lift audit's Q3 from `DerivedOI` enters a manuscript; no manuscript names
  `SourcedOI`, `DerivedOI`, `PhasesAvailable` or the Route B module, as the guards `R7-SUB` and
  `R7-RB0` require.
- The manuscript voice of §A.27, §A.30, §A.32 and §A.33: the sentences are corrected in place, with
  no history, no assert-then-qualify, and no process vocabulary; the kernel names cited are the
  theorems that decide each sentence (`instAvail_trace` for the normalization; `permClass_onesFixing`,
  `onesFixing_not_phasesAvailable`, `substratumClass_not_onesFixing` for the phases;
  `phase_monomial` for the stated member).
- The registry contract of §A.35 in the same commit: the phase-source family becomes `current`
  with anchors in GR and Main; the instrument-realization family's note is corrected, its
  disposition unchanged; the census note's table row follows.
- Derived artifacts regenerated by `sh ./build.sh` for every edited source, GR, Main, the Explainer
  and the book, in the same commit, with the xelatex log clean of dropped glyphs and the page
  counts checked.
- `[Substratum §4]`, the gauge-group and "no complex structure" sentences, is out of scope (P3 of
  the phase-source census, negative there); `[Main §1.2]`, the observer's stated access, is out of
  scope (P7, negative). The census of the phase-source audit is not reopened.

## The tests, fixed in advance

**E1. The normalization sentences.** After the edit, neither "with aggregate probability
normalization" nor "explicit in implementation generation" occurs in any manuscript source, and
`[GR §3.3]` states realization by one protocol with normalization as its consequence, citing
`instAvail_trace`. Admissible outcome: both.

**E2. The attributions.** No manuscript attributes `control_of_lieRank`,
`inverseAccessibility_of_lieRank`, `carrier_general_oiPlusPos` or `oiPlusPos_iff_qm` to
`OIBridge/PositiveReachability.lean`; each is attributed to its module where a path is given;
`universalReachability_of_lieRank_positive` keeps its path; the census resolves every cited
identifier and path. Admissible outcome: all.

**E3. The requalification.** `[GR §3.3]` states the phase intervention as an assumption, names
the necessary condition and the sufficient access tested as two parts, cites the three deciding
theorems of `OIBridge/PhaseSource.lean`, and says nowhere that moving the all-ones ray suffices or
that the phases are derived from the finite states with the bijective dynamics. Admissible
outcome: all.

**E4. The mirrors.** The five summary sites and the GR sentence agree: each qualifies the phases
as a stated intervention principle; the pinned primitive-source summary is unchanged; a corpus
re-grep of the unqualified summary clause returns zero occurrences, and the count is stated in the
outcome. Admissible outcome: zero.

**E5. The surfaces.** Guard `R7-PROP` pins the propagated sentences and rejects the sufficiency
misreading on every manuscript source; the pins of `R7-MIG` and `R7-PHASE` that state the
pre-propagation text and that no manuscript is edited are moved to the propagated text; the
registry, the census note and the README carry the round; the `.tex` and `.pdf` of every edited
source are rebuilt. Admissible outcome: all.

**E6. The checks.** Release gate, all steps; the probe; the census; no kernel change, so the
kernel counts are unchanged at 123 modules and 2,723 named results. Admissible outcome: all green.

## What the round does not do

Prove anything. Attack Q3 from `DerivedOI` or source a relative-phase principle. Change the
verdict, the scope amendment or the note of the phase-source audit. Narrate Route B, the sourced
observer theory, the flow endpoint or the lift audit in a manuscript. Reword any manuscript
sentence outside the sites listed. Reopen `[Substratum §4]`. Refresh the transfer bundle.

Status: preregistered; the outcome follows in its own section.
