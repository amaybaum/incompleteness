name = "oibridge"
defaultTargets = ["OIBridge"]

# Pinned to the Mathlib tag whose own lean-toolchain is exactly the version above.
[[require]]
name = "mathlib"
git = "https://github.com/leanprover-community/mathlib4.git"
rev = "v4.33.0"

[[lean_lib]]
name = "OIBridge"
