# Hydrodynamics round H-D-SR — source reconciliation for the entailment question and the two statements of A5: CONTROL PLANE

**Type-P round.** Every target is a determination about **what the merged record says**, settled by
locating and quoting passages across the manuscript and verification corpora, not by proving
anything. No Lean is written, no theorem is proved, no limit is taken, and no manuscript is edited.
The evidence rule below is frozen for that reason.

**NON-SEALING round, in `§A.37`'s terms.** This round's preregistration **owns no seal state**. It
creates no seal and no pin state, and it **does not take ownership of changing any existing seal
constant**: `verification/lean/edge_rigidity_probe.py` is read as context and is not modified, the
`R7-HY*`, `R7-A6*` and `R7-PQT` constants are read and not touched, and no guard file is added. The
definition budget is zero and no Lean module is written, so no kernel object exists for an ancestry
guard to order and no guard tag is reserved. Under `§A.37` the landing is therefore the merge
alone — `E` → `L`, current green main as first parent and the sealed execution head as second —
with **no archive-pin commit**. Conflicts are resolved in `L` and never in `E`.

**The round label `H-D-SR` is an identifier, not an ordinal.** It is not a rung of the H1–H7
ladder, it is not `H-C`, and its `SR` target prefix is **not** the programme's `S1`–`S5`
continuum-breakdown branch and not rounds `S-A`/`S-B` (hazard 9). The round applies no
`HD`/`HC`/`HI`/`HO` taxonomy label to anything.

**Blob identity is authoritative.** The commit locates the tree; the blob is what is compared. The
result note pins this file by content.

## What this round is

Round H-D reported five statements of one question standing in the corpus in two incompatible
forms, adopted neither, resolved nothing, and named the propagation question for the owner. This
round is the **source reconciliation** that follows: it establishes, by quotation with coordinates,
**exactly what the sources say and exactly where they disagree**, over a named and bounded surface
wider than the one H-D searched — and it **records the disagreement as the finding**. It does not
adjudicate the disagreement, and it does not carry out any propagation.

**Recording a disagreement is a finding. Reconstructing what the sources must have meant is not**
(the evidence rule; hazard 1).

## The authority for this round's scope — H-D's own words, quoted

Every sentence below is quoted from the merged H-D artifacts pinned in the start state. They, and
nothing inferred beyond them, set this round's scope.

**From `round-h-d-a5-status-adjudication/result.md`, `A5S-2d`, the frozen discipline as executed:**

> **The frozen discipline, observed.** This round **adopts neither form over the other**, **edits no
> manuscript**, and **resolves no divergence**. **The propagation question is named here for the
> owner and the round stops**: five statements of the entailment question stand in the corpus, four
> in the open form and one resolved in the negative, and whether the negative resolution should
> propagate to the other four is an owner decision this round records and does not make.

**From the same file, discrepancy D6:**

> **D6 — `A5S-2d`: the entailment-question record grew by two.** The freeze names three statements:
> the parenthetical at [Substratum §3.1] (A5), the closing sentence of [SM §2.7], and
> [Substratum §4]'s formalization-status note. Two further statements stand in the corpus, both in
> the **open** form: [Substratum §3.1]'s **closing paragraph**, in the same section as the
> parenthetical, and **[SM §6]**, which records the map-uniqueness as inheriting the principle's
> "open status". **Five statements now stand, four open and one resolved in the negative.** … **Neither
> form is adopted, no manuscript is edited, and the divergence is not resolved.**

**From `round-h-d-a5-status-adjudication/preregistration.md`, status rule 9:**

> **No manuscript is edited, and no divergence found in the corpus is resolved by this round.**
> `A5S-2d`'s finding is recorded and named for the owner.

**From the same file, the falsifier column for `A5S-2d`, which is the licence for this round's
sweep:**

> a further statement elsewhere in the corpus; it is added to the record

**From `round-h-d-a5-status-adjudication/preregistration.md`, on the two statements of A5:**

> **The kernel's A5 is additivity of `F`; the manuscripts' A5 is linearity of the wave equation.**
> These are two statements, related by the kernel's instance theorem `waveSubstratum_A5` for the one
> rule where both are in play. **No identification theorem between them exists in the tree, and this
> round does not build one** (`A5S-0a`, hazard 11).

**From `round-h-d-a5-status-adjudication/result.md`, chronology control 4:**

> **Whether the `R7-HY*` family should be extended to cover prose-only rounds of this programme is
> an owner decision this round records and does not make.**

**From the same file, discrepancy D7:**

> **The §8 refresh is therefore left to the later action status rule 11 names**, and when that action
> is taken, status rule 11 fixes the words it must carry — the three answers and **both**
> disqualification-rule consequences, in the freeze's own wording and nothing stronger.

Those seven passages are the whole of this round's charter. **Every target below is scoped to
something H-D itself named as unreconciled, unresolved, or left to a later action.** No target is
scoped to a defect this freeze infers on its own account.

## What this round excludes, in terms

- **H-A is not reopened.** Its `H0` linearity gate, its `H1` `q`-gauge finding and its `H2b` stencil
  finding stand exactly as H-A states them, at H-A's own scope and strength. Nothing here revises,
  extends, strengthens or re-executes them.
- **H-B is not reopened.** Its candidate, its A-profile, its conservation and stencil results, its
  labels and its open owner question stand exactly as H-B states them. Nothing here repairs, extends
  or re-executes H-B, and H-B's guardrail is carried wherever H-B is mentioned.
- **The hydrodynamic target is not re-decided.** The programme-level objective, the H1–H7 ladder,
  the taxonomy and the statuses of H3–H7 are read and are not moved. No obligation changes status,
  none is closed, and no limit, scaling map, closure or PDE is asserted.
- **H-D's three answers are not re-decided.** Questions (a), (b) and (c), their outcomes and their
  strengths are consumed as merged and unrevised. This round asks what the sources say, not what
  A5's status is.
- **H3 as a candidate bridge condition is excluded in terms.** A separate concurrent round is
  introducing H3 as a candidate bridge condition toward closure. **This round does not scope H3,
  does not consume H3, and does not depend on H3.** No target of this round is stated in terms of
  H3, no prediction changes sign or strength because of H3, and no finding of this round is evidence
  for or against H3 in either direction. If that round has merged into this round's mandated base,
  its presence there is a fact about the base and not an input to this round (the anti-contamination
  invariant below).
- **A6 is outside this round**, exactly as it was outside H-D. Whatever status the execution base
  carries for it is consumed unchanged and no status for it is asserted here.
- **The OI → QM chain, Track B, Track I, Bell and gravity are outside this round.** Their manuscript
  sections are read as the location of a statement and for nothing else, which is a source audit and
  not an import of evidence (the programme's control 1).

## Start state, pinned by blob

Every file this round reads for content, with its `git hash-object` value at the base.

| path | blob at the base |
| --- | --- |
| `AGENTS.md` | `c51e4fb7b101e6907e23c0ca0c0ccd6e16ec2d08` |
| `papers/Substratum.md` | `9ac6b732e8786897cade416b67c3f7f1df81ab84` |
| `papers/SM.md` | `26d6cbfb230c105eb00a979c2b69c363568455dd` |
| `papers/Structure.md` | `84a7ede451a917513dd488477c93180f5f8c0486` |
| `papers/Main.md` | `a8de3cb760fcb56be7e661bc388b71f7ba2316fb` |
| `papers/Explainer.md` | `b0926875346305ef053995fc10695353df64de68` |
| `papers/Methodology.md` | `1d332b0c9256e33861af0ce3b5598091b63d30a1` |
| `papers/Complexity.md` | `dd38c5b39797433b6f5de469f5711fd38b116441` |
| `book/ch02-substratum.md` | `519188eac9e4c347d7f01eb5b559cfb01df48ea9` |
| `book/ch03-structural-realism.md` | `9de441661c3a0e1132724df4aa392010dde8ed8b` |
| `book/ch05-gauge-structure.md` | `0f55ef3a7b5fca4172073f330f373f4b16542402` |
| `book/ch06-matter-content.md` | `44ef4bd812b174e881592a02bcbf768cf1af09ad` |
| `book/appendix-c-objections.md` | `f6bc786a4923db3dd08488d8ec43d7e1f9fc7a6a` |
| `book/The-Incompleteness-of-Observation-FULL.md` | `9674614cbb55b58b7492f1e4b65a4f09e82e91b5` |
| `verification/ROADMAP.md` | `4f9af3d3e8a2d5d66ff063c94a388d2c7c04f218` |
| `verification/README.md` | `585dd145186dae654f58e2dc16712550b68ec9e0` |
| `verification/lean/edge_rigidity_probe.py` | `dc30d365a06cb3a118d3bdb100c18b9b8c0d799e` |
| `../PROGRAMME.md` | `b7b24112a462aee083c3e8f3c2980283b38cd10f` |
| `../round-h-a-source-audit/preregistration.md` | `934cd6aff1cfb07b823c9b131693ee59bb98c632` |
| `../round-h-a-source-audit/result.md` | `56d34463cf6b68bf28d9ab99b6e09f9fa287d826` |
| `../round-h-b-reversible-fluid-substratum/preregistration.md` | `37cc9dae301ee10d55adb73b296aa2fc7d0578e3` |
| `../round-h-b-reversible-fluid-substratum/result.md` | `dc03b582ed718d374c471b27403b282a295d5c85` |
| `../round-h-d-a5-status-adjudication/preregistration.md` | `a2398c3954f13ac458184c70df8f1ec6b321cc9b` |
| `../round-h-d-a5-status-adjudication/result.md` | `f80dd8832955cb7f17e9712ccdafae364a4059e3` |
| `verification/lean-mathlib/OIBridge/SubstratumInterfaceAudit.lean` | `56a0e4800c08e9a015ce4bc7da4d74aa3ea471b8` |
| `verification/lean-mathlib/OIBridge/HydroSourceAudit.lean` | `fd5f54d8cbba4f68b67335c111d39a2add0c642f` |
| `verification/lean-mathlib/OIBridge/HexLatticeGas.lean` | `374db0387337960888a67f4e98609446c8a1b871` |
| `verification/lean-mathlib/OIBridge/BackgroundIndependence.lean` | `5ec9fe52835642724d0685d9b920f613874279d6` |
| `verification/lean-mathlib/OIBridge/StochasticInterface.lean` | `0f4d1b1625e9c5c5070750bab04c9c989f0f0d04` |
| `verification/lean-mathlib/OIBridge/ManuscriptAxioms.lean` | `f7befdc7b5e814677e6220d91e06fc35b8950c49` |

**Every one of these is read and never written by this round. If any blob differs at the base, the
execution records the discrepancy and does not repair the freeze.**

**Two mechanical sweeps run over directories rather than over this table** — `SR-2`'s corpus sweep
and `SR-4`'s Lean-tree name search. Their surfaces are named and bounded in the targets themselves,
and **the boundary of each sweep is recorded with its finding**, exactly as H-D recorded the
boundary of its own enumeration. A sweep's surface is not a claim that every file on it was quoted;
it is a claim about what a named search over a named surface returned.

## The anti-contamination invariant, FROZEN VERBATIM

> A start-state discrepancy does not license the execution to consume the newer sibling result
> merely because it happens to be present at its mandated base. The round consumes only what its
> freeze says it consumes.

**Why it matters here specifically:** under `§A.37` the execution branches from **this control
plane's merge commit and from nothing else**. The mandated base is therefore fixed at the moment this
control plane merges, and carries exactly what `main` carried at that moment — whatever sibling
results, lane C's H3 round among them, had merged by then. **Those results are not inputs to this
round, however present they are at its base**: their presence there is a fact about the base, and the
round consumes only what this freeze says it consumes. Landing position does not enter the question.
A sibling that merges after this control plane does not appear in this round's base at all, and a
sibling that merged before it is present there without thereby being consumed.

## The evidence rule, FROZEN

The round is **type P**. Every determination must be carried by one or more of:

1. a **verbatim quotation** from a pinned blob, with its file and line coordinate; or
2. a **verbatim quotation** from a merged result note or preregistration, with its coordinate; or
3. an explicit, recorded statement that **the passage sought does not exist**, on a **named and
   bounded** search whose surface is stated with the finding.

**Reconstructive inference is forbidden as a finding.** A determination of the form "the sources
must mean X, because otherwise Y would fail", or "these two passages are saying the same thing once
one allows for Z", is **not evidence** and may not carry any target. It may appear only in a clearly
labelled analysis paragraph that states in terms that it is not evidence and that **no target rests
on it**.

**Where the record is silent, the finding is that it is silent.** Silence is neither assent, nor
denial, nor a licence to supply the missing statement.

**Strength is recorded per determination**, as `full`, `high`, `medium` or `UNDECIDED`. A
determination carried by a passage that names a thing without stating it is at most `medium`. A
determination that rests on a bounded search returning nothing is reported as a statement about that
search and about nothing off it.

## The objects, FROZEN

**`Q-ent` — the entailment question.** Whether the `q`-size gauge freedom of [SM §2.7] entails the
amplitude-scale (additive-automorphism) gauge principle. The round does **not** answer `Q-ent`, does
not adopt an answer to it, and does not rank the answers the sources give.

**`form-open` — the open form.** A statement of `Q-ent` that carries it as an identified open step,
an identified open structural question, or an inherited open status.

**`form-neg` — the resolved-in-the-negative form.** A statement that `Q-ent` resolves in the
negative.

**`form-other` — any third form.** Any statement that bears on `Q-ent` or on the relation between
the `q`-size freedom and the amplitude-scale principle without falling into `form-open` or
`form-neg` — including a statement that the two are instances of one master principle, and a
statement that they are distinct. **A `form-other` statement is recorded as what it says and is not
sorted into `form-open` or `form-neg` on the round's own authority.**

**`A5-ker` — the kernel's A5.** `∀ c c' : 𝒮.ι → 𝒮.V, 𝒮.R.F (c + c') = 𝒮.R.F c + 𝒮.R.F c'`, the
additivity of the substratum rule over the alphabet, in `SubstratumInterfaceAudit.lean`.

**`A5-ms` — the manuscripts' A5.** Linearity of the wave equation, at [Substratum §3.1].

**`prop-surface` — the propagation surfaces.** `../PROGRAMME.md` §8's one-line state,
`verification/ROADMAP.md` and `verification/README.md`.

**`SR11-words` — the wording status rule 11 mandates** for any later refresh of `../PROGRAMME.md`
§8, quoted from H-D's freeze and not paraphrased. **This wording is settled**: status rule 11 fixed
it, this round reproduces it as quotation, and no target of this round proposes, ranks or amends it.
What H-D left open is which later action carries it out.

The round adds **no object of its own**. Where a distinction is needed that the sources do not draw,
the execution **records that the sources do not draw it** and does not draw it on their behalf.

## Recorded readings — the one permitted pre-execution object

Located by hand **before this file was written**, recorded here so that no execution-specific
artifact precedes the freeze. **Nothing here is evidence at any level above "recorded reading"**, and
the execution re-reads each at the mandated base and reports what it finds, including any divergence
from what is recorded here.

1. The five statements H-D's D6 enumerates were located at `papers/Substratum.md:100` (the A5
   parenthetical), `papers/Substratum.md:104` (the same section's closing paragraph),
   `papers/SM.md:92` ([SM §2.7]'s closing sentence), `papers/SM.md:791` ([SM §6]'s inherited open
   status) and `papers/Substratum.md:268` (the formalization-status note on the linearity step).
   Four carry `form-open`; the fifth carries `form-neg`.
2. `papers/Substratum.md:264` — the amplitude-scale-gauge remark, named there as the principle's
   single authoritative home — carries a statement about the relation between the principle and the
   `q`-size freedom that is neither `form-open` nor `form-neg` on its face. It is a `form-other`
   candidate and is recorded as such.
3. `papers/Substratum.md:266` — the *Status* note — records the principle as adjoined, not one of
   Theorem 24's generators, with results invoking it conditional on it. This is a status statement,
   not a statement of `Q-ent`.
4. The book carries the amplitude-scale principle and its conditional status at
   `book/ch02-substratum.md`, `book/ch03-structural-realism.md`, `book/ch05-gauge-structure.md`,
   `book/ch06-matter-content.md` and `book/appendix-c-objections.md`, and
   `book/ch05-gauge-structure.md` carries a `form-other` candidate about the relation between the
   principle and the alphabet-size freedom. **Whether the book poses `Q-ent` in either form
   anywhere is exactly what `SR-2b` must determine**, and this reading does not settle it.
5. **H-D's own search surface was the papers it names.** Extending the named surface to the book and
   to the verification corpora is **this round's doing**, recorded here as such and licensed by
   H-D's falsifier column quoted above.
6. No H-D row was located on `prop-surface` while drafting: `../PROGRAMME.md` §8's one-line state
   carries H-A and H-B, and `verification/ROADMAP.md` and `verification/README.md` carry H-A and
   H-B sections. `SR-5` re-determines this at the base.
7. At the base of this control plane the four manuscript blobs are the blobs H-D's result note
   records at H-D's mandated base. `SR-6` re-determines this at the **execution** base, which is
   later and which sibling landings may have moved.

## The targets, FROZEN

Every target is **type P**. `SR-4` is type P over kernel objects: a name search over a named tree,
not a proof and not a dependency analysis of proof terms.

### `SR-0` — locating controls

**What settles it.** Locate and quote, with file and line coordinates at the mandated base, the
seven charter passages quoted in *The authority for this round's scope* above, plus H-D's status
rule 11 in full (`SR11-words`) and H-D's `A5S-0a` outcome paragraph.

**What evidence counts.** Verbatim quotation with a coordinate from the pinned H-D blobs. A passage
that is absent from the pinned blob at the base is the target's falsifier and is recorded as a
discrepancy, not repaired.

### `SR-1` — the five statements, re-read and reported as five

**What settles it.** For each of the five statements H-D's D6 enumerates: quote it **verbatim** with
its file and line coordinate at the mandated base, and record its form as `form-open` or `form-neg`,
**with the words of the passage that carry that classification quoted inside the quotation**.

**Reported as five, and not merged.** The five are reported separately, in a table, one row each.
**If two of them differ in what they say, the difference is the finding** and is recorded as a
difference of the sources, not normalized.

**What evidence counts.** Quotation with coordinate. A classification of a statement's form that is
not carried by quoted words of that statement is not evidence and may not be recorded as a finding.

### `SR-2` — the sweep beyond H-D's record

The surface, **named and bounded**: `papers/*.md`; `book/*.md`; `verification/programmes/**/*.md`;
`verification/audits/**/*.md`; `verification/README.md`; `verification/ROADMAP.md`; and
`verification/lean-mathlib/OIBridge/*.lean`. `.tex` and `.pdf` renderings are **out of the surface**
and the result note says so. `book/The-Incompleteness-of-Observation-FULL.md` is a concatenation of
the chapter files; a statement found there is reported at **both** coordinates and is counted once.

#### `SR-2a` — further statements about the principle's status or its relation to the `q`-size freedom

**What settles it.** Enumerate, with quotation and coordinate, every statement on the surface that
bears on the relation between the `q`-size gauge freedom and the amplitude-scale principle, or on the
principle's status, and that is **not** among `SR-1`'s five. Classify each as `form-open`,
`form-neg` or `form-other`, carried by quoted words.

**What evidence counts.** Quotation with coordinate; or, for the absence of any such statement in a
named region of the surface, the recorded statement that a named and bounded search returned none
there.

#### `SR-2b` — further statements of `Q-ent` itself

**What settles it.** Determine whether `Q-ent` is **posed** anywhere on the surface outside
`SR-1`'s five — in either form. Report each instance with quotation and coordinate, or record that
the bounded search returns none.

**Silence is a finding.** Where a region of the surface carries the amplitude-scale principle but
does not pose `Q-ent`, the finding is that it does not pose it — not that it answers it either way
(hazard 3).

### `SR-3` — does anything on the record adjudicate between the forms?

**What settles it.** Determine whether any passage on `SR-2`'s surface **adjudicates** the
disagreement: states which of `form-open` and `form-neg` governs; states that the two are one
statement; states that the record is divided; or states that the negative resolution reaches the
statements carrying the open form.

**What evidence counts.** A verbatim quotation, with coordinate, of a passage that says one of those
things in its own words. **A passage that could be read as implying one of them, but does not say
it, is not evidence** and may be discussed only in a labelled analysis paragraph.

**Outcome if nothing is found:** the finding is that **the record does not adjudicate**, on the named
and bounded search — and **that finding is the round's substance**, not a failure of the round.

### `SR-4` — the two statements of A5, re-determined

**What settles it.** Re-determine at the mandated base that `A5-ker` and `A5-ms` are two statements,
and that the Lean tree carries **no theorem identifying them**, `waveSubstratum_A5` being an instance
result for the one rule where both are in play. The search surface is
`verification/lean-mathlib/OIBridge/*.lean`, by name search for `A5`, for the unfolded additivity
hypothesis, and for cross-module citation of every name found; its boundary is recorded with the
finding.

**What evidence counts.** Quotation of the two statements with coordinates; and, for the absence of
an identification, the recorded statement that the named and bounded search returned none. **The
round does not build one** — the definition budget is zero.

### `SR-5` — the propagation surfaces, a census

**What settles it.** Determine, for each of `prop-surface`'s three files, whether H-D's three
answers appear at all; and where they appear, whether the wording is `SR11-words`, quoted from H-D's
status rule 11 and compared to the text found.

**This is a census, not a propagation.** **This round edits none of the three files** and adjudicates
no entry as correct or incorrect. The census exists so that the later action which carries out the
already-worded §8 refresh has its surface enumerated before it acts.

**What evidence counts.** Quotation with coordinate of what is found; or the recorded statement that
a named and bounded search of the named file returns no H-D content.

### `SR-6` — the stability of the quoted passages between H-D's base and this round's base

**What settles it.** For each passage this round quotes from `papers/`, determine whether it is
byte-identical to the text at H-D's mandated execution base
`f81ed6bf660b7b324df93aa466288fe074a021b9`, whose manuscript blobs H-D's result note records
(`papers/Substratum.md` `9ac6b732…`, `papers/SM.md` `26d6cbfb…`, `papers/Structure.md` `84a7ede4…`,
`papers/Main.md` `a8de3cb7…`).

**What evidence counts.** The blob comparison, plus, for any file whose blob differs, the diff
restricted to the quoted passages and the recorded statement of whether each quoted passage moved.
**A movement is recorded as a discrepancy and is not repaired**, and a passage that moved is quoted
at the execution base with the movement recorded beside it.

### `SR-7` — the assembled reconciliation record, and the owner decisions named

**What settles it.** Assemble, in one place: the inventory (`SR-1` and `SR-2`), the adjudication
finding (`SR-3`), the A5-statement finding (`SR-4`), the propagation census (`SR-5`) and the
stability record (`SR-6`); and **name, without making, the three owner decisions H-D recorded**:

1. **whether the negative resolution propagates to the statements carrying the open form** — H-D's
   named propagation question;
2. **whether the `R7-HY*` guard family extends to prose-only rounds of this programme** — H-D's
   chronology control 4;
3. **which later action carries out the refresh of `../PROGRAMME.md` §8** — H-D's D7 and status
   rule 11. **The wording is not the open question and is not this round's to choose**: status
   rule 11 already fixes the words that refresh must carry, and `SR11-words` is that wording quoted.
   What H-D left open, and all that is open, is **which later action performs the already-worded
   refresh**. The result note names that question and states the settled wording beside it, as
   quotation and not as a choice.

**What evidence counts.** The targets above, cited; and quotation of H-D for each named decision.
**Naming a decision is not making it**, and the result note makes none of the three.

## The preregistered predictions, with signs, strengths and recorded reasons

| target | prediction | sign | strength | recorded reason |
| --- | --- | --- | --- | --- |
| `SR-0` | the seven charter passages, status rule 11 and the `A5S-0a` paragraph are located and quoted at the base | positive | full | each was located by hand while drafting this freeze, in the pinned blobs |
| `SR-1` | five statements, reported as five: **four `form-open`, one `form-neg`**, as H-D's D6 records | positive | high | recorded reading 1; the strength is high and not full because the classification must be carried by the passages' own words at the base, which the execution re-reads |
| `SR-2a` | **the record grows**: further statements bearing on the principle's status or its relation to the `q`-size freedom stand on the surface, including at least one `form-other` in `papers/` and several status statements in `book/` | positive | medium | recorded readings 2, 3 and 4; medium because completeness of a corpus-wide sweep is exactly where a missed passage is the plausible error, and because H-D's own narrower sweep grew its record by two |
| `SR-2b` | **`Q-ent` is posed nowhere on the surface outside `SR-1`'s five**, and the book in particular carries the principle without posing the question | negative | medium | recorded reading 4; medium for the same reason as `SR-2a`, and because a negative on a sweep is a statement about the sweep and about nothing off it |
| `SR-3` | **no passage adjudicates**; the disagreement stands unadjudicated on the record | negative | high | H-D searched the papers and named the propagation question for the owner, which it would not have done had it found an adjudication; and no adjudicating passage was seen in the pre-freeze reading of the wider surface. High and not full because `SR-2`'s sweep may surface one |
| `SR-4` | `A5-ker` and `A5-ms` are two statements and **no identification theorem stands in the tree** | positive | high | H-D's `A5S-0a` landed positive at full strength on a narrower base; high here because the base is later and sibling landings may have added Lean this freeze has not read |
| `SR-5` | **H-D's three answers are absent from all three of `prop-surface`** | negative | high | recorded reading 6, and H-D's D7, which left the §8 refresh to a later action. High and not full because the base is later than this control plane's and a sibling round may have refreshed §8 |
| `SR-6` | the quoted passages are **byte-identical** across the two bases | positive | medium | at this control plane's base the four manuscript blobs already equal H-D's; medium because the execution base is this control plane's merge commit and carries whatever siblings had merged by that moment, and a manuscript-touching sibling among them would move them |
| `SR-7` | the record is assembled and **all three owner decisions are named and left open** | positive | full, conditional on `SR-1`–`SR-6` | the three are quoted from H-D as decisions it recorded and did not make; this round has no licence to make any of them |

**`UNDECIDED` remains a permitted outcome of every target**, reported with the obstruction named.

**No prediction licenses its own conclusion.** A target that lands against prediction is reported
against prediction, with its evidence, and **the prediction is not amended**.

## The status rule, and the frozen post-round sentence for each outcome

Exactly one sentence per target is written, verbatim, in the result note and nowhere else.

### `SR-1`

- **FIVE-AS-RECORDED** — five statements, four `form-open`, one `form-neg`:
  > Five statements of the entailment question stand on the record at the execution base, quoted
  > with coordinates in this note: four carry the open form and one carries the resolution in the
  > negative. They are reported as five separate statements, none merged into another, and neither
  > form is adopted over the other.
- **A DIFFERENT COUNT OR DISTRIBUTION** — the statements found differ in number or in form from
  H-D's D6:
  > The entailment-question record at the execution base differs from the record H-D's D6 states, in
  > the respects tabulated in this note; the difference is recorded as a finding about the sources
  > and is repaired nowhere, and H-D's freeze and result note are unedited.
- **UNDECIDED**, with the obstruction named:
  > Whether the five statements stand as H-D's D6 records them is UNDECIDED at the execution base,
  > with the obstruction named in this note.

### `SR-2a` / `SR-2b`

- **THE RECORD GROWS** — further statements are found:
  > The named and bounded sweep enlarges the record by the statements tabulated in this note, each
  > quoted with its coordinate and classified by its own words; the enlargement is an addition to
  > the inventory and changes the form of no statement already in it.
- **THE RECORD DOES NOT GROW** — the sweep returns nothing further:
  > The named and bounded sweep returns no statement beyond those already inventoried. That is a
  > statement about the surface searched, which this note names, and about nothing off it.
- **`Q-ent` POSED ELSEWHERE** (`SR-2b` only):
  > The entailment question is posed at the further coordinates quoted in this note, in the forms
  > their own words carry; they are added to the inventory and neither form is adopted.
- **SILENCE** (`SR-2b` only):
  > On the named and bounded surface, the regions tabulated in this note carry the amplitude-scale
  > gauge principle without posing the entailment question. **The finding is that they are silent on
  > it** — not that they answer it in either direction.

### `SR-3` — the target where recording the disagreement is the finding

- **NO ADJUDICATION FOUND** — the predicted outcome:
  > On the named and bounded search this note describes, **no passage on the record adjudicates
  > between the open form and the resolution in the negative**: none states which governs, none
  > states that the two are one statement, none states that the record is divided, and none states
  > that the negative resolution reaches the statements carrying the open form. **The disagreement
  > stands on the record unadjudicated, and recording it is this round's finding.** This round adopts
  > neither form, resolves nothing, and edits no source; the propagation question stays the owner
  > decision H-D named.
- **ADJUDICATION LOCATED** — a passage says one of those things:
  > The passage quoted with its coordinate in this note adjudicates the disagreement in its own
  > words. It is recorded as what it says. **This round neither endorses nor applies it**, edits no
  > source, and carries out no propagation; whether it governs the statements carrying the other
  > form stays the owner decision H-D named.
- **UNDECIDED** — a passage bears on the question but does not settle whether it adjudicates:
  > Whether the record adjudicates between the two forms is UNDECIDED, with the obstruction named in
  > this note and the passages in question quoted in full. The disagreement is recorded and is
  > resolved nowhere.

### `SR-4`

- **TWO STATEMENTS, NO IDENTIFICATION**:
  > The kernel's A5 and the manuscripts' A5 are two statements at the execution base, and the named
  > and bounded search of the Lean tree returns no theorem identifying them; `waveSubstratum_A5` is
  > an instance result for the one rule where both are in play. This round builds no identification
  > and records none as needed.
- **IDENTIFICATION LOCATED**:
  > The theorem quoted with its coordinate in this note identifies the kernel's A5 with the
  > manuscripts' A5 at the scope its own statement carries. It is cited at that scope and is not
  > restated more broadly, and nothing in this round promotes it.
- **UNDECIDED**:
  > Whether the tree carries a theorem identifying the two statements of A5 is UNDECIDED, with the
  > obstruction named in this note.

### `SR-5`

- **ABSENT FROM ALL THREE**:
  > At the execution base, H-D's three answers appear on none of `../PROGRAMME.md` §8's one-line
  > state, `verification/ROADMAP.md` or `verification/README.md`. The census is recorded; **this
  > round edits none of the three**, and which later action carries out the already-worded refresh
  > is left to the owner, with its surface enumerated here.
- **PRESENT AND IN THE MANDATED WORDING**:
  > At the execution base, H-D's three answers appear at the coordinates quoted in this note, in the
  > wording H-D's status rule 11 mandates. The census is recorded and no file is edited.
- **PRESENT AND DIVERGENT**:
  > At the execution base, H-D's three answers appear at the coordinates quoted in this note, and the
  > wording found differs from the wording H-D's status rule 11 mandates in the respects tabulated.
  > **The divergence is recorded and is repaired nowhere.** The wording it would be aligned to is
  > status rule 11's and is not open; which later action performs that alignment is the owner
  > decision, and this round does not make it.

### `SR-6`

- **IDENTICAL**:
  > Every passage this note quotes from `papers/` is byte-identical to the text at H-D's mandated
  > execution base, on the blob comparison recorded here.
- **MOVED**:
  > The passages tabulated in this note differ between H-D's mandated execution base and this
  > round's execution base. Each is quoted at this round's base with the movement recorded beside
  > it. **The movement is recorded as a discrepancy and is repaired nowhere**, and no finding of
  > H-D is restated on account of it.

### `SR-7`

- **ASSEMBLED, THREE DECISIONS NAMED**:
  > The reconciliation record is assembled in this note, and the three owner decisions H-D
  > recorded — the propagation of the negative resolution, the reach of the `R7-HY*` guard family to
  > prose-only rounds, and which later action carries out the refresh of `../PROGRAMME.md` §8 whose
  > words status rule 11 already fixes — are named here and made nowhere.

### Rules binding every outcome

1. **The round adopts no answer to `Q-ent`**, ranks the forms in no order, and states no preference
   between them, in the note, in a table cell, in a summary, or in any propagation line.
2. **The round edits no source.** No manuscript, no book chapter, no programme file, no roadmap, no
   README, no guard, no Lean module, no merged freeze and no merged result note.
3. **Recording a disagreement is the finding where the record does not adjudicate.** The execution
   does not force a resolution, does not pick the later statement over the earlier, does not pick the
   more specific over the more general, and does not pick the one it finds more convincing.
4. **Silence is reported as silence**, never as denial and never as assent.
5. **H-D's three answers are consumed as merged and are restated at H-D's own scope and strength**,
   never more strongly. **H-B's guardrail is carried wherever H-B is mentioned:** *H-B shows that A5
   is not needed to obtain a promising reversible fluid candidate with the right microscopic
   ingredients; it does not yet show that A5 is unnecessary for an actual Euler/Navier–Stokes limit.*
6. **No taxonomy label is applied** — not to A5, not to any obligation, not to any source.
7. **No label is written "for OI."** Every finding is a statement about a named object: a passage, a
   file, a coordinate, a statement, a theorem.
8. **An analysis paragraph is labelled as such** and states that no target rests on it.

## What no outcome licenses — the forbidden sentences, in terms

- **Nothing here answers the entailment question.** The sentences "the `q`-size gauge freedom
  entails amplitude-scale gauge", "it does not entail it", "the question is settled", "the question
  is open" — as this round's own finding rather than as a quotation of a source — are forbidden in
  terms. The round reports what each source says and stops.
- **Nothing here reconciles the sources.** "The sources agree", "the disagreement is only apparent",
  "read properly these say the same thing", "the open statements are to be read in light of the
  negative resolution", and every paraphrase, are forbidden in terms. **A reader who knows the
  programme can supply the bridging sentence; this round may not** (hazard 1).
- **Nothing here is a propagation.** No source is edited, no wording is aligned, no status is
  refreshed. `SR-5` enumerates a surface; it does not act on it.
- **Nothing here bears on A5's status.** H-D's three answers stand as H-D states them. This round
  does not strengthen, weaken, re-decide or re-derive any of them, and the sentences "A5 is not a
  bare-OI requirement" and "A5 is QM-specific and not required by the hydrodynamic route" are
  forbidden here exactly as H-D's status rules 6 and 7 forbid them there.
- **Nothing here says A5 is unnecessary for an Euler or Navier–Stokes limit**, or that any candidate
  has a hydrodynamic limit. H-B's guardrail applies at every mention of H-B.
- **Nothing here characterizes the substratum structure sufficient for hydrodynamics.** That is the
  programme's objective; this round delivers a source inventory and nothing more.
- **Nothing here moves an obligation.** H1–H7 keep their statuses; nothing is closed, discharged or
  relabelled.
- **Nothing here scopes, consumes or depends on H3 as a candidate bridge condition.** No finding of
  this round is evidence for or against it in either direction.
- **Nothing here changes A1–A6, their statuses, or their number.** No condition is added to the
  A-list or to the C-list, and none is named.
- **Nothing here asserts a status for A6.** Whatever status the execution base carries is consumed
  unchanged.
- **Nothing here is a continuum statement.** No limit, no PDE, no scaling map, no closure; the
  `S1`–`S5` branch stays closed.
- **Nothing here bears on the OI → QM chain, Track B, Track I, Bell or gravity**, in either
  direction.
- **Nothing here promotes a cited result.** Every kernel statement cited keeps the evidence level of
  the round that proved it, and is restated no more broadly than the theorem carrying it.
- **Nothing here makes an owner decision.** `SR-7` names three and makes none.

## Named hazards

1. **A reader who knows the programme silently reconciling sources the record leaves unreconciled —
   the round's strongest hazard.** The failure it guards against: an execution that reads the four
   open statements and the one negative resolution, sees how they *could* be made consistent, and
   writes that reading as the finding. The evidence rule forbids it; `SR-3`'s frozen outcome
   sentences are written so that the unadjudicated case has its own sentence and does not have to be
   forced into a resolution; and the final report states, per determination, which quotation carries
   it.
2. **Treating a `form-other` statement as evidence for one of the two forms.** The failure: sorting a
   passage that says the principle is one instance of a master principle, or that it is distinct from
   the alphabet-size freedom, into `form-open` or `form-neg` because it leans that way. `form-other`
   exists so that such a statement is recorded as what it says.
3. **Treating silence as an answer.** The failure: reading a region that carries the principle
   without posing the question as evidence that the question is settled there. `SR-2b`'s silence
   outcome has its own frozen sentence.
4. **Converting a bounded search into a universal claim.** The failure: writing "no adjudication
   exists" where the evidence is "no adjudication was found on this named surface". Every negative
   is reported with its surface named, and the surface is not a claim about what lies off it.
5. **Performing the propagation while enumerating it.** The failure: `SR-5` notices a surface that
   lacks H-D's answers and an execution helpfully adds them. The census is a census; the round edits
   none of `prop-surface`. **The companion failure is treating status rule 11's wording as open** and
   proposing, ranking or improving words for the §8 refresh. Those words are settled and are
   reproduced as quotation; the open question is which later action carries them.
6. **Making an owner decision by describing it.** The failure: `SR-7` naming the propagation question
   and then recommending an answer, or writing a sentence from which the answer follows. Naming is
   not making.
7. **Restating H-D's answers more strongly than H-D states them.** The failure: a summary line that
   compresses "no derivation was found on the record searched" into the stronger sentence H-D's
   status rule 7 reserves to two grounds, or compresses H-B's guardrail into its second half.
8. **Reading H-D's start-state discrepancies as this round's subject.** H-D's D1–D3 are movements of
   blobs between a freeze and a base, which H-D diffed and recorded. They are not source
   disagreements, and `SR-6` asks a different and narrower question: whether the passages this round
   quotes moved.
9. **Confusing this round's `SR` target prefix with the programme's `S1`–`S5` branch, or the round
   label `H-D-SR` with an ordinal of the H-ladder.** They are different namespaces and no sentence
   reads across them.
10. **Confusing the substratum condition A5 with the act-level target labels of the OI → QM programme's
    Track B, or with `OIBridge.lean`'s "§A5" counting-layer headings.** Three namespaces; no sentence
    reads across them.
11. **Letting a sibling result present at the base enter the round.** The anti-contamination
    invariant governs; lane C's H3 round is the specific case, and the round consumes only what this
    freeze says it consumes.
12. **Building a Lean statement to settle a target.** The definition budget is zero. Where a target
    cannot be settled without a kernel statement, the frozen response is to record that target
    **UNDECIDED** with the obstruction named — not to write the statement under this freeze.

## Definition budget

**Zero.** The execution introduces **no** top-level definition, adds **no** Lean module, and edits
**no** existing one. It cites kernel results and re-proves none. In particular it does **not**
construct an identification between `A5-ker` and `A5-ms`, and defines no predicate of any kind.

**No proof placeholder, no postulated statement and no decision-procedure escape hatch appears
anywhere in this round**, for the plain reason that no Lean is written at all; the result note states
this rather than leaving it to be inferred, and carries no printed-dependency line.

**A single definition requires its own append-only amendment**, separately frozen and merged before
the work it affects, naming the definition and the target it serves.

## The chronology control — checkable mechanically

1. **This preregistration is merged alone**, before any execution-specific `H-D-SR` object enters the
   repository tree. *Check:* the control-plane pull request's diff contains exactly one added file,
   this one.
2. **Round H-D's control plane and result note must have merged before this execution begins.**
   *Check:* `../round-h-d-a5-status-adjudication/preregistration.md` and
   `../round-h-d-a5-status-adjudication/result.md` are both present at the execution base, and
   `git merge-base --is-ancestor f81ed6bf660b7b324df93aa466288fe074a021b9 <execution base>` succeeds
   — H-D's mandated execution base, and with it H-D's landing, is an ancestor of this round's base.
3. **The mandated execution base is exactly the merge commit of this control plane's pull request.**
   The execution branches from that commit and from nothing else. *Check:* the execution branch's
   merge-base with `main` is that commit, and the execution's first parent chain reaches it without
   an intervening merge from later `main`.
4. **The execution's first act is to verify this file's blob at that base**, before any target is
   executed. *Check:* the result note records the blob and it equals this file's
   `git hash-object` value at the control plane's merge.
5. **The result note pins this file by content** and records the base it executed on. *Check:* both
   values appear in the result note's start-state table.
6. **No new guard file is added by this round, and no existing guard is modified.** *Check:* the
   execution's diff touches no file under `verification/lean/` and no `.lean` file.
7. **Before certification the execution never absorbs later main.** No merge from main, no rebase, no
   amend, no force-push. *Check:* the sealed head's ancestry contains no merge whose second parent
   postdates the base.
8. **Under `§A.37` the landing is `E` → `L` with no archive pin**, the round being non-sealing.
   *Check:* the landing merge's second parent equals the sealed execution head, and no pin commit
   follows it.
9. **The claim is scoped to the repository record** at the execution base.

## Evidence level

**Type P throughout.** No target is at evidence level 2. The kernel statements cited at `SR-4` carry
their own evidence level from the rounds that proved them, and **citing them here promotes nothing**:
each is reported at its own scope and is not restated more broadly than the theorem carrying it. The
result note says in terms that the round is type P, and that neither a kernel-dependency table nor a
printed-dependency line appears, rather than leaving the absence to be inferred.

## Immutable inputs

Consumed as merged and unrevised: round H-A's `H0`–`H4a` findings and their labels; round H-B's
candidate, its A-profile, `hexSubstratum_A5_witness`, `hexSubstratum_not_A5`, its conservation and
stencil results, its labels, its guardrail and its open owner question; round H-D's targets
`A5S-0a`–`A5S-5b`, its three answers, its two disqualification rules, its status rules 1–11, its
discrepancies D1–D7 and its chronology certification; `../PROGRAMME.md` §2, §3, §4, §6, §7 and §8;
the `Substratum` interface and `A1`–`A5` as the kernel states them; `AGENTS.md` `§A.37`.

## Non-doings

The round does not: answer the entailment question; adopt, rank or prefer either form of it; edit
any manuscript, book chapter, programme file, roadmap, README, guard, Lean module, merged freeze or
merged result note; carry out any propagation; write or edit any Lean; re-prove any cited result;
construct an identification between the two statements of A5; define any predicate; reopen H-A or
H-B; re-decide the hydrodynamic target or any of H-D's three answers; scope, consume or depend on
H3; assert any status for A6; move, close or discharge any obligation; apply any taxonomy label; take
any limit or assert any PDE, scaling map or closure; make any of the three owner decisions it names;
say anything about Track B, Track I, Bell, gravity or singularities.

## Execution discipline

- Freeze by exact commit SHA **and blob SHA** before any execution. **Blob identity is
  authoritative.**
- Once merged, this file is **immutable**. An execution that diverges from it **records the
  discrepancy** and does not repair the freeze. Execution-affecting corrections are append-only
  amendments, separately frozen and merged before the work they affect.
- **This pull request carries this file alone.**
- **Then exactly one execution pull request**, based on the merge commit of this one, carrying **the
  result note alone**. No Lean, no guard, no manuscript change, no propagation.
- Exact-head certification after execution is complete, with full continuous integration green; the
  certification of record is the run whose `head_sha` is the sealed execution commit.
- **Then the landing on the same pull request:** `L` alone, no pin, the round being non-sealing.
- **No merge without an explicit owner direction after exact-head review, naming the exact head
  SHA.**

## Allowed final report

1. `SR-0`, the charter passages located and quoted with coordinates;
2. `SR-1`, the five statements in a table, one row each, quoted verbatim with coordinates and with
   the form each carries in its own words;
3. `SR-2a` and `SR-2b`, the sweep with its surface named and bounded, every further statement quoted
   with its coordinate and classified by its own words, and silence reported as silence;
4. `SR-3`, the adjudication determination with its search named and bounded, and the frozen sentence
   for the outcome reached;
5. `SR-4`, the two statements of A5 with coordinates and the identification determination with its
   search named and bounded;
6. `SR-5`, the census of `prop-surface`, with `SR11-words` quoted for comparison and no file edited;
7. `SR-6`, the stability record and any movement, recorded and repaired nowhere;
8. `SR-7`, the assembled record and the three owner decisions named and made nowhere;
9. what the outcomes do **not** license, in this file's wording, H-B's guardrail among them;
10. the discrepancy section — every divergence between this freeze and what the execution finds at
    the base, recorded and repaired nowhere;
11. the definition budget, recorded as zero, with the statement that no Lean is written and that no
    kernel-dependency table exists;
12. the chronology certification: this file's blob by content, the execution base, the record that no
    guard was added or modified, and the round's shape recorded as non-sealing with `E` → `L` and no
    pin.

Nothing else.

## Points at which this freeze chose a reading, recorded rather than resolved

1. **The sweep surface is wider than H-D's.** H-D searched the papers it names; this freeze adds the
   book and the verification corpora. The alternative was to hold the surface to H-D's and report
   only on it. This freeze chose the wider surface because H-D's own falsifier column anticipates
   further statements "elsewhere in the corpus", and because `§A.25`'s propagation discipline makes
   the book a place where a statement of the same question can stand. The choice is recorded here,
   and every finding names the surface it rests on.
2. **`form-other` is a category of this freeze, not of the sources.** The sources do not sort their
   statements into forms. `form-other` exists so that a passage which is neither the open form nor
   the negative resolution is recorded as what it says instead of being pushed into one of the two.
   The execution records the category as this freeze's bookkeeping and never attributes it to a
   source.
3. **`Q-ent` is named by this freeze in the sources' own vocabulary** — the `q`-size gauge freedom of
   [SM §2.7] and the amplitude-scale (additive-automorphism) gauge principle. If a source uses a
   different phrase for the same question, the execution **records both phrasings and does not merge
   them**.
4. **The round is non-sealing.** An alternative would add a guard pinning the quoted coordinates.
   This freeze does not, because the definition budget is zero and there is no kernel object to
   order; the blob pins in the start-state table carry the immutability the round needs, and whether
   the `R7-HY*` family should reach prose-only rounds is one of the owner decisions `SR-7` names and
   does not make.
5. **`SR-5` is a census and not a propagation.** The alternative — refreshing `../PROGRAMME.md` §8
   in this round — was set aside because H-D's status rule 11 already fixes the words that refresh
   must carry and leaves the refresh itself to a later action, and this round owns no propagation.
   Carrying out the refresh here would be this round appointing itself that action.

## What the owner must decide before this freezes

1. **The sweep surface.** Is the wider surface of `SR-2` — papers, book, verification programmes and
   audits, the OIBridge Lean modules — the right one, or should the round hold to the papers H-D
   searched? A wider surface makes `SR-2a`'s growth prediction likelier and `SR-2b`'s and `SR-3`'s
   negatives correspondingly stronger; a narrower one makes the round a strict re-read of H-D's
   record.
2. **Whether `SR-5`'s census belongs in this round at all.** It is the one target whose subject is
   the repository's own bookkeeping rather than the manuscripts' statements. If the owner prefers the
   propagation surface enumerated by the action that performs the refresh, `SR-5` comes out and the
   round is purely a source inventory.
3. **Whether `SR-6` should compare against H-D's base or only against this round's.** Comparing
   against H-D's base is what makes the round's quotations commensurable with H-D's; it also makes
   the round's outcome depend on sibling landings it does not consume, which is exactly the coupling
   the anti-contamination invariant exists to bound.
4. **Whether the three owner decisions `SR-7` names are the right three**, and whether any further
   decision H-D recorded should be named beside them.
5. **Whether the execution pull request may carry the `ROADMAP` and `README` pointers this round's
   own section needs**, or the result note alone. This freeze specifies the result note alone; H-D's
   D7 records that the parallel question was live there too.
