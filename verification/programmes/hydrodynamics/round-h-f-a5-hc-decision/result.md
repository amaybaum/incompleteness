# Hydrodynamics round H-F — the A5 / H-C decision: what the programme does next on the H-B axis: RESULT

**Type P throughout.** Every determination below is a determination about **what the merged record
says**, settled by locating and quoting passages with coordinates. No Lean is written, no theorem is
proved, no limit is taken, no manuscript is read and no manuscript is edited. **No
kernel-dependency table appears in this note and no printed-dependency line appears in it**, for the
plain reason that no Lean is written at all. The kernel statements cited here carry their own
evidence level from the rounds that proved them, and citing them promotes nothing.

**The round label `H-F` is an identifier, not an ordinal.** It is not a rung of the H1–H7 ladder, it
is not round `H-C`, and its `HF` target prefix is not the programme's `S1`–`S5` continuum-breakdown
branch and not rounds `S-A`/`S-B`. **`H-F` is not a taxonomy value either**: `../PROGRAMME.md` §4's
four outcome labels are `HD`, `HC`, `HI` and `HO`, and this round applies none of them to anything.

**This is a NON-SEALING round**, in `AGENTS.md` `§A.37`'s terms: it owns no seal state, creates no
seal constant and no pin state, and takes ownership of changing no existing seal constant. The
landing shape is `E` → `L`, with no archive-pin commit `P`. This note's own execution head is
**exact-head-certified**, not sealed; the sealing rounds H-A and H-B keep their own wording.

## Start state, verified at the mandated base

**The mandated execution base** — the merge commit of this round's control plane, `§A.37`'s mandated
base — is `59cfd54d2d1db2437a08fdf9a7f27008208c5b88` ("Merge the H-F control plane: the A5 / H-C
decision (#642)"), whose first parent is `7e988c834fdb6083636f4424f0365bb4d387a65b` and whose second
parent is `a62cb14020545223849eb8ae85d0455589c1c1a1`.

**The freeze, pinned by content.** `round-h-f-a5-hc-decision/preregistration.md` at that base has
`git hash-object` value **`90b0dce3faced6a01460dab3693074400ae5d91a`**, which is exactly the blob the
freeze names. **The freeze is verified and unmoved**, and this was the execution's first act, before
any target was executed.

**The pinned start-state table, re-checked at the base.** The freeze pins sixteen blobs at its
certified drafting base `b78eac870ba3ee9ef9e98659ac933bf97dc62226`. Twelve are unmoved at the
mandated base; four have moved, all four through sibling landings that entered `main` between the
drafting base and the control plane's merge.

| path | blob pinned by the freeze | blob at the mandated base | |
| --- | --- | --- | --- |
| `AGENTS.md` | `c51e4fb7…` | `c51e4fb7…` | unmoved |
| `verification/ROADMAP.md` | `5ee35552…` | `78e36c1d…` | **moved** |
| `verification/README.md` | `d25eb2d4…` | `9983b078…` | **moved** |
| `verification/lean/edge_rigidity_probe.py` | `6e334e83…` | `f3285558…` | **moved** |
| `../PROGRAMME.md` | `b7b24112…` | `3b8e0223…` | **moved** |
| `../round-h-a-source-audit/preregistration.md` | `934cd6af…` | `934cd6af…` | unmoved |
| `../round-h-a-source-audit/result.md` | `56d34463…` | `56d34463…` | unmoved |
| `../round-h-b-reversible-fluid-substratum/preregistration.md` | `37cc9dae…` | `37cc9dae…` | unmoved |
| `../round-h-b-reversible-fluid-substratum/result.md` | `dc03b582…` | `dc03b582…` | unmoved |
| `../round-h-d-a5-status-adjudication/preregistration.md` | `a2398c39…` | `a2398c39…` | unmoved |
| `../round-h-d-a5-status-adjudication/result.md` | `f80dd883…` | `f80dd883…` | unmoved |
| `../round-h-d-source-reconciliation/preregistration.md` | `d222e5c6…` | `d222e5c6…` | unmoved |
| `../round-h-e-h3-closure-bridge/preregistration.md` | `9f3f4ff4…` | `9f3f4ff4…` | unmoved |
| `verification/lean-mathlib/OIBridge/SubstratumInterfaceAudit.lean` | `56a0e480…` | `56a0e480…` | unmoved |
| `verification/lean-mathlib/OIBridge/HydroSourceAudit.lean` | `fd5f54d8…` | `fd5f54d8…` | unmoved |
| `verification/lean-mathlib/OIBridge/HexLatticeGas.lean` | `374db038…` | `374db038…` | unmoved |

**Every one of these is read and never written by this round.** The four movements are recorded in
the discrepancy section and are **repaired nowhere**. Under the freeze's anti-contamination
invariant — *a start-state discrepancy does not license the execution to consume the newer sibling
result merely because it happens to be present at its mandated base; the round consumes only what its
freeze says it consumes* — **no newer content is consumed as evidence by any determination below**.

**`verification/lean/edge_rigidity_probe.py` is read as context only**, for the `R7-HY*` inventory,
and is not modified. The `R7-HY*` guard family belongs to the rounds that set it; this round adds no
member to it and alters no member of it.

---

# `HF0` — the governing passages, located and quoted at the base

**Frozen sentence for the outcome reached — `PARTIAL`:**

> The governing passages are located and quoted with their coordinates at the mandated base, and
> the passages recorded absent or moved are named here with the bounded search that did not find
> them; the movement is recorded as a discrepancy and is repaired nowhere.

**Stated precisely, so the sentence is not over-read: no passage is absent.** All seventeen are
present at the base and are quoted below. Six stand at coordinates different from the freeze's
recorded readings, because two files carrying them moved through sibling landings. **No bounded
search failed to find a passage**, so the clause of the frozen sentence that speaks of such a search
has no instance here; that divergence between the frozen sentence's wording and the facts is recorded
in the discrepancy section. `PARTIAL` is written rather than `LOCATED` because the freeze names
movement as a `PARTIAL` trigger in terms, and the weaker of two available readings is the one this
round writes.

| # | the passage | freeze's recorded reading | coordinate found at the base |
| --- | --- | --- | --- |
| 1 | `../PROGRAMME.md` §1's ultimate objective and its two acceptable endpoints | `11–21` | **`13–23`** |
| 2 | `../PROGRAMME.md` §3's obligations H3, H4, H5, H6, H7 | `69–95` | **`71–97`** |
| 3 | `../PROGRAMME.md` §4's four outcome labels | `99–119` | **`101–121`** |
| 4 | `../PROGRAMME.md` §6's `axis-HC` entry in full | `198–200` | **`200–202`** |
| 5 | `../PROGRAMME.md` §7's controls 2, 5, 8, 9, 10 | `212–223` | **`217`, `220`, `223`, `224`, `225`** |
| 6 | `../PROGRAMME.md` §8's one-line state as it stands at the base | `227–229` | **`229–231`** |
| 7 | `verification/ROADMAP.md`'s round H-A section | `819–834` | **`820–835`** |
| 8 | `verification/ROADMAP.md`'s round H-B section | `836–858` | **`837–859`** |
| 9 | round H-D's three answers, `A5S-4a` | `656–678` | `656–678` |
| 10 | round H-D's `A5S-3d`, the boundary and the two settling conditions | `619–638` | `619–638` |
| 11 | round H-D's `A5S-5b` scope statement | `747–767` | `747–767` |
| 12 | round H-D's guardrail on what round H-B supplies | `575–579` | `575–579` |
| 13 | round H-B's post-round programme status, points 1 and 4 | `48–64` | `48–64` |
| 14 | round H-A's `H4a` skeleton and its closing status | `227–239` | `227–239` |
| 15 | round H-D-SR's declaration of its own subject | `25–36` | `25–36` |
| 16 | round H-E's declaration of its own shape and its exclusion | `20–53`, `112–138` | `20–53`, `112–138` |
| 17 | `AGENTS.md` `§A.37`'s shape split and archive-seal paragraph | `773–799` | `773–799` |

## The passages

**1 — the ultimate objective** (`../PROGRAMME.md:15–23`):

> The strongest hydrodynamic endpoint would be a theorem of the form
> `concrete OI substratum + explicitly named hydrodynamic conditions -> Navier–Stokes`,
> followed, if the named conditions can themselves be derived from the concrete OI physics, by the
> stronger result `concrete OI substratum -> Navier–Stokes in a stated hydrodynamic/continuum
> limit`.
> If Navier–Stokes is not uniquely forced, the acceptable endpoint is a universality/classification
> theorem identifying exactly which OI-compatible local reversible substrata have Euler/Navier–Stokes
> hydrodynamics and which do not.

**2 — the obligations H3–H7** (`../PROGRAMME.md:71–97`), in the section whose opening line reads
"The programme should not jump directly from A1–A6 to Navier–Stokes. It should close the following
obligations in order":

> **H3 — local equilibrium / mixing.** State and test the weakest mixing or local-equilibrium
> condition needed to close the macroscopic equations. Existing hidden-sector mixing assumptions may
> be relevant only if a theorem identifies the same object; analogy is not a bridge.
>
> **H4 — scaling map.** Define the microscopic-to-continuum map and the scaling regime explicitly:
> lattice spacing, time scaling, field normalization, carrier growth, and the topology/norm in which
> convergence is claimed. No statement of a continuum PDE is meaningful until this map is fixed.
>
> **H5 — Euler-level limit.** Derive the conservative hydrodynamic equations first. Identify pressure
> and the equation of state from the microscopic model or name them as additional constitutive input.
>
> **H6 — viscous correction.** Derive the first dissipative correction and identify the viscosity
> coefficient. … A positive viscosity is an outcome to prove, not a default sign convention.
>
> **H7 — incompressible Navier–Stokes.** Only after H1–H6 are closed should the programme take an
> incompressible/low-Mach limit and prove a theorem of the form `∂_t u + (u·∇)u = -∇p + ν Δu`,
> `∇·u = 0`, at an explicitly stated scope, dimension, boundary condition, and regularity class.

**3 — the four outcome labels** (`../PROGRAMME.md:101–121`), quoted as the programme states them and
applied by this round to nothing: `HD — Derived` ("The required hydrodynamic structure follows from
already accepted concrete OI physics at the stated scale and scope"); `HC — Conditional` ("The
structure follows only after adding a named physical/statistical/scaling condition"); `HI —
Independent` ("A lawful OI countermodel shows that the structure is not implied by the current
assumptions. This is a classification result, not a failure"); `HO — Open` ("The derivation is
incomplete and no impossibility/countermodel has been proved. Construction or formalization failure
is not independence").

**4 — the `axis-HC` entry, in full** (`../PROGRAMME.md:200–202`):

> ### Round H-C — hydrodynamic limit
>
> Freeze the scaling and convergence topology before execution. Derive the conservative limit and
> then the viscous correction, with Euler and Navier–Stokes reported separately.

**5 — the controls** (`../PROGRAMME.md:217`, `:220`, `:223`, `:224`, `:225`):

> 2. **Bare OI is not NS.** The programme concerns the concrete local OI substratum plus whatever
>    hydrodynamic conditions survive the source audit, not arbitrary finite reversible systems.
> 5. **Fix the continuum map before testing singularities.** No continuum blow-up claim is admissible
>    without an explicit finite-to-continuum map and convergence notion.
> 8. **Outcome asymmetry.** One explicit counterexample can establish independence at a stated scope;
>    failure to derive NS or a singularity bridge establishes only Open unless a no-go theorem is
>    proved.
> 9. **Evidence type is always stated.** Kernel theorem, exact probe, prose/source audit, and
>    theorem-from-external-premise remain distinct.
> 10. **No manuscript strengthening from this roadmap.** Publication-facing claims wait for the
>     relevant theorem chain or an explicit classified endpoint.

**6 — §8's one-line state as it stands at the base** (`../PROGRAMME.md:229–231`). The state at the
base carries a clause recording round H-E's execution that the freeze's drafting base did not carry.
**It is quoted here because `HF0` item 6 requires §8 as it stands at the base, and its content is
read into no determination of this round.** Its hydrodynamics clauses, as they stand:

> A new parallel programme is opened: determine whether the concrete finite, deterministic,
> reversible, local OI substratum lies in a Navier–Stokes hydrodynamic universality class, first by
> auditing the exact conservation, isotropy, mixing and scaling obligations and then, only if that
> bridge closes, test whether continuum fluid and spacetime singularities can arise as
> failures/incompleteness of the emergent observable description while the underlying substratum
> dynamics remains regular. … H-B: one candidate executed in the A1–A4, ¬A5 class; OI-compatibility of
> the class open (`round-h-b-reversible-fluid-substratum/result.md`). H-E: one candidate bridge
> condition is stated against the H3 obligation for round H-B's candidate … so H3 stands as a
> component of a bridge and not as one and the H3 obligation stays HO
> (`round-h-e-h3-closure-bridge/result.md`).

**7 — the round H-A `ROADMAP` section** (`verification/ROADMAP.md:820–835`), whose operative
sentences read:

> The source audit of the concrete wave representative `waveSubstratum d L q α` is executed at
> evidence level 2 on the mandated base `ae81459` … the linearity gate makes the advection obligation
> HI conditional on `ZMod q`-linear coarse variables, with real-valued or nonlinear coarse variables
> HO; … the axis stencil's fourth moment is proved anisotropic — conditional HI if H5's stress
> closure consumes this tensor, otherwise H2 remains HO; and H3 and H4 are HO. Round H-B is entirely
> alive, nothing here bears on the OI → QM chain, and no row of the queue above changes.

**8 — the round H-B `ROADMAP` section** (`verification/ROADMAP.md:837–859`), whose operative
sentences read:

> The frozen streaming-and-collision candidate — a six-channel Boolean lattice gas on the `L × L`
> periodic lattice in the hexagonal basis, placed inside the kernel's `Substratum` interface
> unmodified as the second-order rule `F c = Φ c + Φ⁻¹ c` — is executed at evidence level 2 on the
> mandated base `8de0478` … mass and both momentum components are exactly conserved by the gas on
> every configuration for every `L`, on `Γ`, and are the only conserved site-independent
> channel-weighted totals; the gas commutes with the lattice's `60°` rotation; the stencil's fourth
> moment is `(3/4)(δδ + δδ + δδ)`, rotation-isotropic, and its sixth is not, with no claim about
> higher orders; the block-charge two-time state does not close at `L = 4`, `b = 2` … **Round H-B is
> not reported closed**: the execution establishes a reversible fluid witness in the A1–A4, ¬A5 class,
> and whether that class is admissible as OI physics is an owner decision recorded as open.

**9 — round H-D's three answers, `A5S-4a`** (`round-h-d-a5-status-adjudication/result.md:656–678`).
Quoted in the source's own words; the three separations are H-D's, and this note keeps them apart
wherever it cites them:

> **(a) — bare-OI necessity.** **No derivation of A5 from bare OI was found on the record searched.**
> … **This is not a proof that no such derivation exists.** **Whether A5 is a bare-OI requirement is
> UNDECIDED** …
>
> **(b) — quantum-route necessity.** **A5 is used at eight located steps of the reconstruction chain,
> and each located use is quantum-specific in this round's sense** — the conclusion each step draws
> is consumed only inside the reconstruction/QM chain and is required by no obligation outside it on
> the present record. … *Strength of the summary: medium* …
>
> **(c) — hydrodynamic necessity.** **UNDECIDED.** No obligation H1–H7 **as stated** names A5 or
> additivity; H-B's conservation and stencil content together do not entail A5, at H-B's scope and
> about microscopic content only; H-A's advection gate takes A5 as its hypothesis and is an
> obstruction on one named class of coarse variables; and H3–H7 are HO with no derivation closing
> them. … *Strength: full, that (c) is UNDECIDED. Obstruction: no evidence at the H3–H7 level exists
> at the base.*

**10 — `A5S-3d`**, quoted at `HF1-b` below. **11 — `A5S-5b`**
(`round-h-d-a5-status-adjudication/result.md:747–767`):

> **THE SCOPE STATEMENT.** The programme-level objective is: *what is the weakest OI substratum
> structure sufficient to produce a Navier–Stokes hydrodynamic limit?* … **The sufficiency question
> belongs to H3–H7 and to H-C**, which fix the mixing/local-equilibrium condition, the scaling map and
> the convergence topology before any limit is taken. **The programme-level objective is left open by
> this round.**

**12 — round H-D's guardrail** (`round-h-d-a5-status-adjudication/result.md:575–579`), carried in
this note wherever round H-B is cited, in its own words:

> **ROUND H-D'S GUARDRAIL, carried at this mention — the `HF0` inventory of governing passages.**
>
> **H-B shows that A5 is not needed to obtain a promising reversible fluid candidate with the right
> microscopic ingredients; it does not yet show that A5 is unnecessary for an actual
> Euler/Navier–Stokes limit.**

**13 — round H-B's post-round programme status, points 1 and 4**, quoted at `HF1-d` below.
**14 — round H-A's `H4a` skeleton**, quoted at `HF2-b` below. **15 — round H-D-SR's declaration**,
quoted at `HF1-c` below. **16 — round H-E's declaration of its own shape and its exclusion**
(`round-h-e-h3-closure-bridge/preregistration.md:22`, `:42`, `:112–116`): "**This is a SEALING
round.**"; "**Therefore this round lands `E` → `L` → `P`, and `P` is mandatory**"; and "**The status
of A5 relative to the hydrodynamic target is round H-D's lane, and a separate concurrent round is
reconciling its sources. This round does not re-open it, does not re-decide it, and does not consume
it as settled.**" **17 — `AGENTS.md` `§A.37`'s shape split** (`AGENTS.md:785–788`, `:793–799`):

> 2. **A non-sealing round — a round that owns no seal state — takes `L` alone.** It **may** modify
>    other contracts inside an existing guard; it **may not** alter existing seal constants. It has
>    nothing to pin, and a pin commit added there would pin nothing.
>
> **An archive seal belongs to the round that set it, and stays immutable afterwards.** A later round
> does not re-pin it, and does not acquire a pin commit merely by touching the guard file that
> carries it. …

---

# `HF1` — `axis-A5`: what the merged record leaves, and what each item is gated on

> **THE A5 ANTI-CONFLATION CLAUSE, carried at this mention — the `axis-A5` enumeration.** `A5-ker`,
> the kernel's A5, is additivity of the substratum rule over the alphabet — `∀ c c' : 𝒮.ι → 𝒮.V,
> 𝒮.R.F (c + c') = 𝒮.R.F c + 𝒮.R.F c'`, at
> `verification/lean-mathlib/OIBridge/SubstratumInterfaceAudit.lean:102`. `A5-ms`, the manuscripts'
> A5, is linearity of the wave equation, at [Substratum §3.1]. **These are two statements, and no
> identification theorem between them exists in the tree**; `waveSubstratum_A5`
> (`SubstratumInterfaceAudit.lean:211`) is an instance result for the one rule where both are in
> play and identifies nothing. **This round builds no identification, assumes none, needs none, and
> writes "A5" unqualified nowhere**: every target, prediction, gate, status sentence and hazard here
> that touches A5 says which of `A5-ker` and `A5-ms` it means, and a sentence that says "A5" without
> saying which is a defect of this round.

**`A5-ms` is carried at one remove throughout.** It is quoted from round H-D's merged result note, at
H-D's own coordinate, and is not located afresh in `papers/`. That is the freeze's boundary choice,
recorded at its *Points at which this freeze chose a reading* item 2, and it is stated here in terms
rather than left to be inferred: `papers/` and `book/` are outside this round's surface entirely and
no manuscript was opened.

## `HF1-a` — question (a)'s obstruction, re-determined at the base

**Frozen sentence for the outcome reached — `CARRIER STILL ABSENT`:**

> On the named and bounded search of the Lean tree this note describes, no predicate and no result
> establishing observer-admission for a concrete substratum stands at the mandated base, so the
> obstruction round H-D named for its question (a) stands unrelieved. That is a statement about the
> surface searched and about nothing off it, and it is not a proof that no such carrier can be built.

**Round H-D's obstruction, quoted** (`round-h-d-a5-status-adjudication/result.md:255–262`, `:268–269`,
`:280–282`, `:284–286`):

> **Condition (iv) has no carrier.** The kernel carries **no** predicate for observer-admission on a
> `Substratum`. `ManuscriptAxioms.lean` records realized-core images for A1 and A2 on the bare
> operational theory (`A1Realized`, `A2Realized`, `A1A2Realized`) and states that **A3–A6 have
> neither images nor weakened predicates** … Its configuration-level sourcing results bound what a
> substratum's own interventions generate without supplying an admission predicate.
>
> A tree-wide search for any result establishing observer-admission for a **concrete** substratum
> returns none.
>
> **Outcome: UNDECIDED.** … **The obstruction, named:** condition (iv) has no carrier anywhere in the
> tree at the mandated base.
>
> **What would change the outcome:** an existing result, anywhere in the tree, establishing
> observer-admission for a concrete substratum that fails A5 — then the counterexample is reported
> exhibited, with that result cited. None exists at the base.

The condition H-D's search is about is stated at `:243–245` and `:249`: "there is a partition
`V ⊂ S` such that `(S, φ, V)` satisfies C1–C4 as [Main §3] states them — [Structure §9.6]'s
Definition 9.9 of an observer-admitting substratum", and condition **(iv)** is that "its
**observer-admission is established**, not assumed, at the definition above".

### The carrier sweep, named and bounded

**Surface.** `verification/lean-mathlib/OIBridge/*.lean` **as that tree stands at
`b78eac870ba3ee9ef9e98659ac933bf97dc62226`**, the freeze's certified drafting base — **166 modules**.
The surface is fixed at that commit and **not** at the execution base, exactly as the freeze
requires, so that no sibling round landing after this freeze was drafted can enter the sweep. It is
later than the base of round H-D's own enumeration, so the re-check is not a restatement of H-D's.

**Search terms, as run.** Case-insensitive over the whole surface: `admissib`, `admission`, `admits`,
`admitting`, `observerAdmi`, `ObserverAdmi`, `observer-admi`, `OIAdmissible`,
`Admissible.*Substratum`, `Substratum.*Admissible`, `observer`, `Definition 9.9`, `Def 9.9`, `9\.9`,
`C1.*C4`. Structurally: every `def`/`abbrev`/`structure`/`class`/`theorem`/`lemma` line whose text
contains `Substratum`; every such line whose declared name contains `observ`; and every `def`,
`structure` or `abbrev` named `C1`–`C4`.

**What the search returned.** **No predicate for observer-admission on a `Substratum`, and no result
establishing it for a concrete substratum.** Specifically:

- The `admissib` family resolves entirely to `AdmissibleDilationAt`, a dilation predicate on
  matrices, in modules that do not mention `Substratum`. It is not a predicate on a `Substratum`.
- The terms `admission`, `observerAdmi`, `ObserverAdmi`, `observer-admi`, `OIAdmissible`,
  `Definition 9.9`, `Def 9.9` and `9.9` return **zero hits** on the surface.
- No `def`, `structure` or `abbrev` named `C1`, `C2` or `C3` exists on the surface. The only `C4`
  declarations are `CausalReadback.lean:62` and `:67`, `C4e` and `C4r`, whose arguments are
  `(K : ℕ) (Γ : ℕ → Matrix V V ℝ)` — **not a `Substratum`**. This is the nearest object round H-D
  named, found again, with the same shape.
- The declarations that take a `Substratum` and state something of it are the interface's own
  conditions and their instances — `Substratum.A1`–`A5`, `A4Exact`, `A3Family`, the `waveSubstratum_*`
  and `hexSubstratum_*` instance theorems, and `SubstratumInterfaceAudit.lean`'s Section E
  configuration-level sourcing results (`obs_sourcedOI`, `obs_embeddedObservation`,
  `obs_readWriteAvailable`, `obs_dynamics_avail`, and their companions). **None of these is a
  predicate for observer-admission**, and none is stated at Definition 9.9.
- `ManuscriptAxioms.lean:30` carries at the base exactly the sentence H-D quoted: "the interface has
  none of these, and **no weakened predicate is defined for** [A3–A6]".

**Bounded, and bounded in terms.** This is a statement about 166 named modules at one named commit and
about nothing off that surface. It is not a claim about the rest of the Lean tree, not a transitive
dependency analysis of proof terms, and **not a proof that no such carrier can be built**. Programme
control 8 applies: failure to find establishes Open, never Independent.

**Which A5 the item means: `A5-ker`.** Carried by H-D's own quoted words at `:273–274`, which state
the failure as a kernel theorem: "its failure of A5 is a theorem, `hexSubstratum_not_A5`, with the
witness pinned by equation in `hexSubstratum_A5_witness`".

> *Analysis paragraph — labelled as such, and **no target rests on it**.* Section E's sourcing results
> are the objects whose shape most invites a second look, and the tree itself records why they are not
> the carrier: `obsTheory_rule_independent` (`SubstratumInterfaceAudit.lean:779–781`) proves that "two
> substrata on the same sites and alphabet have the same observer theory, whatever their rules", so
> the properties Section E establishes of `obsTheory 𝒮` do not separate one rule from another. That
> reading is an inference of this note, it is not evidence, and `HF1-a` rests on the bounded search
> and on round H-D's quoted statement, not on it.

**Strength: medium.** A negative on a sweep is a statement about the sweep. **Against prediction? No** —
the freeze predicted the carrier still absent, negative sign, medium strength, and that is what the
search returned.

## `HF1-b` — question (c)'s two settling conditions, and where each one lives

**Frozen sentence for the outcome reached — `BOTH CONDITIONS QUOTED AND ASSIGNED`:**

> Round H-D's two settling conditions for its question (c) are quoted here with their coordinates,
> and the axis each is assigned to is carried by the quoted words of round H-D's own note. Neither
> condition is met at the mandated base, and nothing here settles that question in either direction.

**The two conditions, quoted** (`round-h-d-a5-status-adjudication/result.md:621–624`, `:626–628`,
`:630–633`, `:635`):

> **The boundary, recorded.** `../PROGRAMME.md` §3's obligations **H3, H4, H5, H6 and H7 are HO** …
> **No derivation closes any of them.** A future derivation of any step of H3–H7 **could consume
> additivity in a step not yet written**, and nothing at the start state excludes that.
>
> **What would settle question (c) positively** — *required*: a derivation of some step of H3–H7 that
> takes A5 as a hypothesis and is shown to need it; or a no-go showing that no rule failing A5 can
> close one of those steps.
>
> **What would settle question (c) negatively** — *not required by the hydrodynamic route*: a
> derivation of the Euler-level limit, the viscous correction or the incompressible limit for a rule
> in the A1–A4, ¬A5 class, **at a stated scaling map and convergence topology**; or an equivalent
> result at that level.
>
> **Neither is found at the mandated base.** This round writes no Lean and takes no limit.

**The assignment, carried by quoted words.** Both conditions are stated by the source wholly in the
vocabulary of the H3–H7 ladder and of the scaling map and convergence topology: the positive one
names "some step of H3–H7" in both its disjuncts; the negative one names "the Euler-level limit, the
viscous correction or the incompressible limit … at a stated scaling map and convergence topology",
which are `../PROGRAMME.md` §3's H5, H6 and H7 and the object §6's `axis-HC` entry requires frozen
before execution. Round H-D's own note then assigns that level, at `:765`: "**The sufficiency question
belongs to H3–H7 and to H-C**, which fix the mixing/local-equilibrium condition, the scaling map and
the convergence topology before any limit is taken." And its recorded obstruction for question (c),
at `:678`: "**no evidence at the H3–H7 level exists at the base**."

**What the assignment is, stated exactly.** The record assigns both settling conditions to the H3–H7
level, and assigns that level to `axis-HC` by the words at `:765`. **Neither condition is assigned by
any quoted words to `axis-A5`**, and no source searched assigns either of them there. That is the
finding; this round assigns nothing on a source's behalf.

**Which A5 the item means: `A5-ker`, on the quoted words available, for the positive condition's
second disjunct and for the negative condition** — "no rule **failing** A5" and "a rule in the A1–A4,
¬A5 class" are statements about the kernel's `Rule`, and round H-B's class membership is carried by
the kernel theorem `hexSubstratum_not_A5`, `¬ (hexSubstratum L).A5`. **For the positive condition's
first disjunct — "a derivation of some step of H3–H7 that takes A5 as a hypothesis and is shown to
need it" — the source does not say which statement of A5 it means**, and the finding is that it does
not say. This round supplies neither in its place. See `HF3`, row 3.

**Strength: high**, for the quotation and for the assignment carried by the quoted words; the
which-A5 row for the positive condition's first disjunct is reported as silence, not as high. **As
predicted.**

## `HF1-c` — the source-level question, held at arm's length

**Frozen sentence for the outcome reached — `HELD AT ARM'S LENGTH`:**

> The source-level question stands at the mandated base as round H-D named it for the owner, and
> round H-D-SR's freeze scopes the reconciliation, adjudicates nothing and makes no owner
> decision. **No outcome of that execution is consumed, assumed, anticipated, ranked or preferred
> by any determination of this round**, and where its result note is present at the base its
> presence is a fact about the base and not an input.

**Round H-D's naming of the question for the owner, quoted**
(`round-h-d-a5-status-adjudication/result.md:490–494`):

> **The frozen discipline, observed.** This round **adopts neither form over the other**, **edits no
> manuscript**, and **resolves no divergence**. **The propagation question is named here for the
> owner and the round stops**: five statements of the entailment question stand in the corpus, four
> in the open form and one resolved in the negative, and whether the negative resolution should
> propagate to the other four is an owner decision this round records and does not make.

**Round H-D-SR's freeze, scoping the reconciliation and adjudicating nothing**
(`round-h-d-source-reconciliation/preregistration.md:27–35`):

> Round H-D reported five statements of one question standing in the corpus in two incompatible
> forms, adopted neither, resolved nothing, and named the propagation question for the owner. This
> round is the **source reconciliation** that follows: it establishes, by quotation with coordinates,
> **exactly what the sources say and exactly where they disagree**, over a named and bounded surface
> wider than the one H-D searched — and it **records the disagreement as the finding**. It does not
> adjudicate the disagreement, and it does not carry out any propagation.
>
> **Recording a disagreement is a finding. Reconstructing what the sources must have meant is not.**

**The fact about the base.** `round-h-d-source-reconciliation/result.md` **is present** at the
mandated base. Its presence is recorded here as a fact about the base, under the anti-contamination
invariant. **It was not opened, and its content is read into no determination of this note.**

**The enumeration the target asks for.** The sentences of the freeze that would have to exist for this
round to consume that outcome are the ones that would state a determination in terms of how the
reconciliation lands — a target whose settling condition referred to which form prevailed, a gate
conditioned on it, a prediction whose recorded reason invoked it, or a frozen status sentence naming
a resolution. **There are none.** The freeze instead forbids each in terms, at its anti-contamination
invariant, at `HF1-c`, at `HF4`, at the gate on `HF5-A5`, at its forbidden-sentence list and at hazard
1. The walk that establishes this is `HF4`.

**Which A5 the item means: `A5-ms`.** Carried by round H-D's own quoted words at
`round-h-d-a5-status-adjudication/result.md:102`, whose row subject is "**linearity of the wave
equation**" and whose status cell reads "**equivalent to amplitude-scale gauge invariance via
[SM §4.1]'s linearity-equivalence lemma; necessary *given* that gauge principle and 'partly derived'
in exactly that sense; the entailment question from [SM §2.7]'s `q`-size freedom carried as an
identified open step**". The attribution is recorded from those quoted words and is not asserted by
this round.

**Strength: full.** **As predicted.**

## `HF1-d` — round H-B's open owner question, restated at its own scope

**Frozen sentence for the outcome reached — `OPEN, IN ROUND H-B'S WORDS`:**

> Whether the class round H-B's candidate lies in is admissible OI physics stands open at the
> mandated base in round H-B's own words, quoted here with its coordinate. This round names that
> question and answers it nowhere.

**Round H-B's post-round programme status, points 1 and 4, quoted**
(`round-h-b-reversible-fluid-substratum/result.md:48–50`, `:53–57`, `:58–62`):

> 1. **Round H-B is not reported closed by this round**, and the programme's H-B obligation is not
>    reported discharged. The round is reported as **one candidate executed**, with the outcomes
>    below.
> 3. What the execution establishes, at full strength, is exactly this: **a rigorous reversible fluid
>    witness in the A1–A4, ¬A5 class** — finite, deterministic, reversible, translation-covariant, of
>    bounded degree, inside the kernel's `Substratum` interface — **with exact mass and momentum
>    conservation on every configuration for every lattice size and fourth-order stencil isotropy**,
>    proved and not assumed.
> 4. **Whether the A1–A4, ¬A5 class counts as admissible OI physics is a separate question** — the
>    owner decision the freeze names — and **its admissibility is recorded here as open.** If the
>    owner later rules the class admissible, the programme-level closure of H-B is a separate owner
>    action on the record, not a consequence this execution draws; if the owner rules it inadmissible,
>    the candidate stands as a witness about the class and H-B's obligation stays open.

**Round H-D's guardrail, carried here because round H-B is cited here**
(`round-h-d-a5-status-adjudication/result.md:577–579`):

> **ROUND H-D'S GUARDRAIL, carried at this mention — round H-B's open owner question at `HF1-d`.**
>
> **H-B shows that A5 is not needed to obtain a promising reversible fluid candidate with the right
> microscopic ingredients; it does not yet show that A5 is unnecessary for an actual
> Euler/Navier–Stokes limit.**

**Naming the question is not answering it.** No sentence of this note answers it or implies an answer,
and nothing here rules the class admissible or inadmissible.

**Which A5 the item means: `A5-ker`.** Carried by round H-B's own quoted words at
`round-h-b-reversible-fluid-substratum/result.md:114–116`, which state the class membership as the
kernel predicate's failure: "`F (c + c') ≠ F c + F c'`, for every `L ≥ 1` (`hexSubstratum_A5_witness`
…) and therefore `¬ (hexSubstratum L).A5` (`hexSubstratum_not_A5`)".

**Strength: full.** **As predicted.**

---

# `HF2` — `axis-HC`: what the merged record says the next round must fix

## `HF2-a` — the `axis-HC` entry, quoted, and what it requires before execution

**Frozen sentence for the outcome reached — `QUOTED`:**

> The programme's round `H-C` entry is quoted here in full with its coordinate, and what it requires
> to be frozen before execution and reported separately afterwards is reported from its own words and
> from nothing else.

**The entry, in full** (`../PROGRAMME.md:200–202`):

> ### Round H-C — hydrodynamic limit
>
> Freeze the scaling and convergence topology before execution. Derive the conservative limit and then
> the viscous correction, with Euler and Navier–Stokes reported separately.

**What it requires to be frozen before execution, from its own words:** the **scaling** and the
**convergence topology**. **What it requires to be reported separately, from its own words:** Euler
and Navier–Stokes. **What it requires derived, and in that order, from its own words:** the
conservative limit first, then the viscous correction. The entry is `../PROGRAMME.md`'s; this round
neither extends it nor narrows it, and fixes none of what it names.

**The entry carries no precondition clause**, where §6's two other forward rounds do: the `S-A` entry
opens "Only after H-C:" (`:206`) and the `S-B` entry opens "Only after an effective-geometry bridge
exists:" (`:210`). That is a statement about the words of three entries and is carried into `HF2-c`,
where its search surface is named.

**Strength: full.** **As predicted.**

## `HF2-b` — the five scaling-skeleton items, reported separately

**Frozen sentence for the outcome reached — `REPORTED PER ITEM`:**

> The five items of round H-A's scaling skeleton are reported here separately, each recorded fixed
> with the passage that fixes it, unfixed with the passage that records it unfixed, or silent with
> the named and bounded search that found no passage. **The table is the finding**, and no single
> verdict is written in place of it.

**Round H-A's skeleton, quoted** (`round-h-a-source-audit/result.md:227–239`):

> ## `H4a` — the scaling skeleton
>
> Not a theorem, and no theorem is claimed; evidence type "prose/source audit" (programme control 9).
> A continuum map for this substratum must fix, before any PDE statement is meaningful:
>
> 1. the lattice spacing as a function of `L`;
> 2. the time step;
> 3. the field normalization — including how `ZMod q` values are lifted (to `ℤ`, to `ℝ`, or otherwise)
>    and rescaled;
> 4. the carrier growth — how `L`, `q` and the coarse-graining scale are taken together;
> 5. the topology in which convergence would be claimed.
>
> **None is fixed by the manuscripts or by A1–A6.**

**Round H-B's record of which of the five its candidate fixes, quoted**
(`round-h-b-reversible-fluid-substratum/result.md:313–319`):

> H-A's `H4a` skeleton — lattice spacing as a function of `L`, time step, field normalization and
> lift, carrier growth, convergence topology — is inherited unchanged and is not a target of this
> round; evidence type "prose/source audit" (programme control 9). The candidate fixes **one** of its
> five choices: the field lift is the integer count of Boolean occupations, `(c i k).val`, fixed by
> the construction. The other four — the lattice spacing as a function of `L`, the time step, the
> carrier growth and the convergence topology — remain unfixed. **Reported status for H4: HO.** No
> limit is taken and no PDE is written.

### The table — one row per item

| # | item | status on the merged record at the base | the passage that carries it |
| --- | --- | --- | --- |
| 1 | the lattice spacing as a function of `L` | **unfixed** | H-A `:239` "None is fixed by the manuscripts or by A1–A6"; H-B `:317–318` "The other four — the lattice spacing as a function of `L`, … — remain unfixed" |
| 2 | the time step | **unfixed** | H-A `:239`; H-B `:317–318`, naming "the time step" among the four |
| 3 | the field normalization, including the lift of `ZMod q` values | **unfixed by the manuscripts or by A1–A6; fixed for round H-B's candidate only** | H-A `:239`; H-B `:315–317` "The candidate fixes **one** of its five choices: the field lift is the integer count of Boolean occupations, `(c i k).val`, fixed by the construction" |
| 4 | the carrier growth | **unfixed** | H-A `:239`; H-B `:317–318`, naming "the carrier growth" among the four |
| 5 | the topology in which convergence would be claimed | **unfixed** | H-A `:239`; H-B `:317–318`, naming "the convergence topology" among the four |

**A row is not settled by another row.** Row 3's fixing is stated by round H-B **for its own
candidate**, by construction, and is restated no more broadly: it is not a fixing on the merged record
for the wave representative and not a fixing of the scaling map. Rows 1, 2, 4 and 5 stand unfixed with
the passages that record them unfixed; **no row required the silence branch**, since a passage
recording each as unfixed exists.

**Strength: high.** The prediction — at least four of the five unfixed or silent — holds: **four are
unfixed with no qualification, and the fifth is unfixed on the record while fixed for one candidate by
its own construction.** **As predicted.**

## `HF2-c` — does anything on the merged record block writing the `axis-HC` control plane?

**Frozen sentence for the outcome reached — `NO BLOCKAGE FOUND`:**

> On the named and bounded search this note describes, no passage of the merged record states that
> the round `H-C` control plane cannot be written, and none makes an unfixed item of the scaling
> skeleton depend on an input the merged record does not carry. **That is a statement about the
> search, which this note names, and about nothing off it**: it is not a finding that the round is
> ready, that its work is tractable, or that any limit exists.

### The blockage search, named and bounded

**Surface — nine files, at the mandated base**, being `../PROGRAMME.md` and the eight pinned round
artifacts: `../round-h-a-source-audit/preregistration.md` and `result.md`,
`../round-h-b-reversible-fluid-substratum/preregistration.md` and `result.md`,
`../round-h-d-a5-status-adjudication/preregistration.md` and `result.md`,
`../round-h-d-source-reconciliation/preregistration.md`, and
`../round-h-e-h3-closure-bridge/preregistration.md`. **The two sibling result notes present at the
base are not on the surface**, the freeze excluding them from this round's evidence in terms.

**Search terms, as run.** `Only after`, `only after`, `cannot be written`, `may not be written`,
`must not be written`, `blocked`, `is blocked`, `not enabled`, `premature`, `prerequisite`,
`before .*H-C`, `not be commissioned`.

**What the search returned, and what each hit says.**

- `../PROGRAMME.md:206` — "**Only after H-C**: use the derived continuum map to test whether regular
  finite substratum approximants can converge to a continuum singularity …". This gates round `S-A`
  **on** round H-C; it does not gate round H-C.
- `../PROGRAMME.md:210` — "**Only after an effective-geometry bridge exists**: …". This gates round
  `S-B`; it does not gate round H-C.
- `../PROGRAMME.md:127` — "The singularity programme opens **only after** a controlled continuum bridge
  such as H4–H7 exists." This gates the `S1`–`S5` branch; it does not gate round H-C.
- `../PROGRAMME.md:93` — "**Only after H1–H6 are closed** should the programme take an
  incompressible/low-Mach limit …". This orders H7 after H1–H6 inside §3's ladder. §6's `axis-HC`
  entry asks for "the conservative limit and then the viscous correction", and this hit states an
  ordering within the ladder; **it does not state that the control plane cannot be written**.
- `../PROGRAMME.md:111` — the `HC — Conditional` label's text. A taxonomy label, a different object
  under the same two characters; no blockage.
- `../round-h-d-a5-status-adjudication/preregistration.md:16–17` and
  `../round-h-e-h3-closure-bridge/preregistration.md:7–8` — "Executing it before `../PROGRAMME.md`
  §6's H-C entry is not a reordering of the H1–H7 ladder", and the same sentence for round H-E. Both
  say that running a round **ahead of** the H-C entry is admissible; neither blocks the H-C entry.
- `../round-h-d-a5-status-adjudication/preregistration.md:653` and `:684`, and
  `../round-h-e-h3-closure-bridge/preregistration.md:647` and `:692` — restrictions those rounds place
  on their **own** sentences, including round H-E's own forbidden-sentence list, which forbids writing
  "the hydrodynamic programme is blocked". None states a blockage on writing the H-C control plane.

**No hit states, in its own words, that the round `H-C` control plane cannot be written**, and **no hit
makes an item `HF2-b` reports unfixed depend on an input the merged record does not carry.** On the
contrary, the fixing of the two objects §6 names is assigned by §6 to that round itself
(`../PROGRAMME.md:202`: "Freeze the scaling and convergence topology before execution"), and round
H-A's `:230` states the same requirement in the same direction: "A continuum map for this substratum
**must fix, before any PDE statement is meaningful** …".

**Bounded, and bounded in terms.** This is a statement about nine named files at one named commit and
about nothing off that surface. **It is not a finding that the round is ready, that its work is
tractable, that a limit exists, or that round `H-C` will succeed.**

**Strength: medium.** A negative on a bounded search is a statement about the search. **As predicted.**

## `HF2-d` — the `carrier-question`, enumerated and not decided

> **THE A5 ANTI-CONFLATION CLAUSE, carried at this mention — the carrier enumeration.** `A5-ker`,
> the kernel's A5, is additivity of the substratum rule over the alphabet — `∀ c c' : 𝒮.ι → 𝒮.V,
> 𝒮.R.F (c + c') = 𝒮.R.F c + 𝒮.R.F c'`, at
> `verification/lean-mathlib/OIBridge/SubstratumInterfaceAudit.lean:102`. `A5-ms`, the manuscripts'
> A5, is linearity of the wave equation, at [Substratum §3.1]. **These are two statements, and no
> identification theorem between them exists in the tree**; `waveSubstratum_A5`
> (`SubstratumInterfaceAudit.lean:211`) is an instance result for the one rule where both are in
> play and identifies nothing. **This round builds no identification, assumes none, needs none, and
> writes "A5" unqualified nowhere**: every target, prediction, gate, status sentence and hazard here
> that touches A5 says which of `A5-ker` and `A5-ms` it means, and a sentence that says "A5" without
> saying which is a defect of this round.

**Frozen sentence for the outcome reached — `ENUMERATED, NEITHER CHOSEN`:**

> The exhibited candidates the merged record carries inside the kernel's substratum interface are
> enumerated here with what each citing round already says about them, each at that round's own scope
> and no more broadly. **Neither carrier is chosen, preferred, ranked or recommended**, the choice is
> named at `HF6` as the owner's, and no recorded consequence of either carrier is a status for
> `A5-ker` or for `A5-ms`.

### Candidate 1 — the wave representative, `waveSubstratum d L q α`

Exhibited at `SubstratumInterfaceAudit.lean:177` as `def waveSubstratum (α : ZMod q) : Substratum`.

**Its `A5-ker` profile:** `waveSubstratum_A5 : (waveSubstratum d L q α).A5`
(`SubstratumInterfaceAudit.lean:211`). Round H-D records what this object is and is not
(`round-h-d-a5-status-adjudication/result.md:80–85`): "**These are two statements.** A search of the
tree at the mandated base returns **no theorem identifying them**. What the tree carries is
`waveSubstratum_A5` …, an instance result: the one rule for which both are in play satisfies the
kernel predicate. An identification would have to quantify over rules that the kernel's `Rule`
interface does not constrain to be wave-like, and no such statement exists. **This round does not
build one.**"

**Round H-A's linearity gate, and the class of coarse variables it is stated on**
(`round-h-a-source-audit/result.md:59–69`):

> **Consequently no closed `ZMod q`-linear coarse description of the present rule carries a quadratic
> (advective) term on the coarse states it is defined on.** …
>
> **Reported status for the advection obligation: HI, conditional on the coarse-variable class.** The
> class is `ZMod q`-linear coarse-graining, named in the statement. **Real-valued or nonlinear coarse
> variables are HO**: the round does **not** assert "the mod-`q` wraparound is the only nonlinearity
> available" — a real lift `ZMod q → ℤ → ℝ` followed by products is *a* nonlinear coarse-graining, and
> whether it carries an advective term is a separate question this round neither asks nor answers.
> The status carries its class; dropping the class over-reads it.

**Round H-D's `A5S-3c` reading of it** (`round-h-d-a5-status-adjudication/result.md:594–600`,
`:610–617`):

> As H-A proved it: `coarse_evolution_additive` and `coarseCloses_additive_on_range` take A5 as their
> **hypothesis** — carried unfolded as `(hF : ∀ c c', F (c + c') = F c + F c')` — so … any closing
> coarse rule agrees with an additive map on the range of `(C, C)`, and is `ℕ`-homogeneous of degree
> one there … **Consequently, on substrata satisfying A5, no closed `ZMod q`-linear coarse description
> carries an advective term on the coarse states it is defined on.**
>
> **The frozen reading, carried in terms.** This is a **conditional no-go on one named class of coarse
> variables**, for substrata satisfying A5. **Real-valued and nonlinear coarse variables are HO** by
> H-A's own statement … It is therefore **not** a proof that A5 excludes a Navier–Stokes limit, **not**
> a proof that Navier–Stokes requires the failure of A5, and **not** a ground on which this round
> labels A5 anything at all. It is recorded as what it is: **an obstruction found, under a stated
> hypothesis, on a stated class.**

Also on the record for this candidate, at round H-A's own scope
(`verification/ROADMAP.md:824–829`): the only total-sum conservation law is conserved in the
manuscript instance iff `q ∣ 4`; the axis stencil's fourth moment is proved anisotropic — conditional
HI if H5's stress closure consumes this tensor, otherwise H2 remains HO; and H3 and H4 are HO.

### Candidate 2 — round H-B's streaming-and-collision gas, `hexSubstratum L`

Exhibited at `HexLatticeGas.lean:272` as `def hexSubstratum [NeZero L] : Substratum`.

**Its conservation and stencil content**, at round H-B's own scope
(`round-h-b-reversible-fluid-substratum/result.md:53–57`, `:41–42`, and
`verification/ROADMAP.md:842–849`): "**a rigorous reversible fluid witness in the A1–A4, ¬A5 class** —
finite, deterministic, reversible, translation-covariant, of bounded degree, inside the kernel's
`Substratum` interface — **with exact mass and momentum conservation on every configuration for every
lattice size and fourth-order stencil isotropy**, proved and not assumed"; mass and both momentum
components "are the only conserved site-independent channel-weighted totals"; "the stencil's second
moment is `3 δ`, its fourth moment is `(3/4)(δδ + δδ + δδ)` and rotation-isotropic, and its sixth is
not"; the gas commutes with the lattice's `60°` rotation; its graph sector `Γ` is invariant and mass is
not conserved off `Γ`; and the block-charge two-time state does not close at `L = 4`, `b = 2`.

**Its recorded `A5-ker` profile** (`round-h-b-reversible-fluid-substratum/result.md:114–116`):
"`F (c + c') ≠ F c + F c'`, for every `L ≥ 1` (`hexSubstratum_A5_witness`, stated for the rule …) and
therefore `¬ (hexSubstratum L).A5` (`hexSubstratum_not_A5`)".

**Round H-D's guardrail on what round H-B supplies, carried here in its own words**
(`round-h-d-a5-status-adjudication/result.md:577–579`):

> **ROUND H-D'S GUARDRAIL, carried at this mention — candidate 2 of the carrier enumeration.**
>
> **H-B shows that A5 is not needed to obtain a promising reversible fluid candidate with the right
> microscopic ingredients; it does not yet show that A5 is unnecessary for an actual
> Euler/Navier–Stokes limit.**

**Round H-B's open owner question** (`round-h-b-reversible-fluid-substratum/result.md:58–59`):
"**Whether the A1–A4, ¬A5 class counts as admissible OI physics is a separate question** … and **its
admissibility is recorded here as open.**"

**Which scaling item it fixes, and only for itself** (`:315–317`): the field lift, "the integer count
of Boolean occupations, `(c i k).val`, fixed by the construction".

### The restriction that makes this target safe, applied

**Neither carrier is chosen, preferred, ranked or recommended here.** A carrier's recorded `A5-ker`
profile is a **fact about that carrier**, proved by the round that proved it. Reporting it is not an
adoption of `A5-ker`, not an adoption of `A5-ms`, not a status for either, and not a statement that
either is required by or excluded from the hydrodynamic route. The choice between the two is named at
`HF6` as the owner's, and an `axis-HC` control plane is where it would be made.

**Strength: high.** **As predicted.**

---

# `HF3` — which A5 does each enumerated item mean?

> **THE A5 ANTI-CONFLATION CLAUSE, carried at this mention — the which-A5 determination.** `A5-ker`,
> the kernel's A5, is additivity of the substratum rule over the alphabet — `∀ c c' : 𝒮.ι → 𝒮.V,
> 𝒮.R.F (c + c') = 𝒮.R.F c + 𝒮.R.F c'`, at
> `verification/lean-mathlib/OIBridge/SubstratumInterfaceAudit.lean:102`. `A5-ms`, the manuscripts'
> A5, is linearity of the wave equation, at [Substratum §3.1]. **These are two statements, and no
> identification theorem between them exists in the tree**; `waveSubstratum_A5`
> (`SubstratumInterfaceAudit.lean:211`) is an instance result for the one rule where both are in
> play and identifies nothing. **This round builds no identification, assumes none, needs none, and
> writes "A5" unqualified nowhere**: every target, prediction, gate, status sentence and hazard here
> that touches A5 says which of `A5-ker` and `A5-ms` it means, and a sentence that says "A5" without
> saying which is a defect of this round.

**Frozen sentence for the outcome reached — `ONE OR MORE ROWS SILENT`:**

> For the items tabulated here the source does not say which statement of A5 it means, and **the
> finding is that it does not say**. This round supplies neither statement in the source's place,
> builds no identification between them, and adopts neither.

| # | enumerated item | which statement the source means | the quoted words that carry it |
| --- | --- | --- | --- |
| 1 | `HF1-a` — the absent carrier, and the counterexample condition it serves | **`A5-ker`** | H-D `:273–274`: "its failure of **A5** is a theorem, **`hexSubstratum_not_A5`**, with the witness pinned by equation in **`hexSubstratum_A5_witness`**" |
| 2 | `HF1-b` — question (c)'s **negative** settling condition | **`A5-ker`** | H-D `:630–632`: "a rule in the **A1–A4, ¬A5 class**", the class round H-B `:116` carries as "**`¬ (hexSubstratum L).A5` (`hexSubstratum_not_A5`)**" |
| 3 | `HF1-b` — question (c)'s **positive** settling condition, first disjunct | **does not say** | H-D `:626–627`: "a derivation of some step of H3–H7 that **takes A5 as a hypothesis** and is shown to need it" — the words name no statement, and no other passage on the surface assigns one |
| 4 | `HF1-b` — question (c)'s **positive** settling condition, second disjunct | **`A5-ker`** | H-D `:627–628`: "a no-go showing that **no rule failing A5** can close one of those steps" — predicated of the kernel's `Rule`, the object `Substratum.A5` is stated of |
| 5 | `HF1-c` — the entailment question and its propagation | **`A5-ms`** | H-D `:102`, row subject "**linearity of the wave equation**", cell: "**equivalent to amplitude-scale gauge invariance via [SM §4.1]'s linearity-equivalence lemma** … the entailment question from [SM §2.7]'s `q`-size freedom carried as an identified open step" |
| 6 | `HF1-d` — the admissibility of the class round H-B's candidate lies in | **`A5-ker`** | H-B `:114–116`: "**`F (c + c') ≠ F c + F c'`**, for every `L ≥ 1` … and therefore **`¬ (hexSubstratum L).A5`**" |
| 7 | `HF2-d` — candidate 1's recorded profile | **`A5-ker`** | `SubstratumInterfaceAudit.lean:211`, **`waveSubstratum_A5 : (waveSubstratum d L q α).A5`**; H-D `:81–84` records it as "an **instance result**", not an identification |
| 8 | `HF2-d` — candidate 1's linearity gate, as round H-D reads it | **`A5-ker`** | H-D `:594–595`: the theorems "take A5 as their **hypothesis** — carried unfolded as **`(hF : ∀ c c', F (c + c') = F c + F c')`**" |
| 9 | `HF2-d` — candidate 2's recorded profile | **`A5-ker`** | H-B `:116`, **`hexSubstratum_not_A5`** |

**Row 3 is the silence, and it is reported as silence.** The source names the condition without saying
which statement of it is meant, and **this round supplies neither**: it neither reads the kernel
statement into a manuscript sentence nor the manuscript statement into a kernel one. Nothing in this
round rests on resolving row 3 — `HF1-b`'s assignment finding is carried by the axis words, not by the
which-A5 words, and `HF5`'s gate does not consult row 3.

**The two statements are reported as two.** No identification between them is built, assumed or
needed, and neither of them is adopted by this round.

**Strength: high for rows 1, 2, 4–9; full strength that row 3 is a silence.** The freeze predicted
every row identifiable at medium strength, with a silent row a live outcome; **one row landed silent,
which the prediction names in terms as live, and the prediction is not amended.**

---

# `HF4` — the independence audit

**Frozen sentence for the outcome reached — `HF4-independent`:**

> The walk recorded in this note reaches every target, prediction, gate and frozen status sentence
> of the freeze and finds none whose determination is stated in terms of how the source
> reconciliation lands. **The sibling results present at the mandated base are recorded as present
> and are read into no determination.**

## The walk

| object | the sentence that shows it is reached without any statement about how the reconciliation lands |
| --- | --- |
| `HF0` | Settled by locating seventeen named passages in pinned blobs; the reconciliation's surface is `papers/`, which is outside this round entirely, and no listed passage is one it would move. |
| `HF1-a` | Settled by a Lean-tree sweep at a commit fixed **before** any sibling landing, plus H-D's merged quotation. Neither input can carry a reconciliation outcome. |
| `HF1-b` | Settled by quoting two conditions from a blob unmoved at the base and reading their axis assignment off H-D's own words. Neither condition mentions the entailment question. |
| `HF1-c` | Constructed to hold the question at arm's length: it records that H-D named it and that H-D-SR's freeze scopes it, and records the presence of the sibling result as a fact about the base. **No branch of it turns on which form prevailed.** |
| `HF1-d` | Settled by quoting round H-B's own open owner question, which is about class admissibility and not about the sources. |
| `HF2-a` | Settled by quoting `../PROGRAMME.md` §6's entry. |
| `HF2-b` | Settled by quoting H-A's five items and H-B's record of which its candidate fixes. |
| `HF2-c` | A bounded search whose surface is named and **excludes both sibling result notes in terms**. |
| `HF2-d` | Settled by quoting each citing round at its own scope; both carriers' profiles are kernel theorems from merged rounds. |
| `HF3` | Each row is carried by a source's quoted words, and row 3 is reported as silence rather than resolved. **No row is resolved by an expected reconciliation outcome.** |
| `HF4` | Its own subject is this freeze, not the corpus. |
| `HF5` | Its gates are stated over `HF1`'s and `HF2`'s outcomes only. The gate on `HF5-A5` forbids reaching that outcome "on an anticipated, assumed, or preferred outcome of round H-D-SR's execution" in terms; the outcome reached is `HF5-H-C`, carried by `HF2-a`, `HF2-b`, `HF2-c` and by `HF1-a`'s and `HF1-b`'s quoted unmet conditions. |
| `HF6` | Names decisions and makes none; decision 1 is the propagation question, **named and left open**, which is naming and not anticipating. |
| every prediction | Each recorded reason cites pinned blobs, recorded readings or the freeze's own construction. **None names the reconciliation.** |
| every frozen status sentence | Read one at a time. None states a resolution of the entailment question; `HF1-c`'s three sentences state in terms that no outcome is consumed, assumed, anticipated, ranked or preferred. |

## The sibling results at the base, recorded as present

| file | present at the mandated base | read into a determination |
| --- | --- | --- |
| `../round-h-d-source-reconciliation/result.md` | **yes** | **no** — not opened |
| `../round-h-e-h3-closure-bridge/result.md` | **yes** | **no** — not opened |

Round H-E's execution has additionally left traces in three files this round reads for other reasons:
`../PROGRAMME.md` §8's refreshed one-line state, `verification/ROADMAP.md`'s round H-E section, and a
new guard section in `verification/lean/edge_rigidity_probe.py`. **Their presence is a fact about the
base. No determination of this round consumes any of them**, and `../PROGRAMME.md` §8 is quoted at
`HF0` item 6 only because that item requires §8 as it stands at the base.

**This target's finding is about this round**, not about the corpus, and it is reported as such.

**Strength: high.** **As predicted.**

---

# `HF5` — `next-round`: the adjudication

**`HF5` adds no evidence of its own.** It is this round's reading of `HF0`, `HF1`, `HF2` and `HF3`
through the two frozen gates, and it is **this round's adjudication over the merged record and never
a theorem**.

## The gates, reported requirement by requirement

**The gate on `HF5-H-C`**, in the freeze's words, with each requirement reported:

| requirement | reported |
| --- | --- |
| `HF2-a` reports what the `axis-HC` control plane must fix | **satisfied** — the entry requires the scaling and the convergence topology frozen before execution, the conservative limit derived first and then the viscous correction, and Euler and Navier–Stokes reported separately; quoted in full at `HF2-a` |
| `HF2-b` reports it item by item | **satisfied** — five items, one row each, four unfixed on the record and the fifth unfixed on the record while fixed for one candidate by its own construction |
| `HF2-c` finds no blockage on its named and bounded search | **satisfied** — nine named files, twelve search terms, no passage stating that the control plane cannot be written and none making an unfixed item depend on an input the record does not carry |
| `HF1` reports at least one `axis-A5` item whose enabling condition is **unmet at the base, quoted, and not merely unexamined** | **satisfied, twice over** — see below |
| `HF5-H-C` **may not** be written on `HF1`'s blockage alone | **observed** — the `axis-HC` side is carried by `HF2-a`, `HF2-b` and `HF2-c` independently of anything `HF1` reports |

**The two unmet enabling conditions, quoted and re-checked, not merely unexamined.**

1. `HF1-a`: round H-D's own enabling condition for its question (a) is "an existing result, anywhere
   in the tree, establishing observer-admission for a concrete substratum that fails A5 — then the
   counterexample is reported exhibited, with that result cited. **None exists at the base.**" This
   round **re-ran** the search on a named surface of 166 modules at a commit later than H-D's own
   enumeration base, with its terms recorded, and found none. Unmet, quoted, re-checked.
2. `HF1-b`: round H-D's two settling conditions for its question (c) are quoted in full, and H-D's own
   closing words on them read "**Neither is found at the mandated base.**" Both are stated at the
   H3–H7 level, and H-D records the obstruction for question (c) as "**no evidence at the H3–H7 level
   exists at the base**". Unmet, quoted.

**The gate on `HF5-A5`**: it may be written only if `HF1` names an `axis-A5` question whose enabling
condition is **met at the base, with the meeting quoted**. **No such question is named.** Question
(a)'s condition is unmet (`HF1-a`); question (c)'s two conditions are unmet (`HF1-b`); the
source-level question is one this round may not consume and whose route runs through an outcome of a
concurrent execution (`HF1-c`); and the admissibility of round H-B's class is an owner decision
(`HF1-d`). The gate forbids reaching `HF5-A5` through any of those three routes in terms, and **none
is taken**. `HF5-A5` is therefore unavailable.

**Why not `HF5-OWNER`.** `HF5-OWNER` is written when "the merged record discriminates in neither
direction: both axes' next steps are writable, or neither is, on the record searched". On the record
searched the record does discriminate: the `axis-HC` next step is a control plane whose required
fixings `../PROGRAMME.md` §6 assigns to that round itself and whose writing nothing on the searched
surface blocks, while every `axis-A5` question `HF1` enumerates has an enabling condition that is
unmet at the base, quoted as unmet, and — for both of question (c)'s — situated by the record at a
level at which no evidence exists at the base. `HF5-OWNER` remained live throughout and is not
reached.

## The outcome

**Frozen sentence for the outcome reached — `HF5-H-C`:**

> On the merged record at the mandated base, and as **this round's adjudication over that record
> rather than as a theorem**, the programme's next round on the axis round H-B opened is round
> `H-C`: the items its control plane must freeze are enumerated in this note and are **fixed
> nowhere here**, and the live questions of the other axis are gated at the base by the items named
> in this note. **What follows is exactly this and no more**: a round is named as the next one to
> write. No obligation of the H1–H7 ladder moves, none is closed, no limit is taken, no scaling map
> is fixed, no carrier is chosen, no status is asserted for `A5-ker` or for `A5-ms`, and nothing
> here says that round `H-C` will succeed or that a hydrodynamic limit exists.

**Strength: medium** — the strength jointly reached by the targets it rests on, whose weakest
load-bearing links are `HF1-a` and `HF2-c`, each a negative on a named and bounded search. **As
predicted**, and reported at the strength the freeze fixed for it and no higher.

## The three necessity separations, kept apart, and what `HF5` rests on

> **THE A5 ANTI-CONFLATION CLAUSE, carried at this mention — the three necessity separations.**
> `A5-ker`, the kernel's A5, is additivity of the substratum rule over the alphabet —
> `∀ c c' : 𝒮.ι → 𝒮.V, 𝒮.R.F (c + c') = 𝒮.R.F c + 𝒮.R.F c'`, at
> `verification/lean-mathlib/OIBridge/SubstratumInterfaceAudit.lean:102`. `A5-ms`, the manuscripts'
> A5, is linearity of the wave equation, at [Substratum §3.1]. **These are two statements, and no
> identification theorem between them exists in the tree**; `waveSubstratum_A5`
> (`SubstratumInterfaceAudit.lean:211`) is an instance result for the one rule where both are in
> play and identifies nothing. **This round builds no identification, assumes none, needs none, and
> writes "A5" unqualified nowhere**: every target, prediction, gate, status sentence and hazard here
> that touches A5 says which of `A5-ker` and `A5-ms` it means, and a sentence that says "A5" without
> saying which is a defect of this round.

Round H-D's three questions are three, and this adjudication keeps them three
(`round-h-d-a5-status-adjudication/result.md:656–678`):

| separation | what the merged record says, at H-D's own scope | what `HF5` takes from it |
| --- | --- | --- |
| **bare-OI necessity** — question (a) | "No derivation of A5 from bare OI was found on the record searched … **This is not a proof that no such derivation exists** … **UNDECIDED**", obstructed by an absent carrier | Only the **obstruction's standing at the base**, re-checked at `HF1-a`. **Nothing about what OI does or does not force** is taken, asserted or implied. |
| **quantum-route necessity** — question (b) | "A5 is used at eight located steps of the reconstruction chain, and each located use is quantum-specific in this round's sense", at medium strength | **Nothing.** No gate, no requirement and no sentence of `HF5` consults question (b). What the quantum route needs is not an input to which hydrodynamics round comes next, and is not made one here. |
| **hydrodynamic necessity** — question (c) | "**UNDECIDED** … *Obstruction: no evidence at the H3–H7 level exists at the base*", with two settling conditions both stated at that level | Only that **both settling conditions are unmet at the base, in H-D's own words**, and that the record situates both at the H3–H7 level. **No status for `A5-ker` or `A5-ms` relative to the hydrodynamic route is drawn**, in either direction. |

**The separation is load-bearing and is stated in terms.** `axis-A5` is **not** reported gated because
round H-B has failed to supply H3–H7-level evidence; round H-B was never chartered to supply it, and
round H-D's guardrail — "**H-B shows that A5 is not needed to obtain a promising reversible fluid
candidate with the right microscopic ingredients; it does not yet show that A5 is unnecessary for an
actual Euler/Navier–Stokes limit**" — is carried here unchanged and in both its halves. What is
reported is narrower and is H-D's own: the conditions that **H-D recorded** as settling its question
(c) are stated at a level at which **H-D records** that no evidence exists at the base. That is a fact
about where those two conditions live, quoted from the round that wrote them, and it is neither a
judgement about `axis-A5`'s promise nor a status for either statement of A5.

---

# `HF6` — the owner decisions, named and not made

**Frozen sentence — `NAMED, NONE MADE`:**

> The owner decisions this axis carries are assembled in this note — the propagation of the
> negative resolution, the admissibility of the class round H-B's candidate lies in, the reach of
> the `R7-HY*` guard family to prose-only rounds, which later action carries out the refresh of
> the programme's one-line state, which substratum a round `H-C` would run on, and whether that
> round is commissioned before or after round H-E's result lands — and every one of them is named
> here and made nowhere.

**The five the target fixes.** The freeze's `HF6` target lists five, and its *Settled by the owner*
item 6 states in terms that **"`HF6` names five decisions, not six"**. The frozen sentence above
enumerates six. The sentence is byte-fixed and is written verbatim; the divergence between it and the
frozen target list is recorded in the discrepancy section and repaired nowhere. **The sixth is made
nowhere either**, so the sentence's operative claim holds of it whichever list governs.

| # | the decision | the round that recorded it, quoted |
| --- | --- | --- |
| 1 | **whether the negative resolution of the entailment question propagates to the statements carrying the open form** | H-D `:492–494`: "five statements of the entailment question stand in the corpus, four in the open form and one resolved in the negative, and **whether the negative resolution should propagate to the other four is an owner decision this round records and does not make**" |
| 2 | **whether the class round H-B's candidate lies in is admissible OI physics** | H-B `:58–59`: "**Whether the A1–A4, ¬A5 class counts as admissible OI physics is a separate question** … and **its admissibility is recorded here as open.**" |
| 3 | **whether the `R7-HY*` guard family extends to prose-only rounds of this programme** | H-D's chronology control 4, at `round-h-d-a5-status-adjudication/result.md:917–918`, as this round's freeze cites it: an owner decision round H-D recorded and did not make |
| 4 | **which later action carries out the refresh of `../PROGRAMME.md` §8** | round H-D's `D7`, whose wording round H-D's status rule 11 already fixes |
| 5 | **the `carrier-question`** — which substratum an `axis-HC` round would run on | enumerated at `HF2-d`: the wave representative with round H-A's linearity gate and its named coarse-variable class and round H-D's `A5S-3c` reading, or round H-B's gas with its conservation and stencil content, its recorded `A5-ker` profile, round H-D's guardrail and round H-B's open owner question |

**Naming a decision is not making it.** This note makes none of them. In particular no carrier is
preferred, ranked or recommended, and no sentence of this note is one from which a recommendation
follows.

**Strength: full, conditional on `HF0`–`HF5`.** **As predicted.**

---

# What no outcome of this round licenses

> **THE A5 ANTI-CONFLATION CLAUSE, carried at this mention — the list of what no outcome licenses.**
> `A5-ker`, the kernel's A5, is additivity of the substratum rule over the alphabet —
> `∀ c c' : 𝒮.ι → 𝒮.V, 𝒮.R.F (c + c') = 𝒮.R.F c + 𝒮.R.F c'`, at
> `verification/lean-mathlib/OIBridge/SubstratumInterfaceAudit.lean:102`. `A5-ms`, the manuscripts'
> A5, is linearity of the wave equation, at [Substratum §3.1]. **These are two statements, and no
> identification theorem between them exists in the tree**; `waveSubstratum_A5`
> (`SubstratumInterfaceAudit.lean:211`) is an instance result for the one rule where both are in
> play and identifies nothing. **This round builds no identification, assumes none, needs none, and
> writes "A5" unqualified nowhere**: every target, prediction, gate, status sentence and hazard here
> that touches A5 says which of `A5-ker` and `A5-ms` it means, and a sentence that says "A5" without
> saying which is a defect of this round.

- **Nothing here answers the entailment question**, and nothing here states, assumes, anticipates,
  ranks or prefers an outcome of the concurrent source reconciliation.
- **Nothing here is a status for `A5-ker` or for `A5-ms`.** Neither is reported a bare-OI requirement
  or not one, required by the hydrodynamic route or not required by it, derived or independent.
- **Nothing here identifies the two statements of A5 or treats one as standing for the other**, in
  either direction.
- **Nothing here rules the class round H-B's candidate lies in admissible or inadmissible as OI
  physics.** That is round H-B's open owner question, named at `HF1-d` and `HF6` and answered nowhere.
- **Nothing here closes, discharges, moves or relabels an obligation.** H1–H7 keep the statuses the
  merged record gives them; naming what a round `H-C` control plane must fix is not fixing it.
- **Nothing here is a continuum statement.** No limit is taken, no PDE is asserted or denied, no
  scaling map is fixed, no convergence topology is chosen, no closure is asserted, no transport
  coefficient is named, and the `S1`–`S5` branch stays closed until H4–H7 exist, per control 5.
- **Nothing here says OI yields Navier–Stokes**, that any candidate has a hydrodynamic limit, or that
  any candidate lacks one. Control 2 applies, and control 8's outcome asymmetry applies to every
  negative reported here: a bounded search returning nothing establishes Open, never Independent.
- **Nothing here says `axis-A5` is closed, blocked or abandoned.** Its live questions are gated at the
  base and the gates are named; that is not a no-go, not an impossibility, and not a judgement that
  the axis is unpromising.
- **Nothing here commissions a round.** The owner commissions it, and a round `H-C` control plane is a
  separate artifact written under its own freeze.
- **Nothing here consumes round H-E's result**, in either direction.
- **Nothing here characterizes the substratum structure sufficient for hydrodynamics.**
- **Nothing here bears on the OI → QM chain, Track B, Track I, Bell or gravity**, in either direction.
- **Nothing here promotes a cited result.** Every kernel statement cited keeps the evidence level of
  the round that proved it.
- **Nothing here makes an owner decision.**
- **No manuscript is edited and none is read.**

**Round H-D's guardrail, carried once more in its own words**
(`round-h-d-a5-status-adjudication/result.md:577–579`):

> **ROUND H-D'S GUARDRAIL, carried at this mention — the list of what no outcome licenses.**
>
> **H-B shows that A5 is not needed to obtain a promising reversible fluid candidate with the right
> microscopic ingredients; it does not yet show that A5 is unnecessary for an actual
> Euler/Navier–Stokes limit.**

---

# Discrepancies — recorded, repaired nowhere

1. **Four pinned start-state blobs moved between the freeze's drafting base and the mandated
   execution base.** `verification/ROADMAP.md` (`5ee35552…` → `78e36c1d…`), `verification/README.md`
   (`d25eb2d4…` → `9983b078…`), `verification/lean/edge_rigidity_probe.py` (`6e334e83…` →
   `f3285558…`) and `../PROGRAMME.md` (`b7b24112…` → `3b8e0223…`). All four movements are sibling
   landings that entered `main` between the two commits. **Recorded; repaired nowhere; and no newer
   content is consumed as evidence.** The freeze anticipated exactly this and its anti-contamination
   invariant governs.
2. **Six of `HF0`'s seventeen passages stand at coordinates different from the freeze's recorded
   readings** — items 1–6 in `../PROGRAMME.md`, each shifted by two lines, and items 7–8 in
   `verification/ROADMAP.md`, each shifted by one. **No listed passage is absent, and no quoted
   passage's text changed.** Recorded at `HF0`'s table; repaired nowhere.
3. **`../PROGRAMME.md` §8's one-line state at the base carries a clause the drafting base did not.**
   It is quoted at `HF0` item 6 because that item requires §8 as it stands at the base; **its content
   is read into no determination of this round**.
4. **Both sibling result notes are present at the mandated base.** Recorded at `HF4` as facts about
   the base; **neither was opened and neither is an input**.
5. **The frozen sentence for `HF0`'s `PARTIAL` outcome speaks of "the bounded search that did not find
   them", and no such failed search exists here**, every listed passage being present. The sentence is
   written verbatim and the divergence is recorded rather than the sentence adjusted. `PARTIAL` was
   written rather than `LOCATED` because the freeze names movement as a `PARTIAL` trigger in terms and
   the weaker reading is the one this round writes; both triggers are satisfiable on their faces, and
   that ambiguity in the freeze is itself recorded here.
6. **The frozen sentence for `HF6` enumerates six decisions; the `HF6` target and the freeze's
   *Settled by the owner* item 6 fix five and exclude the sixth in terms** ("`HF6` names five
   decisions, not six"). The byte-fixed sentence is written verbatim and five are named, per the
   target. The sixth — whether an `axis-HC` control plane is commissioned before or after round H-E's
   result lands — is made nowhere here either; the premise on which the freeze excluded it, that round
   H-E's result would have landed before this execution, **holds at the base**. Recorded; repaired
   nowhere.
7. **Round H-D's chronology control 4 is cited at `HF6` row 3 at the coordinate this round's freeze
   gives for it** (`round-h-d-a5-status-adjudication/result.md:917–918`), as the freeze's own citation,
   and the decision it names is reported no more strongly than as an owner decision round H-D recorded
   and did not make.
8. **No start-state failure of the release gate or the edge-rigidity probe was found at this base.**
   Both ran clean, so no start-state defect is recorded under this head.

---

# Definition budget, and what this round did not write

**Zero.** The execution introduced **no** top-level definition, added **no** Lean module, and edited
**no** existing one. It cited kernel results and re-proved none. It constructed **no** identification
between the two statements of A5, defined **no** predicate for observer-admission or for anything
else, and **did not supply** the carrier `HF1-a` searched for.

**No Lean is written at all**, so **no proof placeholder, no postulated statement and no
decision-procedure escape hatch appears anywhere in this round**, and **this note carries no printed
kernel-dependency line and no kernel-dependency table**. That is stated here rather than left to be
inferred.

---

# Chronology certification, clause by clause

| # | the freeze's clause | certified |
| --- | --- | --- |
| 1 | the preregistration merged alone, before any execution-specific `H-F` object entered the tree | **yes** — the diff of the control plane's second parent against its merge-base adds exactly one file, `round-h-f-a5-hc-decision/preregistration.md`, 1188 lines |
| 2 | the control plane's branch cut from exactly `b78eac870ba3ee9ef9e98659ac933bf97dc62226` | **yes** — the merge-base of the control plane's second parent `a62cb14020545223849eb8ae85d0455589c1c1a1` with that commit is that commit |
| 3 | rounds H-A, H-B and H-D merged before this execution | **yes** — `git merge-base --is-ancestor` holds for round H-A's sealed head `6a8675efca5e5ceeab0195036af1a658b49ace80` and for round H-B's sealed head `54b33c4304bdbda52a09dfe0a06f3e7ff350d832` against the mandated base; round H-D's preregistration and result note are both present there |
| 4 | nothing else required present or absent; the presence of a sibling licenses nothing | **observed** — both sibling result notes are present and neither is consumed |
| 5 | the mandated base is exactly the control plane's merge commit; the execution branches from it and nothing else | **yes** — the execution branch is cut at `59cfd54d2d1db2437a08fdf9a7f27008208c5b88` and its first-parent chain reaches it with no intervening merge from later `main` |
| 6 | the execution's first act is to verify this file's blob at that base | **yes** — `90b0dce3faced6a01460dab3693074400ae5d91a`, verified before any target was executed |
| 7 | the result note pins the freeze by content and records the base | **yes** — both values stand in the start-state table above |
| 8 | no new guard file added and no existing guard modified | **yes** — the execution's diff touches no file under `verification/lean/`, no `.lean` file, and no seal constant of the `R7-HY*` family |
| 9 | before certification the execution never absorbs later main | **yes** — no merge, no rebase, no amend, no force-push; the head's ancestry contains no merge whose second parent postdates the base |
| 10 | the landing is `E` → `L` with no archive pin, the round being non-sealing | **declared** — the landing merge's second parent is to equal the exact-head-certified execution head `E`, and no pin commit follows it |
| 11 | the claim is scoped to the repository record at the execution base | **yes** — every determination above is a statement about named files at `59cfd54d…` and about nothing off them |

**The round's shape, recorded: NON-SEALING, `E` → `L`, no pin, no guard tag claimed.** The `R7-HY*`
names appear in this note only as reads, and belong to the rounds that set them.

**Controls run at the mandated base, before the commit:** `python3 tools/release_gate.py` — **PASS,
17 of 17 steps**; `python3 verification/lean/edge_rigidity_probe.py` — **ALL CHECKS PASS**. No Lean
build was required, the round touching no Lean.
