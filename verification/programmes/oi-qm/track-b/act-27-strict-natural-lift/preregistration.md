# Track B act 27 — the strictly natural lift of every class map at the single-carrier configuration: the vacuity of representative-level naturality under existential lifting, its reversible corollary, and the tuple-level contrast: CONTROL PLANE

Owner-called. This file is the whole of act 27's control plane and is merged **alone**, before any
execution object exists. It is a **one-target round with one gated corollary and two recorded
sub-questions**: the universal statement that every map of the realizable classes at the
single-carrier configuration admits a tuple-level representative carrying a representative-level
lift that is **strictly natural** in act 20's exact sense, with both induced gauge maps the
identity; then, gated on it, the reversible corollary that for a bijection of the classes the
construction supplies inverse lifts and the induced constant transition family satisfies the rungs
of act 21's ladder before factorization other than the standing propagation hypotheses; and, as
sub-questions entering no label, the instantiation at act 25's four shapes and the tuple-level
contrast that a given tuple-level formula need not lift strictly. It re-opens nothing of acts 12
through 26, changes no rung, restates no rung, widens no equivalence, adjusts no gauge group, adds
no generator to act 25's family, and adopts nothing: act 20's three notions, act 21's ladder, act
12's two-sided gauge and act 25's family are **named objects of test**, consumed exactly as landed,
and every verdict is about those objects at the single-carrier configuration and at no other.

It takes up what act 26 left in front of the programme: act 26's `A26-2-UNDECIDED`, with the
observation, recorded in its result note as certifying nothing, that the isometries of the union of
the nine relabelled Fourier circles are more than act 25's family's action on it, the reflection of
a single circle through its two marked points being an isometry that no member of the family
realizes. The question that observation raised — whether some condition of act 21's ladder before
factorization separates act 25's family from the other isometries — is answered here for the one
rung that could: **`L4n`, representative-level naturality at act 20's certified strength**. The
other rungs before `L5` are act 25's hypotheses or follow from them. The freeze's hand reading,
disclosed below and predicted positive, is that `L4n` **separates nothing**: every class map,
isometry or not, admits a strictly natural lift, by an explicit choice of representatives.

It is **not** a classification of any isometries, **not** a statement about the product
configuration or any `|A| > 1`, **not** a statement about a given tuple-level transition family's
own lift — act 24's cell `a4` asserts `L4n` of a **given** family at the **product** configuration
and is untouched — **not** a statement about `L5`, and **not** an attempt to derive or to recognise
quantum evolution, which stays an explicit non-doing. **No lift named here is read as a symmetry, a
dynamics or a unitary evolution of anything**: a lift is a map on dilations named by its
construction and as nothing else.

**Blob identity is authoritative.** The execution guard pins this file by content, by path and by
blob together, so the path below is load-bearing and does not move after this merges.

## The commit vocabulary this freeze uses, fixed first

`AGENTS.md` `§A.37`, at the drafting snapshot, **lines 713–729**, gives a control plane three commit
names, and this file uses them in exactly that sense:

- **`D`, the drafting snapshot** — `22de675ed6a6dd3b98313e6908107404e988352d`, certified `main` at
  the landing of #699 (act 26's execution, `E` = `2b30604c…`, `L` = `7564ab67…`, `P` =
  `3eda05dd…`), whose main-push run 35507975235 is fully green with `R7-CGR` in `ARCHIVED` state,
  ninety `R7-*` tags all `PASS`, and the control-plane base check in mode `B` with nothing to
  evaluate. Every measurement below — every locating coordinate, every pinned blob, every
  name-freedom check and the chronology simulation — was made at `D`, and is a statement about
  `D`. **`D` is never the execution base.**
- **`B`, the mandated execution base** — the certified merge commit on `main` of **this** file, or
  of the latest execution-affecting append-only amendment to it should one be needed. **Before that
  merge exists `B` has no SHA, and this file assigns it none.** The prospective declaration, the
  declared baseline, the chronology control and the precondition rows scoped at `B` all read `B` in
  this sense.
- **`M`, a candidate control-plane merge** — predictive test state only: the synthetic merge
  continuous integration builds for this pull request, against which the `B`-scoped and `D → B`
  rows of the block below are evaluated in mode `M` before the merge. `M` is never execution
  ancestry, never seal state and never historical evidence; the same rows are re-evaluated in mode
  `B` against the actual merge commit by the main-push certification, and **no execution branch is
  created before that certification is green**.

## The three hazards, stated before anything else

**Hazard one — the predicate replaced.** The rung is act 20's `TwistedNatural` and this round's
target is act 20's `StrictNatural`, each **as its module declares it**: quantified over **every**
matrix `L`, `U`, `K` of the dilated carrier, not only the admissible ones; the induced maps fixed
**before** the quantifier over inputs; the two gauge classes act 12's `LeftFibreGroup` and act 11's
`WeakAnchorStabilizer a₀` and no paraphrase of them. A "normalizer" formulation, an
"intertwining" formulation, a formulation over admissible dilations only, or a formulation with the
induced maps chosen per input has changed the object, and a theorem about it earns nothing unless
its connection to act 20's predicate is itself proved in the kernel. Every statement of this round
names `StrictNatural (0 : Fin 1) Ψ` or the conjuncts of `LadderConds` verbatim.

**Hazard two — the class map confused with a tuple map.** The target is **existential over
tuple-level representatives**: it says that for every map of the classes **some** tuple-level map
inducing it has a strictly natural lift. It does **not** say that a **given** tuple-level family —
act 25's relabelling formula, act 20's `RelabelTransition`, any family act 24 tested — has one; the
sub-question `A27-T` is frozen precisely to certify that the tuple-level statement is **false** for
one such formula. Act 24's cell `a4`, which asserts `L4n` of a given family at the product
configuration, is therefore touched by nothing here in either direction, and act 20's
`STRICT-NO`, which is about the one lift act 20 built, stands. A result note that writes "every
transition family satisfies `L4n`" has written forbidden sentence 3.

**Hazard three — the choice of representatives disguised.** The lift is built from a **choice**:
one base dilation per class and one base dilation per image class, through `Classical.choice`, and
the identity off the admissible set. Nothing about the lift is canonical, unique, continuous,
linear, or induced by any map of the carrier, and nothing of the kind is claimed; the theorem
asserts existence. A note that calls the lift "the" lift, "the natural lift" or "canonical" has
written forbidden sentence 5.

## The round's shape, declared first, in `§A.37`'s terms — under the manifest protocol

**This is a SEALING round.** `AGENTS.md` `§A.37`, **lines 858–863** at `D`:

> 1. **A sealing round — a round whose preregistration prospectively owns seal
>    state: either it creates new seal and pin state, or it explicitly takes
>    ownership of changing existing seal state — takes a pin commit `P`, and `P`
>    is mandatory.** `P` writes the round's own **manifest record** —
>    `sealed_head` = `E` and `merge` = `L`, under `verification/seals/` — and not
>    a legacy constant; see *Sealing through the manifest* at the end of this

This freeze **creates new seal state**, in the representation `SI-3` left in force — **data under
`verification/seals/`, no constant in the guard file** — and carries it in two places at two times:

| object | where it lives | state during execution | state from `P` |
| --- | --- | --- | --- |
| the mandated execution base | the **prospective declaration** in `verification/lean/edge_rigidity_probe.py`, `_MANIFEST_PROSPECTIVE = {'NLV': B}` | **declared**; the validator classifies `NLV` as `EXECUTION` against it | **removed** by `P`; a stem both declared and recorded is a failure |
| the declared integrity baseline | `_MANIFEST_BASELINE = {'base': B, 'authorized': ('NLV',)}`, in the same file | the seals tree at `B`, read from git, plus the one addition this freeze authorizes, by stem | unchanged; `NLV.json` is the authorized addition, validated by content |
| the round's manifest record | `verification/seals/NLV.json` | **absent** | **written by `P`**: `{"round": "NLV", "kind": "sealed", "base": B, "sealed_head": E, "merge": L}`; the validator classifies `NLV` as `ARCHIVED` |

So the round lands **`E` → `L` → `P`, and `P` is mandatory.** `P` is the one pin-only commit that
writes `NLV.json` and removes the `NLV` entry from the prospective declaration, and touches nothing
else. Without `P` the round sits at `LANDED-PENDING-PIN`, permitted at `L` itself, and every head
descending from that unpinned landing fails as *seal pending*.

**No legacy seal constant is written, at any commit of the round.** Nothing matching
`_NLV_(BASE|SEALED_HEAD|MERGE)` exists at any commit, `SI-3`'s standing contract holds at every
head, and the execution's guard clause certifies chronology through one keyed call,
`_si2_authority('NLV', tag='R7-NLV')`, and never through a per-round constant.

### The lifecycle derivation, and why it comes out SEALING

The rule the owner set for act 19 and act 21 is carried: a round is sealing **if and only if** its
execution creates a new formal object whose chronology matters to the result.

1. **The execution creates new formal objects.** A new Lean module carrying this round's named
   results — the universal lift theorem, the reversible corollary, the four instantiations and the
   tuple-level contrast.
2. **Their chronology is load-bearing.** The round's claim is that its target, its route, its gate
   and its execution order were frozen — here, in this file — before any kernel work, and that no
   statement was strengthened or weakened after a proof attempt; a validator-certified ancestry
   rooted at this control plane's merge commit is what makes that checkable.
3. **A new module with new named results is new seal state**, which only a sealing round's `P` may
   record.

**Therefore act 27 is SEALING, and it owns no other seal state.**

### The tag, the stem, the module and the round directory are free at `D`

At `D`:

- `git grep -l -- 'R7-NLV'` returns nothing anywhere in the tree, and the tag is absent from the
  **ninety** `R7-*` tags `verification/lean/edge_rigidity_probe.py` carries at `D`.
- `git grep -l -- 'NLV'` returns nothing anywhere in the tree — the bare three-letter form occurs in
  no text file and in no binary artifact, as a word or as a substring — so a bare-stem search for it
  is unambiguous, and `git grep -l -- '_NLV'` returns nothing.
- No record `verification/seals/NLV.json` exists; the thirty-one records at `D` use the stems
  `A12P`, `A6D`, `A6I`, `A6P`, `ABR`, `CGR`, `CLG`, `CTI`, `HYA`, `HYB`, `HYE`, `OGC`, `OGS`, `OLG`,
  `OLN`, `OLT`, `PC4`, `PC4S`, `PQT`, `RBR`, `RNC`, `RNT`, `SGT`, `SI1`, `SI2`, `SI3`, `TCF`, `TRJ`,
  `TSG`, `WTS` and `XTS`, and no substring search for `NLV` reaches any of them.
- **The stem was chosen for its reading and checked before anything was written**: `NLV` —
  natural-lift vacuity, the proposition this round tests — matches nothing at `D`. `SNL`, the first
  candidate, was rejected because the bare form occurs in one file at `D`; `LVT` occurs in three.
- **The target names are free.** `git grep -l -- 'A27-'` returns nothing, and so does the
  theorem-name prefix `a27_`.

**The round directory and the module name are free at `D` too.** `git grep -l -- 'act-27'`,
`git grep -l -- 'strict-natural-lift'` and `git grep -l -- 'StrictNaturalLift'` return nothing.
The round directory is `verification/programmes/oi-qm/track-b/act-27-strict-natural-lift/` and
the module is `verification/lean-mathlib/OIBridge/StrictNaturalLift.lean`.

### What this round does NOT own, named exhaustively

It alters **no existing manifest record**. The thirty-one records under `verification/seals/` at
`D` — the seals tree `73927a618072453213fd27189aceff76b6b499e5`, twenty-five `sealed` and six
`base-only` — are **read and never written**; the one addition this freeze authorizes is `NLV`. It
writes **no legacy constant**. It does not re-pin, re-derive or re-declare any other round's seal:
`CGR.json`, `OGC.json`, `OGS.json`, `RNT.json` and the rest belong to the rounds that set them.
Inside the guard file the execution **adds** the `R7-NLV` clause and **sets** the two stem-free
declarations named above — and changes nothing else in the file. **No contract of any closed round
is superseded**, and the measurement that none needs to be is recorded in the chronology section.

## Provenance — what this freeze carries from acts 7, 11, 12, 17, 18, 20, 21, 24, 25 and 26, from the library, and what is its own

**The rule.** Act 27 consumes act 12's fibre-Gram data, phase equivalence, realizable set, orbit
theorem and gauge lemmas, act 11's weak anchored stabilizer, act 7's admissible dilations, act 20's
three notions and its lift, act 21's ladder and single-carrier configuration, act 25's family, and
act 26's recorded observation, exactly as landed, **unchanged**, and adds one universal lift
theorem, one reversible corollary, four instantiations, one tuple-level contrast and its own
lifecycle. **No definition is introduced.** Every rung is the declaration act 21's merged module
carries, pinned by blob at `B`; **no rung is restated in this round's module**; every notion of
naturality is act 20's declaration, consumed and never redefined; every gauge class is act 12's or
act 11's declaration, consumed and never redefined.

| source (frozen blob, line range) | this file | status |
| --- | --- | --- |
| act 12's `LeftFibreGroup`, `TwoSidedGauge.lean` `4bba2040…` lines 78–79; `FibreGram`, lines 95–97; `GramPhaseEquiv`, lines 102–103; `RealizableGram`, lines 108–110 | the left gauge class, the fibre-Gram data, the quotient and the realizable set every statement quantifies over | **consumed unrestated** |
| act 12's `fibreGram_apply`, line 115; `sh1_necessity`, line 168; `sh1_sufficiency`, line 1070 | the entry formula of the fibre-Gram data; every realizable tuple is the fibre-Gram data of an admissible dilation and conversely | **consumed** |
| act 12's `fibreGram_left_mul`, line 207; `left_preserves_admissible`, line 217; `one_leftFibreGroup`, line 226; `fibreGram_mul_weak_apply`, line 242; `weak_mul`, line 252; `gramPhaseEquiv_of_twoSided`, line 263; `weak_diagonal_phase`, line 723; `twoSided_of_gramPhaseEquiv`, line 752; `twoSided_slice_iff`, line 826 | **the identification of classes with two-sided gauge orbits** (obligation 1 below): two unitaries have phase-equivalent fibre-Gram data exactly when they are two-sided related; the left action is invisible; the right weak action is the phase action | **consumed** |
| act 11's `StrongAnchorStabilizer`, `CoherentLiftGauge.lean` `8d171777…` lines 101–102; `WeakAnchorStabilizer`, lines 114–116; `strong_mem_weak`, line 140; `weak_anchor_coeff_norm_one`, line 146; `strong_eq_one_of_ancilla_subsingleton`, line 193; `weak_preserves_admissible`, line 273 | the right gauge class; its coefficients are unit phases; at one ancilla the strong class is trivial; the right action preserves admissibility | **consumed** |
| act 7's `AdmissibleDilationAt`, `DilationChoice.lean` `7e3a8222…` lines 134–136 | the admissible dilations every lift is stated over | **consumed unrestated** |
| act 17's `GramTrajEquiv`, `GramTrajectorySelection.lean` `afc22cfc…` line 121, and its equivalence lemmas, lines 141–170 | named where act 18's declarations mention it; nothing here uses it | **consumed unrestated** |
| act 18's `ProperAt`, `PropagatesFrom`, `IntermediateCrossTimeStructure.lean` `cb14c43b…` lines 167–171, 186–193 | the two standing hypotheses of `LadderConds`, named as what the corollary does **not** assert | **consumed unrestated** |
| act 20's `StrictNatural`, `RepresentativeNaturality.lean` `4c1137f3…` lines 106–108; `TwistedNatural`, lines 128–135; `OrbitNatural`, lines 150–154; `RelabelTransition`, line 167; `RelabelLift`, line 181; `rnt1_strict_imp_twisted`, line 192; `rnt2_lifting_property`, line 226; `rnt2_admissible`, line 245; `rnt3_law_exact`, line 375; `rnt4b_not_strictNatural`, line 563 | **the exact predicate** every statement names; strict implies twisted with the identity maps; act 20's lift of the relabelling, twisted-natural and not strictly natural, consumed as landed and revised by nothing | **consumed unrestated** |
| act 20's freeze `131783f4…`, lines 556–602, the three notions in the freeze's wording | quoted in the objects section; the kernel's forms govern | **consumed as landed** |
| act 21's ladder `L0`–`L5`, `OrbitLawRigidityTwisted.lean` `860daac4…` lines 85–196, `LadderConds` at lines 179–196 with its `L4n` conjunct at lines 190–195 | the rung conjuncts the corollary states, written out from `LadderConds`; the `L4n` conjunct verbatim | **consumed unrestated** |
| act 21's freeze `316d635a…`, lines 796–866, `L4` in two parts, in particular **lines 852–866**, the freeze's own analysis that the intertwining conjuncts bind a lift only through the lifting obligation | quoted below as the provenance of this round's reading | **consumed as landed** |
| act 21's single-carrier configuration, its freeze lines 1263–1264 | the one configuration of every label of this round | **carried unchanged** |
| act 24's `realizable_entry_ne_zero`, `OrbitGeometrySelector.lean` `ce9d1aa0…` line 385 | every entry of a realizable tuple at `Γ₀ ≡ ¼` is nonzero, which is what makes the common stabilizer the global phase | **consumed** |
| act 24's result `a2719d5f…`, §8, lines 585–714, cell `a4` at lines 607 and 645–649 and its obstruction at lines 691–698 | quoted below as the cell this round does **not** decide, with the reason | **consumed as landed, untouched** |
| act 24's `conj_gramPhaseEquiv`, line 1541, `realizable_conj`, line 1523 | the conjugation's descent and realizability, for the instantiation `A27-H` | **consumed** |
| act 25's family, its four generators and the transpose relation, its freeze `f4892f64…` lines 679–753; `relabel2_realizable`, `relabel2_gramPhaseEquiv`, `transpose_admissible`, `transpose_single_valued`, `transpose_descends`, `OrbitGeometryIsometries.lean` `954fbdda…` lines 72–488 | the four shapes' class maps for the instantiation `A27-H`, each shape's realizability and descent consumed | **consumed unrestated** |
| act 26's result `76ebf0ff…`, §19, lines 923–974, items 3–5 | the observation this round takes up, quoted below as an observation and consumed as nothing | **consumed as an observation** |
| act 26's `featureVec`, `normalizedSet`, `IsSurjIsometryOn`, `OrbitGeometryRigidity.lean` `3e153841…` lines 99–116 | named as what this round does not use: no metric, no feature vector and no geometry enters any statement | **untouched** |
| the attestation set, act 21's freeze lines 1033–1043 | carried verbatim, asked at **every** boundary of this round, with the prior-knowledge rule act 25's packaging fixed | **carried, re-scoped** |
| THE CLAUSE, act 21's freeze lines 1897–1905 | carried verbatim with "Act 21" → "Act 27" in its first sentence | **carried, one substitution** |
| act 13's `P0` threading sentence, its result `2c38dbf1…` lines 267–273 | named as what this round does not touch | **untouched** |
| everything else — the geometry, the isometries, the censuses, the headlines, the `P0` sentences of acts 21 through 26, act 24's twelve verdicts and its cells, act 25's cells, act 26's four labels | not this round's | **untouched** |

### Library provenance — what the kernel's library holds at `D`, read and not consumed as a step

The kernel's library is Mathlib at commit `db584cd6d46c92f209a44c0f1c829460d327499d` under
`leanprover/lean4:v4.33.0`, as `verification/lean-mathlib/lean-toolchain` and the lake manifest
pin it at `D`. One drafting-time reading of it is recorded, a statement about what is **available
to be used** and not a step of any proof: `Matrix.unitaryGroup`, `Matrix.mem_unitaryGroup_iff`,
`Matrix.diagonal`, `Matrix.diagonal_mul_diagonal`, `Matrix.mul_assoc`, `Classical.choice` and
`Classical.choose_spec` are what the construction needs beyond this repository's own theorems,
and each is in `Mathlib/LinearAlgebra/UnitaryGroup.lean`, `Mathlib/Data/Matrix/Diagonal.lean` or
the core library. No theorem of the literature is provenance of anything here.

## Locating controls — the governing passages at `D`, each with a coordinate

Every quotation below is verbatim from a blob pinned in the start-state table, with its file and
line coordinate at `D`.

### The lifecycle rule that fixes this round's base

`AGENTS.md`, **lines 703–706**:

> The control plane's **merge commit is the mandated execution base**. The
> execution branches from exactly that commit and from nothing else, and its
> first act is to verify that the preregistration at that base has the blob the
> freeze names, before any target is executed.

**The mandated base, stated as this round's own commitment.** The execution branches from the merge
commit of **this** control-plane pull request and from **nothing else**, after that commit's
main-push certification — the four jobs, the control-plane base check in mode `B` included — is
fully green, and not before. **Its first act is to verify that the preregistration at that base
carries the blob this freeze names**, before any target is executed, and to record the verification
in the result note. If the blob differs, the execution records the discrepancy and does not repair
the freeze.

### The three commit names, and the machine-checkable form

`AGENTS.md`, **lines 731–732** and **763–765**:

> Every mechanical precondition names its evaluation scope explicitly as at "D",
> at "B", or from "D" to "B".

> **Machine-checkable form.** A control plane that wants its preconditions run
> mechanically carries one fenced block whose info string is
> `control-plane-preconditions`.

This file carries that block, at the end of the preconditions section, and the release gate's
`control-plane-lint` and the workflow's `control-plane-base-check` job read it. The base check
evaluates only the control-plane artifacts added or changed relative to the evaluated commit's
first parent, `tools/control_plane_base_check.py` at blob `d6d65782…`; this file is such an artifact
at `M` and at `B`, and is historical at every later commit.

### The manifest protocol, in the passages this round executes under

`AGENTS.md`, **lines 962–972** (the seal record is data), **1024–1035** (the legacy representation
is retired), **1037–1046** (how a sealing round carries its base), **1047–1053** (how manifest
integrity is declared) and **1055–1061** (a closed round's contracts are read over the records it
manifested) are the passages acts 21 through 26 quoted at their own bases, unchanged in wording at
`D` — `AGENTS.md` carries the same blob `a9687b39…` at `D` as at act 26's drafting snapshot.

### The observation this round takes up, and the cell it does not touch

Act 26's result, `76ebf0ff…`, **§19, item 4**, the observation, in the note's own status for it:

> the isometries of the union of the nine relabelled Fourier circles number 36864, against 2304
> for the action of act 25's family on the union; the reflection of any single circle through its
> two marked points, the identity on the other eight, preserves every distance of the union … and
> is the action of no member of the family. **This bears on the sign of `A26-2`'s proposition and
> is not a verdict**

**This round consumes that observation as an observation**: it certifies nothing, it enters no
hypothesis of any statement here, and no statement of this round quantifies over "the isometries"
of anything. The universal target below quantifies over **every** map of the classes, and so its
conclusion holds of every isometry act 26 observed and of every map act 26 did not, without
depending on the enumeration's exhaustiveness.

Act 24's result, `a2719d5f…`, **§8.2, lines 691–698**, cell `a4`'s obstruction:

> **`a4`, the prefix with isometry implies `L4n`.** The step that did not close is the construction
> of a dilation-level map `Ψ`, fixed before the inputs, from an isometry of the class-level geometry
> … What would settle it is a lifting theorem from isometries of the feature geometry on realizable
> tuples to twisted-natural maps on dilations, or a family on the frozen list that is an isometry,
> satisfies the prefix, and fails `L4n`, of which the list has none

**Cell `a4` is not decided by this round, in either direction, and the reason is fixed here so
that it cannot be argued later.** `a4` is stated at act 24's frozen **product** configuration, and
it asserts `L4n` of a **given** transition family `Φ` — the lifting conjunct reads
`FibreGram a₀ (Ψ U) = Φ t (FibreGram a₀ U)` with `Φ t` the family the cell quantifies over. This
round's theorem supplies, for a class map, **some** tuple-level representative with a lift; it does
not supply a lift of a given representative, and `A27-T` below certifies that a given tuple-level
formula can have none. So neither a positive `A27-0` nor its instantiations bear on `a4`, and no
sentence of this round reports `a4` as closed, narrowed, or reversed.

### The freeze's reading, and its provenance in act 21's own analysis

Act 21's freeze, `316d635a…`, **lines 860–866**:

> What the observation says is that the intertwining conjuncts of `N-TWIST`
> bind a lift only **through the lifting obligation (i)**, which ties `Ψ_t` to `Φ t`; whatever content
> `L4n` has on the class the shared theorem produces is carried by (i) and (iii) jointly. The freeze
> records this so that a `L4n-FREE`, if reached, is read as a fact about the notion at act 20's
> strength and not as a surprise, and it **adds no conjunct**

This round's reading is the class-level form of that analysis: when the representative `Φ t` is
**chosen**, obligation (i) binds nothing, and the lift can be built so that (iii) holds strictly.
When the representative is **given**, (i) binds, and the contrast `A27-T` records one representative
that no strictly natural lift satisfies.

## Start state, pinned by blob

Pinned **by blob** at `D`. Blob identity is authoritative: the commit locates the tree, the blob is
what is compared, and the `D → B` rows of the block below require each of these unchanged at `B`.

| path | blob at `D` |
| --- | --- |
| `verification/lean-mathlib/OIBridge/TwoSidedGauge.lean` | `4bba2040c33424fafbc6d31c0d63b86dff33691a` |
| `verification/lean-mathlib/OIBridge/CoherentLiftGauge.lean` | `8d17177799327d648bbbd001cf237e1ac37bd3fc` |
| `verification/lean-mathlib/OIBridge/DilationChoice.lean` | `7e3a8222cedf530f3c109662e7174d72b6358063` |
| `verification/lean-mathlib/OIBridge/GramTrajectorySelection.lean` | `afc22cfc93b244c80e1c55a273dcfda1ddebb121` |
| `verification/lean-mathlib/OIBridge/IntermediateCrossTimeStructure.lean` | `cb14c43b0becfe1a379ae3615d5553723ede9163` |
| `verification/lean-mathlib/OIBridge/RepresentativeNaturality.lean` | `4c1137f35600320b9273c857ec62271341b05cd0` |
| `verification/lean-mathlib/OIBridge/OrbitLawRigidityTwisted.lean` | `860daac4eb20dbe92c35c2b3ca7aaa1ed798e7b8` |
| `verification/lean-mathlib/OIBridge/OrbitLawNaturalityFactorization.lean` | `d41b157a3f38d4ebedbe11ad9682a8693836a383` |
| `verification/lean-mathlib/OIBridge/OrbitLawGaps.lean` | `5ed0dad78d87314dfd9e1a8ec241f479ded1e3e1` |
| `verification/lean-mathlib/OIBridge/OrbitGeometrySelector.lean` | `ce9d1aa05dfdedfb5cac171cfe6379681942195f` |
| `verification/lean-mathlib/OIBridge/OrbitGeometryIsometries.lean` | `954fbddaa7511713a26c316b3b2e0f29497e81d2` |
| `verification/lean-mathlib/OIBridge/OrbitGeometryRigidity.lean` | `3e15384196203939d348f2a313a873818ec4b684` |
| `verification/programmes/oi-qm/track-b/act-26-orbit-geometry-rigidity/preregistration.md` | `521b63ccde0056453f42035ba9d7970be66cf6f9` |
| `verification/programmes/oi-qm/track-b/act-26-orbit-geometry-rigidity/result.md` | `76ebf0ffa512ce2e53868e529d42fbf9895a39d8` |
| `verification/programmes/oi-qm/track-b/act-25-orbit-geometry-isometries/preregistration.md` | `f4892f64e1667244fc262a01fb46b27f9b1ba1ba` |
| `verification/programmes/oi-qm/track-b/act-25-orbit-geometry-isometries/result.md` | `e4441e9a6f121f96089dc2c9e0ca2e2b9a7a33d9` |
| `verification/programmes/oi-qm/track-b/act-24-orbit-geometry-selector/preregistration.md` | `3b61d6c90fe3f05bbf5e791f74d7106e9be2f94a` |
| `verification/programmes/oi-qm/track-b/act-24-orbit-geometry-selector/result.md` | `a2719d5f63c4ce517590ec7fbe8e61bafe04c3a7` |
| `verification/programmes/oi-qm/track-b/act-21-orbit-law-rigidity-twisted/preregistration.md` | `316d635a31f91faebeeebef7688b30002d24b4ca` |
| `verification/programmes/oi-qm/track-b/act-21-orbit-law-rigidity-twisted/amendments/amendment-1.md` | `d140978e6f0031063e7aa4b9bbe3e960d9b7f8e1` |
| `verification/programmes/oi-qm/track-b/act-21-orbit-law-rigidity-twisted/result.md` | `bb02ef41eb221696ffa45c9281b69553c8279cbb` |
| `verification/programmes/oi-qm/track-b/act-20-representative-naturality/preregistration.md` | `131783f48ac492fdfdc46072aee3f39278973622` |
| `verification/programmes/oi-qm/track-b/act-20-representative-naturality/result.md` | `6f2d2c6a70eda2806e41b88b3b9fe5d4292f46db` |
| `verification/programmes/oi-qm/track-b/act-12-two-sided-gauge/preregistration.md` | `5850238f290f0424ff677ff6d5cc2c039b1f58c2` |
| `verification/programmes/oi-qm/track-b/act-12-two-sided-gauge/result.md` | `467d8be147b6ebd91f2eed12404566af74ac779f` |
| `verification/programmes/oi-qm/track-b/act-13-cross-time-invariants/result.md` | `2c38dbf1c79a0aa3eb654a40d6cb527e99349b4a` |
| `verification/programmes/oi-qm/track-b/act-18-intermediate-cross-time-structure/result.md` | `14a2cd8c54946bf0078329402e6f853107b31d9d` |
| `verification/programmes/oi-qm/track-b/act-22-orbit-law-naturality-factorization/result.md` | `b0f9ae48dbb2044de353efe1a0f5a1fab4f88c9e` |
| `verification/programmes/oi-qm/track-b/act-23-orbit-law-gaps/result.md` | `174e790d2cc5c663f9f54ae7daaeca90b51f96e1` |
| `verification/seals/CGR.json` | `5a1c18831a6c215c182c30952f7f9d3fc3b16c95` |
| `verification/seals/OGC.json` | `6674357ff7a51df416b6ec0d747d5325b9a3756a` |
| `verification/seals/OGS.json` | `5436e01852e9999483dd4aff9f605e47575b7415` |
| `verification/seals/RNT.json` | `546965414aa47fa9b9554448e9c5d13d52f81109` |
| `verification/seals/OLT.json` | `8ed0ef5391410db3a112cbe845d27536b7c1ab9b` |
| `verification/seals/OLN.json` | `1552065eeae26b5e07ad1cfe1cdb97b76aefa1bb` |
| `verification/seals/OLG.json` | `8a058df23c07e2b7571672c039a5a7b4f911a339` |
| `AGENTS.md` | `a9687b39c69973d35a2ff81c257687071fd35eca` |
| `tools/control_plane_base_check.py` | `d6d6578201a9cab3fcb91d63f4818b38104fc4e4` |
| `tools/control_plane_lint.py` | `7678dbe25f51e6e6b14f07e8842f1b6d205f4fed` |

Every one of these is read and never written by this round. If any blob differs at `B`, the
execution records the discrepancy and does not repair the freeze — and for the first thirty-six,
the base check has already refused the merge.

### The files this round writes

**The files this round writes are named separately and are not in the table above.** Each is
pinned by blob at `D` all the same, so that a discrepancy in what the round writes onto is as
visible as a discrepancy in what it reads; a difference at `B` in any of them is recorded in the
result note as a discrepancy and the freeze is not repaired.

| path | blob at `D` | what the round does to it |
| --- | --- | --- |
| `verification/ROADMAP.md` | `d51b6f0fb3506122b6d3b93fe124a7f9d4681345` | **read** as the pinned statement of the `P0` row, line 63 at `D`, and **written** only by appending the frozen post-round sentence for the vector reached after act 26's sentence in the same cell; the row's label unchanged |
| `verification/lean/edge_rigidity_probe.py` | `8b2f3bd2c5226af6b8280f96a5c492ad56d65ed8` | the `R7-NLV` clause **added** directly before `# ---- R7-SI1` (line 27438 at `D`), after the `R7-CGR` clause; `_MANIFEST_PROSPECTIVE` (line 10125 at `D`) and `_MANIFEST_BASELINE` (line 10116 at `D`) **set** as the shape section states, the former emptied by `P`; **nothing else**; no contract of any closed round touched; no legacy constant written |
| `verification/lean-mathlib/OIBridge.lean` | `e3c24085f4737d85d4e845208a317b02f7d6c9ad` | one import line added directly after `import OIBridge.OrbitGeometryRigidity`, at line 215 at `D` |
| `verification/lean-manuscript-census.json` | `08282043de2f79bbaf9dd72e47360efa31d30a1c` | one census entry added for this round's module |
| `verification/lean-mathlib/OIBridge/StrictNaturalLift.lean` | — | created by the execution |
| `verification/programmes/oi-qm/track-b/act-27-strict-natural-lift/result.md` | — | created by the execution |
| `verification/seals/NLV.json` | — | created by **`P`**, and by nothing before `P` |

### The anti-contamination invariant, verbatim

> A start-state discrepancy does not license the execution to consume the newer sibling result
> merely because it happens to be present at its mandated base. The round consumes only what its
> freeze says it consumes.

The base is fixed the moment this file merges, and whatever sibling lanes have landed in `main` by
then is a fact about the base's tree and not a fact about this round's inputs. The start-state
table above is the complete list of what this round consumes, and a file present at `B` and absent
from that table is read by nothing in this round. The seals tree at `B` is whatever `B` carries;
the execution records the tree it found, and the declared baseline reads it from git.

## Source scoping, carried from acts 13 through 26

Source identities per act 1's frozen table: **A** = arXiv:2302.10778v3, **B** = arXiv:2507.21192v1,
**C** = arXiv:2309.03085v2. **Only Source A is adjudicated.** **Track I is not touched**, in either
direction; neither branch is evidence for the other.

## Why this round exists, and what act 26 left in front of it

Act 26 established, at evidence level 2, the affine extension, the census with its count of nine,
and the rigid-motion step of the classification, and recorded the classification undecided. Its
result note recorded, as observations certifying nothing, that the union of the nine circles
admits isometries the family does not realize. Three things stand in front of the programme as a
result:

1. **The metric alone does not select act 25's family, on the observation.** Whether some further
   condition the corpus already carries does is the question the observation raised. The
   conditions the corpus carries on a map of the classes at the single-carrier configuration are
   act 21's rungs before factorization: `L1` (realizability preserved), `L3` (bijectivity on
   classes) and `L4d` (descent) are act 25's three hypotheses or follow from them, so every
   isometry act 26 observed satisfies them once lifted to tuples; `L5` is stated only at a product
   configuration; **`L4n` is the one rung left**, and act 24's cell `a4` recorded that no theorem
   in the record connects an isometry of the geometry to a twisted-natural lift.
2. **At one ancilla, both gauge classes are phases, and the dilations over a class are a torsor.**
   `LeftFibreGroup` is block-diagonal over fibres, and with `A = Fin 1` its blocks are `1 × 1`;
   `WeakAnchorStabilizer a₀` fixes every anchored column up to a phase, and with `A = Fin 1` every
   column is anchored. Act 12's `twoSided_slice_iff` says two unitaries have phase-equivalent
   fibre-Gram data exactly when they are two-sided related, and at `Γ₀ ≡ ¼` every entry of an
   admissible dilation has modulus `½`, so the pair `(D, K)` carrying a base dilation to another
   dilation of its class is unique up to the common global phase. **This is the whole of what the
   construction uses**, and each piece is a landed theorem or a one-line reading of a landed
   definition.
3. **The construction is explicit and it is the freeze's hand reading, disclosed here.** Choose one
   base dilation per class and one base dilation over a tuple of the image class; send
   `D · U_ω · K` to `D · W_ω · K`; extend by the identity off the admissible set. The lift is then
   well defined by the torsor uniqueness, carries admissible to admissible by act 12's and act 11's
   preservation lemmas, induces the class map by construction, and commutes with every left and
   every right gauge element **exactly**, on every matrix, because the gauge elements are the `D`
   and `K` of the normal form. **So the freeze predicts, at high strength, that `L4n` separates
   nothing at this configuration**, and the execution is what establishes or refutes it in the
   kernel.

**Each target's proof route is the freeze's reading and not a finding**; whether it closes is for the
execution to establish, and every target may end `UNDECIDED` with its obstruction named. **What is
kept out of this round is named now**: the product configuration and act 24's cell `a4`; any
`|A| > 1`, where the left blocks are not phases and the strong class is not trivial; `L5`; the
metric, the feature vector and every isometry statement — no statement of this round names `d`,
`mixedTriple` or `featureVec`; composition in time, continuity, generators and any one-parameter
structure; the threading and the cross-time representative; and any comparison of any lift with a
unitary, antiunitary or quantum symmetry. Each is a separate question for a separate freeze.

### Why there is no record-check target

Act 24's `GEO4` recorded, at its lines 691–698, that the record holds no lifting theorem from
isometries to twisted-natural maps; act 21's freeze at lines 852–866 recorded the analysis this
round's reading extends; and the artifacts that have entered the record since are acts 25's and
26's executions, whose bearing on the question is the observation quoted above. This freeze quotes
both recordings directly and consumes them; a further search would find the same sentences. **No
record-check target is frozen, and the targets are `A27-0` and `A27-1`, with the sub-questions
`A27-H` and `A27-T`.**

### What act 27 inherits, and consumes without re-proving

Consumed at merged strength. **None is re-proved, strengthened, redefined or enlarged**, and a
merged statement is not enlarged by being consumed.

1. **Act 12's `FibreGram`, `GramPhaseEquiv`, `RealizableGram`, `LeftFibreGroup`, `fibreGram_apply`,
   `sh1_necessity`, `sh1_sufficiency`, `fibreGram_left_mul`, `left_preserves_admissible`,
   `one_leftFibreGroup`, `fibreGram_mul_weak_apply`, `weak_mul`, `gramPhaseEquiv_of_twoSided`,
   `weak_diagonal_phase`, `twoSided_of_gramPhaseEquiv`, `twoSided_slice_iff`**; **act 11's
   `StrongAnchorStabilizer`, `WeakAnchorStabilizer`, `strong_mem_weak`,
   `weak_anchor_coeff_norm_one`, `strong_eq_one_of_ancilla_subsingleton`,
   `weak_preserves_admissible`**; **act 7's `AdmissibleDilationAt`**.
2. **Act 20's `StrictNatural`, `TwistedNatural`, `OrbitNatural`, `RelabelTransition`, `RelabelLift`,
   `rnt1_strict_imp_twisted`, `rnt2_lifting_property`, `rnt2_admissible`, `rnt3_law_exact`,
   `rnt4b_not_strictNatural`**, and act 20's four labels as its note states them.
3. **Act 21's ladder**, as the declarations of its merged module, `LadderConds` included; **act
   18's `ProperAt` and `PropagatesFrom`** as the standing hypotheses the corollary does not assert.
4. **Act 24's `realizable_entry_ne_zero`, `conj_gramPhaseEquiv`, `realizable_conj`**; **act 25's
   family with `relabel2_realizable`, `relabel2_gramPhaseEquiv`, `transpose_admissible`,
   `transpose_single_valued`, `transpose_descends`**.
5. **Acts 20 through 26's historical verdicts are not changed by anything here**: act 20's
   `CLASS-TWISTED-NOT-STRICT` and `STRICT-NO` for the lift act 20 built, act 21's rung verdicts, act
   24's twelve verdicts and seven cells with `a4` undecided, act 25's four labels, act 26's four
   labels and its observations stand as their notes state them.

## The questions, FROZEN — one target, one gated corollary, two sub-questions

> **A27-0.** At the single-carrier configuration, does **every** map of the realizable classes —
> every map on tuples that preserves realizability and respects act 12's phase equivalence on
> realizable tuples — admit a tuple-level representative, phase-equivalent to it on every
> realizable tuple, together with a map on dilations that lifts the representative exactly on
> every admissible dilation, carries admissible dilations to admissible dilations, and is
> **strictly natural** in act 20's exact sense, with both induced gauge maps the identity?
>
> **A27-1.** When the map is moreover a bijection of the classes — injective and surjective on
> realizable classes — can the representative and the lift of `A27-0` be chosen so that the
> inverse class map's representative and lift, built from the **same** choice of base dilations,
> invert them exactly on admissible dilations, and does the constant transition family the
> representative induces satisfy the conjuncts `L0`, `L1`, `L2`, `L3`, `L4d` and `L4n` of act 21's
> `LadderConds` at that configuration, written out verbatim — asked only if `A27-0` reaches its
> label?
>
> **A27-H** (sub-question, entering no label). Instantiated at the class maps of act 25's four
> shapes — the relabelling, the entrywise conjugation, the dilation transpose, and the transpose
> composed with the conjugation — does each admit the lift `A27-0` supplies, each as its own named
> corollary?
>
> **A27-T** (sub-question, entering no label). Is the tuple-level statement false for a given
> formula: is there **no** strictly natural map on dilations lifting act 20's `RelabelTransition σ`
> **itself**, at `σ = Equiv.swap 2 3`, exactly on every admissible dilation?

Each is a question about act 12's exact equivalence and gauge classes, act 20's exact predicate,
act 21's exact declarations and act 25's exact family at the record's single-carrier
configuration. **The round does NOT try to derive Schrödinger evolution**, does not classify any
isometries, does not ask whether naturality is the right condition to impose, does not ask about
continuity or composition in time, and does not ask what any rung means beyond the declaration act
21 froze.

### The correspondence with the owner's direction

| the owner's item | this freeze's target |
| --- | --- |
| the target: every class map admits an admissibility-preserving, strictly natural lift inducing it, with act 20's exact predicate and identity induced maps | `A27-0`: `a27_0_strict_lift`, the conclusion displayed in the objects section with `StrictNatural (0 : Fin 1) Ψ` named verbatim |
| obligation 1: identify classes with gauge orbits; prove the correspondence between class equality and the two-sided phase action | act 12's `twoSided_slice_iff` consumed, with the `A = Fin 1` shapes of the two classes read in the route's step 1 and proved as shared lemmas |
| obligation 2: well-definedness through the common stabilizer `{(λI, λ⁻¹I) : |λ| = 1}` | the route's step 3, the torsor lemma, a shared lemma of the module commit, its nonvanishing input act 24's `realizable_entry_ne_zero` |
| obligation 3: naturality on the full domain; admissibility and its complement gauge-invariant; the identity extension; the choice of representatives disclosed; existence only | the route's steps 4–6; hazard three; forbidden sentence 5 |
| obligation 4: the reversible case with one fixed section for source and target, inverse lifts, descent and reversibility established together | `A27-1`: `a27_1_reversible_lift`, the conclusion displayed with the inverse conjuncts and the rung conjuncts |
| the construction recorded as pre-freeze hand reasoning with a positive prediction at evidence level 2 | the section "What was and was not run", the predictions table |
| the scope: `L4n` imposes no restriction on class maps under existential lifting; the pre-`L5` rungs cannot distinguish `H` from any other bijection; `a4` unchanged | the frozen sentences of `A27-0` and `A27-1`; the locating control on `a4`; forbidden sentences 3, 4 and 9 |
| `L5` at the product configuration named as the next candidate, its selecting power open | the non-doings section's closing statement |
| the guardrails: single carrier only, no product claim, no symmetry/antiunitary/time-reversal reading, no continuity/generator claim, no computation-derived candidate after the freeze | the configurations section, the forbidden-sentence list, the non-doings |

## The strength of the ask, FROZEN

**The ask is one universal existence theorem over all class maps, one gated corollary for
bijections carrying the rung conjuncts, four instantiations and one negative tuple-level
contrast, each earned by its own named route or reported undecided.** It does **not** undertake a
statement about the product configuration, about `|A| > 1`, about `L5`, about act 24's `a4`, about
any given tuple-level family beyond the one formula `A27-T` names, or about the inference of one
target's status from another's beyond the consumptions the freeze places.

**What each verdict would and would not establish, stated in advance.** A positive `A27-0` says
that act 20's strict naturality, read existentially over tuple-level representatives, holds of
**every** class map at this configuration, so that as a condition on class maps it excludes none;
it says nothing about any given tuple-level family, nothing at any other configuration, and
nothing about whether naturality is the right condition. A positive `A27-1` says that every
bijection of the classes induces a constant family satisfying the named rung conjuncts, so that
none of those rungs, read of class maps, separates act 25's family from any other bijection here;
it does not assert act 18's two standing hypotheses of any family and does not assert `L5`. A
positive `A27-T` says that one given tuple-level formula has no strictly natural lift, so that the
tuple-level reading of `L4n` is not vacuous; it revises nothing of act 20, whose `STRICT-NO` is
about a different statement. **No outcome of this round says that any lift is, resembles or
approaches a unitary, an antiunitary, a symmetry, a time reversal or quantum evolution.**

## The objects, FROZEN — act 7's, act 11's, act 12's, act 20's, act 21's and act 25's, consumed; NO definition

`V`, `A`, `a₀`, `Γ`, the dilations, the fibre-Gram data, the phase equivalence and the realizable
set are act 12's and act 7's, consumed without restatement; the two gauge classes are act 12's
`LeftFibreGroup` and act 11's `WeakAnchorStabilizer`, consumed without restatement; the three
notions of naturality are act 20's, consumed without restatement; the rungs are act 21's,
consumed without restatement. In particular:

- **The frozen quotient list** is act 12's `GramPhaseEquiv`, and **no other equivalence may be used
  in any verdict**. Every "equal on classes" of this round is `GramPhaseEquiv`, and nothing is
  taken modulo anything else.
- **Realizable** means `RealizableGram (Fin 1) Γ₀` at the single-carrier configuration, exactly as
  acts 24 through 26 use the word; **admissible** means `AdmissibleDilationAt Γ₀ (0 : Fin 1)`.
- **The predicate** is `StrictNatural (0 : Fin 1) Ψ` of `RepresentativeNaturality.lean` at
  `4c1137f3…` lines 106–108, quantified over every `L`, `U`, `K` of
  `Matrix (Fin 4 × Fin 1) (Fin 4 × Fin 1) ℂ`; the rung is the `L4n` conjunct of `LadderConds` at
  `860daac4…` lines 190–195. Nothing of this round paraphrases either.

### The one frozen configuration

Act 21's single-carrier configuration, and no other: `V = Fin 4`, `A = Fin 1`, `a₀ = 0`,
`Γ₀ ≡ ¼` — act 12's, the one acts 24 through 26 are stated at. **Every label of this round is
earned there.** Nothing is asserted at the product configuration, at `|A| > 1`, or at any other
visible family; the reading that the gauge classes are phases is a reading **at `A = Fin 1`** and
is stated at no other cardinality.

**The one-`|A|`-value caveat, carried from acts 18 through 26 and recorded again.** `|A| = 1` is
the case at which the left blocks are `1 × 1` and the strong class is trivial
(`strong_eq_one_of_ancilla_subsingleton`); the vacuity this round predicts is a statement at that
cardinality, and the freeze predicts nothing at any other.

### The gauge classes at `A = Fin 1`, as the freeze reads act 12's and act 11's declarations

**The freeze's reading, proved as shared lemmas by the execution and not assumed**:

1. `LeftFibreGroup L` at `A = Fin 1` holds iff `L` is unitary and `L p q = 0` whenever `p.1 ≠ q.1`,
   which at one ancilla is every off-diagonal entry; so `L` is diagonal with unit-modulus entries,
   and every diagonal matrix of unit-modulus entries is in the class.
2. `WeakAnchorStabilizer (0 : Fin 1) K` holds iff `K` is unitary and `K p (j, 0) = if p = (j, 0)
   then c j else 0` for some `c`; at one ancilla every column is anchored, so `K` is diagonal with
   the unit-modulus entries `c` (`weak_anchor_coeff_norm_one`), and every such diagonal matrix is
   in the class (`weak_diagonal_phase`).
3. `FibreGram (0 : Fin 1) U i j k = star (U (i, 0) (j, 0)) * U (i, 0) (k, 0)`, by `fibreGram_apply`
   with the one-element sum.
4. `AdmissibleDilationAt Γ₀ (0 : Fin 1) U` holds iff `U` is unitary and `‖U (i, 0) (j, 0)‖ ^ 2 =
   1 / 4` for all `i`, `j`; so every entry of an admissible dilation is nonzero.

### The class-map hypotheses, displayed — what "a map of the classes" is, in every statement

In every statement of this round **a map of the realizable classes** is a map `f` on tuples
together with the two hypotheses written out:

```
(∀ G, RealizableGram (Fin 1) Γ₀ G → RealizableGram (Fin 1) Γ₀ (f G))
  ∧ (∀ G G', RealizableGram (Fin 1) Γ₀ G → RealizableGram (Fin 1) Γ₀ G' →
        GramPhaseEquiv G G' → GramPhaseEquiv (f G) (f G'))
```

— realizability preserved, descent on realizable tuples — and **nothing else**: not surjectivity,
not injectivity, not isometry of any geometry, not continuity, not that it is induced by any map on
dilations. **A bijection of the classes** adds act 21's two `L3` conjuncts in act 21's form:

```
(∀ G G', RealizableGram (Fin 1) Γ₀ G → RealizableGram (Fin 1) Γ₀ G' →
    GramPhaseEquiv (f G) (f G') → GramPhaseEquiv G G')
  ∧ (∀ G', RealizableGram (Fin 1) Γ₀ G' → ∃ G, RealizableGram (Fin 1) Γ₀ G ∧ GramPhaseEquiv (f G) G')
```

### The conclusion of `A27-0`, displayed — what "admits a strictly natural lift inducing it" is

```
∃ (Φ₀ : (Fin 4 → Matrix (Fin 4) (Fin 4) ℂ) → (Fin 4 → Matrix (Fin 4) (Fin 4) ℂ))
  (Ψ : Matrix (Fin 4 × Fin 1) (Fin 4 × Fin 1) ℂ → Matrix (Fin 4 × Fin 1) (Fin 4 × Fin 1) ℂ),
    (∀ G, RealizableGram (Fin 1) Γ₀ G → GramPhaseEquiv (Φ₀ G) (f G))
  ∧ (∀ G, RealizableGram (Fin 1) Γ₀ G → RealizableGram (Fin 1) Γ₀ (Φ₀ G))
  ∧ (∀ U, AdmissibleDilationAt Γ₀ (0 : Fin 1) U →
        FibreGram (0 : Fin 1) (Ψ U) = Φ₀ (FibreGram (0 : Fin 1) U))
  ∧ (∀ U, AdmissibleDilationAt Γ₀ (0 : Fin 1) U → AdmissibleDilationAt Γ₀ (0 : Fin 1) (Ψ U))
  ∧ StrictNatural (0 : Fin 1) Ψ
```

The third and fourth conjuncts are `L4n`'s obligations (i) and (ii) for `Φ t := Φ₀` at `Γ ≡ Γ₀`,
verbatim; the fifth is act 20's `N-STRICT`, from which `TwistedNatural (0 : Fin 1) id id Ψ` — `L4n`'s
obligation (iii) with the identity induced maps — follows by act 20's `rnt1_strict_imp_twisted`,
consumed. **`Φ₀` is existentially quantified**: it is a representative the theorem supplies, and
the first conjunct is what ties it to `f`. A statement with `Φ₀ := f` in place of the existential
is a different theorem, the tuple-level one, and `A27-T` is frozen to show it false for one `f`.

### The conclusion of `A27-1`, displayed

For `f` a bijection of the classes, with `Φ := fun _ : ℕ => Φ₀` and `Γ := fun _ : ℕ => Γ₀`:

```
∃ (Φ₀ Φ₀' : (Fin 4 → Matrix (Fin 4) (Fin 4) ℂ) → (Fin 4 → Matrix (Fin 4) (Fin 4) ℂ))
  (Ψ Ψ' : Matrix (Fin 4 × Fin 1) (Fin 4 × Fin 1) ℂ → Matrix (Fin 4 × Fin 1) (Fin 4 × Fin 1) ℂ),
    [the five conjuncts of A27-0's conclusion for (Φ₀, Ψ) and f]
  ∧ [the five conjuncts of A27-0's conclusion for (Φ₀', Ψ') and an inverse of f on classes,
     stated as: ∀ G realizable, GramPhaseEquiv (Φ₀' (Φ₀ G)) G, and ∀ G realizable,
     GramPhaseEquiv (Φ₀ (Φ₀' G)) G, in place of the first conjunct]
  ∧ (∀ U, AdmissibleDilationAt Γ₀ (0 : Fin 1) U → Ψ' (Ψ U) = U)
  ∧ (∀ U, AdmissibleDilationAt Γ₀ (0 : Fin 1) U → Ψ (Ψ' U) = U)
  ∧ EvolvesTotally (Fin 1) Γ Φ
  ∧ PreservesAdmissible (Fin 1) Γ Φ
  ∧ (∃ Φ₁, ∀ t, Φ t = Φ₁)
  ∧ Reversible (Fin 1) Γ Φ
  ∧ (∀ t (G G'), GramPhaseEquiv G G' → GramPhaseEquiv (Φ t G) (Φ t G'))
  ∧ (∀ t, ∃ Ψₜ αL αR, (∀ U, AdmissibleDilationAt (Γ t) (0 : Fin 1) U →
        FibreGram (0 : Fin 1) (Ψₜ U) = Φ t (FibreGram (0 : Fin 1) U))
      ∧ (∀ U, AdmissibleDilationAt (Γ t) (0 : Fin 1) U →
        AdmissibleDilationAt (Γ (t + 1)) (0 : Fin 1) (Ψₜ U))
      ∧ TwistedNatural (0 : Fin 1) αL αR Ψₜ)
```

The last six conjuncts are `LadderConds`' conjuncts three through eight, in `LadderConds`' order
and wording, at `A = Fin 1`, `a₀ = (0 : Fin 1)`; the ninth conjunct of `LadderConds`, `L5`, and its
first two, `ProperAt` and `PropagatesFrom`, are **not stated and not asserted**. The `L4d` conjunct
is quantified over **all** tuples, as `LadderConds` quantifies it, which is why the route fixes
`Φ₀` to be the identity off the realizable set.

### The definition budget is ZERO, and the whole budget is stated as a number

**The execution introduces NO top-level Lean definition**: no `def`, `abbrev`, `structure`, `class`,
`instance`, `axiom` or `opaque`. Every object is a bound variable or a written-out proposition in
the statement that needs it, as act 25 wrote everything out; the base dilations, the sections and
the lifts live inside proofs, through `Classical.choice`, and inside existential conclusions.
**A definition requires its own append-only amendment**, separately frozen and merged before the
work it affects. Theorems are not budgeted: the execution proves whatever shared lemmas its verdicts
need — the shapes of the two gauge classes at one ancilla, the entry formula, the torsor lemma, the
preservation of admissibility and non-admissibility by both actions, the inverse of a gauge element
being a gauge element — as named results, each printed in the axiom table.

## The route, recorded here as the freeze's reading and not as a finding

**`A27-0`**, for a class map `f`, in the order the execution follows.

1. **Classes are two-sided orbits.** By act 12's `sh1_sufficiency` every realizable tuple is
   `FibreGram (0 : Fin 1) U` for an admissible `U`, and by `sh1_necessity` conversely; by
   `twoSided_slice_iff`, two admissible dilations have phase-equivalent fibre-Gram data iff one is
   `L * U * K` of the other with `L` in the left class and `K` in the weak class. **Obligation 1 is
   act 12's theorem consumed**, and what the execution adds is the `A = Fin 1` shape of the two
   classes, read above and proved as shared lemmas.
2. **The section.** For each realizable class choose one admissible base dilation: for realizable
   `G`, `U_G := Classical.choose` of `sh1_sufficiency`'s existential at `G`; and choose for each
   image a base dilation `W_G := U_{f G}` over a tuple of the class `f G`, realizable by the first
   hypothesis on `f`. **The choice is disclosed and is a choice**: nothing about it is canonical,
   and hazard three governs.
3. **The torsor lemma — obligation 2.** For admissible `U₀` and left `D, D'`, weak `K, K'`:
   `D * U₀ * K = D' * U₀ * K'` iff there is a unit `λ` with `D' = λ • D` and `K' = λ⁻¹ • K`. The
   direction that needs the nonvanishing: `D'⁻¹ D U₀ = U₀ K' K⁻¹` reads entrywise
   `a_i · U₀ (i,0) (j,0) = U₀ (i,0) (j,0) · b_j`, so `a_i = b_j` for all `i, j` since every entry is
   nonzero (the admissibility reading, or act 24's `realizable_entry_ne_zero` through the
   fibre-Gram data), so `a` and `b` are one constant `λ`. **The common stabilizer of the written
   action `U ↦ D U K` is `{(λ I, λ⁻¹ I) : |λ| = 1}`**, and this is the whole of the well-definedness
   argument.
4. **The lift.** On an admissible `U` with class `ω`, write `U = D * U_ω * K` by step 1 and set
   `Ψ U := D * W_ω * K`; off the admissible set set `Ψ U := U`. Well defined by step 3, because
   `λ • W_ω` composed with `λ⁻¹` on the other side is `W_ω`. **The representative** is
   `Φ₀ G := FibreGram (0 : Fin 1) (Ψ U_G)` for realizable `G` and `Φ₀ G := G` otherwise.
5. **Lifting, realizability and admissibility.** `FibreGram (Ψ U) = FibreGram (D W_ω K) =
   K · FibreGram W_ω` by `fibreGram_left_mul` and `fibreGram_mul_weak_apply`, and
   `Φ₀ (FibreGram U) = Φ₀ (K · G_ω) = FibreGram (Ψ U_{K·G_ω})`, the same tuple because
   `U_{K · G_ω}` and `U_ω K` lie over phase-equivalent data and differ by a left element and a
   weak element whose phase part is forced to a global phase by step 3 — the execution proves this
   as the lemma that `Φ₀` is well defined on tuples with the same class and the same phase data;
   `Ψ U` is admissible by `left_preserves_admissible` and `weak_preserves_admissible`; `Φ₀ G` is
   realizable by `sh1_necessity`; `GramPhaseEquiv (Φ₀ G) (f G)` because `W_G` lies over a tuple of
   the class `f G` and the fibre-Gram data of `D W K` is phase-equivalent to that of `W`
   (`gramPhaseEquiv_of_twoSided`).
6. **Strict naturality on the whole domain — obligation 3.** For left `L` and any `U`: if `U` is
   admissible, `L * U = (L * D) * U_ω * K` is admissible with the left factor `L * D` (the class is
   closed under products, `mul_mem`), so `Ψ (L * U) = L * D * W_ω * K = L * Ψ U`; if `U` is not
   admissible, neither is `L * U` — because `L⁻¹` is in the left class and would carry it back,
   `left_preserves_admissible` applied to `L⁻¹` — so `Ψ (L * U) = L * U = L * Ψ U`. The right side is
   the same with `weak_mul`, `weak_preserves_admissible` and the inverse of a weak element being
   weak (its coefficients conjugated). **The identity extension off the admissible set is what
   makes the equations hold on every matrix**, as `StrictNatural` quantifies them, and the
   gauge-invariance of admissibility **and of its complement** is the lemma that validates it.

**`A27-1`**, for a bijection `f`, gated on `A27-0-LIFTS` — obligation 4.

1. **One section for both directions.** Fix the section `G ↦ U_G` of step 2 once. Build `Ψ` for `f`
   with `W_G := U_{f G}`, and `Ψ'` for the inverse class map `f⁻¹` — the map on tuples
   `G ↦ Classical.choose` of the surjectivity hypothesis at `G`, realizable and descending by the
   two `L3` conjuncts — with `W'_G := U_{f⁻¹ G}`.
2. **The inverse.** On admissible `U = D U_ω K`: `Ψ U = D U_{f ω} K` and `Ψ' (Ψ U) = D U_{f⁻¹ f ω} K
   = D U_ω K = U`, because `f⁻¹ f ω = ω` **as classes** and the section is a function of the class;
   symmetrically `Ψ (Ψ' U) = U`. **The two lifts are built from the same section, which is what
   makes the composite the identity exactly and not merely up to gauge**; two lifts built from
   different sections would compose to a gauge move, and the freeze fixes the section for that
   reason.
3. **The rung conjuncts.** `PreservesAdmissible` is `A27-0`'s second conjunct; the `L2` conjunct is
   `rfl`; `Reversible`'s two conjuncts are the two `L3` hypotheses on `f` read through
   `GramPhaseEquiv (Φ₀ G) (f G)` and transitivity; the `L4d` conjunct on realizable tuples is
   descent of `f` through the same equivalence, and on non-realizable tuples is the identity, the
   realizable set being closed under `GramPhaseEquiv` (positivity, rank, the sum and the diagonal
   are each preserved by the phase action — a shared lemma); `EvolvesTotally` takes the iterates
   `t ↦ Φ₀^[t] G₀`, realizable by `PreservesAdmissible` and solutions by reflexivity of the
   equivalence; the `L4n` conjunct takes `Ψₜ := Ψ`, `αL := id`, `αR := id` at every `t`, its third
   part by `rnt1_strict_imp_twisted`.

**`A27-H`.** Each shape's class map is act 25's formula read as a map on tuples: the relabelling
`fun G => fun i => (G (π i)).submatrix τ τ`, realizability and descent by `relabel2_realizable`
and `relabel2_gramPhaseEquiv`; the conjugation `fun G => fun i => Matrix.of fun j k => star (G i j
k)`, by `realizable_conj` and `conj_gramPhaseEquiv`; the transpose as the map
`G ↦ FibreGram (0 : Fin 1) (U_G)ᵀ` through the section, realizability by `transpose_admissible`
and descent by `transpose_single_valued` and `transpose_descends`; the fourth shape the composite.
Each corollary is `A27-0` applied, and its content is that **the class map** of the shape admits a
strictly natural lift — not that act 25's formula does. For the relabelling this is a different
map from act 20's `RelabelLift`, which act 20 proved twisted-natural and not strictly natural;
act 20's note said in terms that it did not rule out another lift, and this corollary supplies one
at this configuration.

**`A27-T`.** Suppose `Ψ` strictly natural with `FibreGram (Ψ U) = RelabelTransition σ (FibreGram U)`
for every admissible `U`, `σ = Equiv.swap 2 3`. For weak `K` with phases `c`, at `U` admissible,
the two evaluations of `FibreGram (Ψ (U * K))` — through strictness as `K · (σ · G)` and through
the lifting equation as `σ · (K · G)` — give, at every coordinate `(i, j, k)` with `G`'s entry
nonzero, `star (c j) * c k = star (c (σ j)) * c (σ k)`; at `j = 2`, `k = 0` this is `c 2 = c 3`,
refuted by `c = (1, 1, 1, Complex.I)` at `U = H(1)`, act 23's real Hadamard dilation, admissible
by `hadamard_z_admissible`. **The witness is one dilation, one weak element and one coordinate**,
named here in advance; the theorem is the negation of the existential over `Ψ`.

## The verdict matrix, FROZEN

| target or sub-question | configuration | assumed | tested | route | explicitly out of scope |
| --- | --- | --- | --- | --- | --- |
| `A27-0` | single carrier | realizability preserved, descent | a representative and a strictly natural, admissibility-preserving lift | classes as orbits; the section; the torsor lemma; the lift; the identity extension | any given tuple-level family; `|A| > 1`; the product configuration; any metric |
| `A27-1` | single carrier | the same, injectivity and surjectivity on classes | inverse lifts from one section; `LadderConds`' conjuncts three through eight | one section for both directions; the rung conjuncts read from `A27-0` | `ProperAt`, `PropagatesFrom`, `L5`; act 24's `a4` |
| `A27-H` (sub-question) | single carrier | act 25's four shapes as class maps | each shape's corollary of `A27-0` | instantiation | act 25's formulas as tuple maps; act 20's `RelabelLift` |
| `A27-T` (sub-question) | single carrier | — | no strictly natural lift of `RelabelTransition (swap 2 3)` itself | the two evaluations at one coordinate | any other formula; any twisted lift |

**Nothing in one row is inferred from another row** beyond the consumptions the rows name: `A27-1`
and `A27-H` consume `A27-0`; `A27-T` consumes nothing and earns nothing.

### The labels of each target, and the three-way discipline

A positive universal label (`A27-0-LIFTS`, `A27-1-REVERSIBLE`) is earned only by a universal kernel
proof of the displayed conclusion under the displayed hypotheses; **`UNDECIDED` by the recorded
statement that it was not reached, with the obstruction named.** `A27-0` and `A27-1` have **no
negative label**: the freeze names no candidate class map without a lift, and a class map found
without one would be a refutation of the universal, recorded as `UNDECIDED` with the map named as
the obstruction and as an observation for a later freeze — **not** as a negative verdict, this
freeze having frozen no negative sentence for it. `A27-H` records each shape `OBTAINED` or
`UNDECIDED`; `A27-T` records `OBTAINED` or `UNDECIDED`; neither enters any label. **Failure to
obtain a universal theorem is `UNDECIDED`, never a positive label.**

## The gate, FROZEN — the execution order and the three failure rules

**The gate is this round's freeze rule and not an `AGENTS.md` rule.** `A27-1` consumes `A27-0`'s
construction, and a corollary read without it would report a consequence of nothing.

### Safeguard 1 — the fixed order, one verdict commit per executed target, and the attestation set at every boundary

The targets are executed in the order **`A27-0` → `A27-1`**, and in no other; `A27-H` and `A27-T`
are recorded **inside `A27-0`'s verdict commit**, after `a27_0_strict_lift`.

The execution's commits on the first-parent chain from `B` are, in order: **the stage-A commit**
(the two declarations set to `B`, in the guard file only); **the module commit** (the module with
its shared lemmas and **no verdict of any target**: no named result of `A27-0`, `A27-1`, `A27-H`
or `A27-T`, and in particular no lemma stating that any class map has a lift, and no lemma stating
that any formula has none); then **exactly one verdict commit per executed target**, in target
order; then the packaging commit. A target the gate does not open has **no verdict commit**, and
its label is `NOT-EXECUTED` with the frozen sentence. A verdict commit that carries a later
target's result, or a target executed out of order, is recorded as a discrepancy and the round's
ordering obligation is reported as undischarged for that target.

**The attestation set is answered at every boundary.** Act 20's three questions, in act 21's
wording at its lines 1033–1043 — **Q1 INTENTIONAL**, **Q2 INCIDENTAL**, **Q3 UNAIDED REASONING** —
are answered by the result note **once per span**, for the spans `B` → module commit, module commit
→ `A27-0`'s verdict commit, `A27-0` → `A27-1`, each answer a measurement about what the execution
acquired **in that span** bearing on any target **not yet closed at the span's end**. **The
prior-knowledge rule act 25's packaging fixed governs**: knowledge acquired before a span is not
acquired in it; knowledge the executor holds before the chain's first commit is disclosed **once,
before the span attestations**, and is not re-counted at any span; and a disclosure does not cure a
YES. **What the freeze itself places in front of the execution is not a YES and is listed rather
than left implicit**: this file carries the complete construction for `A27-0` and `A27-1`, the
witness for `A27-T` with its dilation, weak element and coordinate, the shapes of the gauge classes
at one ancilla, and the consumption of `A27-0` by `A27-1` and `A27-H`; the questions are about what
was acquired beyond the freeze and the pinned blobs. **The construction was reasoned to by hand
before this freeze, and that reasoning is the freeze's content and not a span's acquisition.**

### Safeguard 2 — the route-authorization matrix

| construction or route | `A27-0` | `A27-1` | `A27-H` | `A27-T` |
| --- | --- | --- | --- | --- |
| classes as orbits; the section; the torsor lemma; the lift; the identity extension; strictness on every matrix | **authorized** | consumed | consumed | — |
| one section for both directions; the inverse composites; the rung conjuncts | — | **authorized** | — | — |
| the four shapes' realizability and descent from acts 24 and 25; instantiation | — | — | **authorized** | — |
| the two evaluations at `U = H(1)`, `c = (1, 1, 1, i)`, coordinate `(i, 2, 0)` | — | — | — | **authorized** |
| the shared lemmas of the module commit | consumed | consumed | consumed | consumed |

**Shared lemmas are not verdicts.** The module commit's lemmas — the `A = Fin 1` shapes of the two
gauge classes, the entry formula, the torsor lemma, the gauge-invariance of admissibility and of
its complement, the inverse of a gauge element, the closure of the realizable set under the phase
action — are consumed by whichever verdict needs them and answer no target by themselves. **A
lemma of the module commit that exhibits a lift for any class map, or refutes one for any formula,
is a verdict in the wrong commit and a YES at the first boundary.**

### Safeguard 3 — the outcome-vector table

The round's headline is a **vector of two labels**, one per target, each drawn from its target's
frozen label set, and **the headline is selected verbatim from the outcome-vector table in the
status rule below**. No single-label headline, no summary label and no combination label exists
for this round.

### Failure rule 1 — a universal theorem not obtained is `UNDECIDED`

For every target and sub-question, failure to obtain the theorem — whether or not one was attempted
— is reported as `UNDECIDED` with the obstruction named, never as a positive label. A class map
found to have no lift is `A27-0-UNDECIDED` with that map named, and an observation.

### Failure rule 2 — a route that fails its own reading is recorded, not repaired

If a named route unexpectedly fails a step the freeze's reading takes for granted — a gauge-class
shape the kernel reads differently, a torsor lemma that needs a hypothesis the reading omitted, a
strictness equation that fails off the admissible set — the execution **records that fact**, with
the step, and the target takes its `UNDECIDED` label with that obstruction named. **No definition
is introduced, the predicate is not paraphrased, no hypothesis is added to the class map, and no
external statement is imported.**

### Failure rule 3 — the gate closes forward and never reopens

`A27-1` is executed only if `A27-0` reaches `A27-0-LIFTS`; otherwise it takes
`A27-1-NOT-EXECUTED`, has no verdict commit, and its route is not attempted "for information".
`A27-H` and `A27-T` are recorded in `A27-0`'s verdict commit whatever `A27-0`'s label, `A27-H`
taking `UNDECIDED` for every shape if `A27-0` is `UNDECIDED`.

## The countercontrols and the positive controls

| target or sub-question | the route this freeze names | configuration |
| --- | --- | --- |
| `A27-0` | the section, the torsor lemma, the lift, the identity extension | single carrier |
| `A27-1` | one section, the inverse composites, the six rung conjuncts | single carrier |
| `A27-H` | instantiation at the four shapes | single carrier |
| `A27-T`, the contrast | `U = H(1)`, `c = (1, 1, 1, i)`, coordinate `((2 : Fin 4), 2, 0)` read as `(i, j, k) = (2, 2, 0)` or any coordinate at which the reading's equation `c 2 = c 3` appears; the execution records the coordinate used | single carrier |

**`A27-T` is the round's countercontrol on its own reading**: a positive `A27-0` together with a
positive `A27-T` certifies that the class-level and tuple-level readings of `L4n` come apart, which
is the distinction hazard two protects. **Evidence that earns any control or countercontrol**: a
Lean theorem at evidence level 2 whose statement names `StrictNatural`, `TwistedNatural`, the
conjuncts of `LadderConds`, `AdmissibleDilationAt`, `FibreGram`, `GramPhaseEquiv` and
`RealizableGram` as their modules declare them. **Searching and not finding earns nothing.**

## What was and was not run before this freeze

**No drafting-time check of any target's outcome was run.** By the owner's direction this control
plane was drafted from the sealed record alone: **no kernel proof, no elaboration of any statement,
no computation of any lift, no test of any class map and no exact-arithmetic evaluation** was
performed. **The construction above was reasoned to by hand from the merged declarations**, and it
is disclosed in full so that the attestation set can measure what the execution acquires beyond
it; the execution is what establishes or refutes each step. A step stated above that the kernel
refuses is a discrepancy of the freeze's reading, recorded in the result note and not repaired; it
changes no label by itself, the label being earned by what the kernel proves.

**One drafting-time reading of the library was made and is recorded in the provenance section**:
that the unitary-group and diagonal-matrix lemmas the construction needs are present. It is a
statement about what the library offers and not about any target's outcome.

**One simulation was run, and it concerns chronology only.** A throwaway detached worktree at `D`
with the two declarations set to `D` under the stem `NLV` — no Lean file, no guard clause, no
record — was run against the guard at `D`: every one of the ninety `R7-*` tags passed, the
twenty-five sealed rounds each classified `ARCHIVED`, reading their own records and nothing of the
declaration, and the run ended `ALL CHECKS PASS`. **No contract of any closed round needs
superseding for this round to execute**, and the supersession table below is therefore empty. The
simulation's worktree was removed and its state exists on no branch of the repository.

## The ordering obligation, as it binds a round with a definition budget of zero

> **The ordering obligation, act 27.** The rungs are act 21's declarations at blob `860daac4…`, the
> equivalence and the left gauge class are act 12's at blob `4bba2040…`, the right gauge class is
> act 11's at blob `8d171777…`, the notions of naturality are act 20's at blob `4c1137f3…`, the
> family is act 25's at blob `954fbdda…`, and nothing else. The execution's module **states no
> rung, no notion of naturality, no gauge class and no equivalence, and declares nothing**; every
> statement names `StrictNatural`, `TwistedNatural`, `AdmissibleDilationAt`, `FibreGram`,
> `GramPhaseEquiv` and `RealizableGram` as their modules declare them; the configuration is bound,
> in every theorem, to `Γ₀ = Matrix.of (fun _ _ => (1 / 4 : ℝ))`, `A = Fin 1`, `a₀ = (0 : Fin 1)`.
> From the module commit to the certified head `E`, **no commit of the branch adds a declaration,
> restates a rung or a notion, alters the configuration, adds a hypothesis to any class map, or
> uses an equivalence outside the frozen quotient list**; the module commit carries no verdict of
> any target; the verdict commits of the executed targets follow it in the order `A27-0`, `A27-1`;
> and no theorem of a later target's section is present before that target's verdict commit.

### What the execution must record

1. **The declaration table.** For `FibreGram`, `GramPhaseEquiv`, `RealizableGram`,
   `LeftFibreGroup`, `WeakAnchorStabilizer`, `AdmissibleDilationAt`, `StrictNatural`,
   `TwistedNatural`, `RelabelTransition`, each rung conjunct `A27-1` states, and act 25's four
   shapes, the merged declaration consumed with its line range at its blob; and for this round's
   module the statement that it carries **no declaration** — checked mechanically by `R7-NLV`,
   which requires no line of the module to begin with `def `, `abbrev `, `structure `, `class `,
   `instance `, `axiom ` or `opaque `, and the import `OIBridge.OrbitGeometryRigidity`.
2. **The stage-A commit.** The SHA of the commit that sets the two declarations to `B`, and nothing
   else.
3. **The module commit.** The SHA of the first commit at which the module is present, with the
   named results it carries, each of which is a shared lemma and none a verdict.
4. **The verdict commits**, in order, each with its SHA and its named results; and, for a target
   the gate did not open, the statement that it has none.
5. **The immutability span.** The statement, with the command that checks it, that between the
   module commit and `E` no diff touches a declaration:
   `git diff <module commit> <E> -- verification/lean-mathlib/OIBridge/StrictNaturalLift.lean`
   contains no added or removed line beginning with `def `, `abbrev `, `structure `, `class `,
   `instance `, `axiom ` or `opaque `.
6. **The quotient, predicate and configuration record.** The only equivalence used in any verdict
   is act 12's `GramPhaseEquiv`; the predicate is act 20's at every mention; the configuration is
   the single carrier at every mention; **no rung was restated, no notion was paraphrased, no
   hypothesis was added to any class map, no definition was introduced, and nothing was imported**.
7. **The prior-knowledge disclosure and the three attestation answers**, one per span, as safeguard
   1 fixes them.
8. **The gate record.** For `A27-1`, the label of `A27-0` as earned and whether the gate opened.

### The anti-expansion rule, FROZEN

**If execution discovers a class map without a lift, a further notion of naturality, a further
configuration, a strengthening of a merged theorem, a lifting theorem for a given tuple-level family
beyond the one contrast frozen, or anything bearing on act 24's cell `a4`, it is recorded as an
observation for a later round with its own freeze and is not executed here.** The predicate is
closed at act 20's three, the quotient list at one, the ladder at act 21's `L0`–`L5`, the
configuration at act 12's single carrier, the definitions at zero.

## The targets, FROZEN

### `A27-0` — the strictly natural lift of every class map

**The statement.** With `Γ₀ = Matrix.of (fun _ _ => (1 / 4 : ℝ))`, for every
`f : (Fin 4 → Matrix (Fin 4) (Fin 4) ℂ) → (Fin 4 → Matrix (Fin 4) (Fin 4) ℂ)` satisfying the two
class-map hypotheses displayed in the objects section, the five-conjunct conclusion displayed
there.

**What settles it.** One Lean theorem at evidence level 2, `a27_0_strict_lift`, universal in `f`,
whose conclusion names `StrictNatural (0 : Fin 1) Ψ` verbatim and carries the five conjuncts
**separately**. **`A27-0-LIFTS` is earned only by all five together**; a proof that adds a
hypothesis to `f` — surjectivity, injectivity, isometry, continuity, a formula — or that weakens
`StrictNatural` to `TwistedNatural` with non-identity maps, or that states the strictness
equations over admissible dilations only, has proved a different theorem and earns
`A27-0-UNDECIDED` with that change named.

### `A27-1` — the reversible corollary and the rung conjuncts

**The statement.** For every `f` satisfying the two class-map hypotheses and the two `L3`
conjuncts displayed in the objects section, the conclusion displayed there: the two lifts, the
two inverse equations on admissible dilations, and `LadderConds`' conjuncts three through eight
for `Φ := fun _ => Φ₀` at `Γ := fun _ => Γ₀`, `A = Fin 1`, `a₀ = (0 : Fin 1)`.

**What settles it.** One Lean theorem at evidence level 2, `a27_1_reversible_lift`, whose
conclusion carries every conjunct separately, the rung conjuncts in `LadderConds`' wording.
**`A27-1-REVERSIBLE` is earned only by all of them together**; the inverse equations without the
rung conjuncts, or the rung conjuncts with the inverse equations only up to gauge, earns
`A27-1-UNDECIDED` with the missing conjunct named. `A27-1-NOT-EXECUTED` when the gate is closed.

### `A27-H` — the four shapes (sub-question, entering no label)

**The statement.** For each of act 25's four shapes, read as a class map as the route fixes, the
conclusion of `A27-0` at that map. **What settles it.** Four Lean theorems at evidence level 2,
`a27_h_relabel_lift`, `a27_h_conj_lift`, `a27_h_transpose_lift`, `a27_h_transpose_conj_lift`, each
`OBTAINED` or `UNDECIDED` with the step named, recorded in `A27-0`'s verdict commit.

### `A27-T` — the tuple-level contrast (sub-question, entering no label)

**The statement.** With `Γ₀` as above and `σ = Equiv.swap (2 : Fin 4) 3`: there is **no** `Ψ` with
`StrictNatural (0 : Fin 1) Ψ` and `∀ U, AdmissibleDilationAt Γ₀ (0 : Fin 1) U →
FibreGram (0 : Fin 1) (Ψ U) = RelabelTransition σ (FibreGram (0 : Fin 1) U)`. **What settles it.**
One Lean theorem at evidence level 2, `a27_t_relabel_no_strict_lift`, `OBTAINED` or `UNDECIDED`
with the step named, recorded in `A27-0`'s verdict commit.

## The preregistered predictions, with their signs, strengths and recorded reasons

| target | prediction (sign) | strength | recorded reason |
| --- | --- | --- | --- |
| `A27-0` | **`A27-0-LIFTS`** | **high** | The construction is explicit, every step is a landed theorem or a one-line reading of a landed definition at one ancilla, and the only new lemma with content is the torsor lemma, whose input is the nonvanishing of admissible entries. The strength is high and not certain because `StrictNatural` quantifies over every matrix and the identity extension has to be checked against the gauge-invariance of non-admissibility, a step the reading takes for granted. **UNDECIDED with the step named is an allowed outcome.** |
| `A27-1` | **`A27-1-REVERSIBLE`** | **medium-high** | The inverse composites are exact because one section serves both directions; the six rung conjuncts are readings of `A27-0`'s conjuncts and act 21's declarations, the `L4d` conjunct on non-realizable tuples resting on the closure of the realizable set under the phase action. The rating is below `A27-0`'s because the statement is long and each conjunct is its own kernel obligation. |
| `A27-H` | **`OBTAINED`** for all four shapes | **high**, following `A27-0` | Each is an instantiation whose hypotheses are acts 24's and 25's theorems consumed. |
| `A27-T` | **`OBTAINED`** | **medium** | The two evaluations are two rewrites and one coordinate; the rating is medium because the reading's coordinate bookkeeping — which entry carries `c 2 = c 3` — is the freeze's hand reading and the kernel may place it elsewhere, in which case the execution records the coordinate it used. |

**No target has a numerical fallback**, and none is offered for a universal statement. **UNDECIDED
remains a permitted label for every target**, reported with the obstruction named specifically.

## The status rule: the outcomes per target, each with its FROZEN post-round sentence

The execution reports each target with exactly the sentence frozen here for the outcome reached.
**UNDECIDED is a live preregistered outcome for every target and is not a failure, and
NOT-EXECUTED is a live preregistered outcome for `A27-1` and is not a failure.**

### The outcomes of `A27-0`

- **Outcome `A27-0-LIFTS`:**
  > Every map of the realizable classes at the single-carrier configuration — every map on tuples
  > that preserves realizability and respects act 12's phase equivalence on realizable tuples —
  > admits a tuple-level representative, phase-equivalent to it on every realizable tuple, together
  > with a map on dilations that lifts the representative exactly on every admissible dilation,
  > carries admissible dilations to admissible dilations, and is strictly natural in act 20's exact
  > sense, with both induced gauge maps the identity, proved universally at evidence level 2 by an
  > explicit choice of representatives. **So representative-level naturality at act 20's certified
  > strength, read existentially over tuple-level representatives, imposes no restriction on class
  > maps at that configuration.** This is a statement about the exact predicate at the exact
  > configuration: it says nothing about any given tuple-level family's own lift, nothing at any
  > other configuration or ancilla cardinality, does not say naturality is or is not the right
  > condition, and adopts nothing.
- **Outcome `A27-0-UNDECIDED`:**
  > Whether every map of the realizable classes at the single-carrier configuration admits a
  > strictly natural lift is undecided in this round, with the obstruction named specifically — the
  > step of the construction at which the proof stopped, the hypothesis that had to be added, or the
  > class map found without a lift, and what would settle it. No lift is claimed for any map, the
  > absence of a proof is not a counterexample, and the reversible corollary was not executed.

### The outcomes of `A27-1`

- **Outcome `A27-1-REVERSIBLE`:**
  > For every bijection of the realizable classes at the single-carrier configuration, the
  > representative and the lift can be chosen, from one fixed choice of base dilations, so that the
  > inverse class map's representative and lift invert them exactly on admissible dilations, and the
  > constant transition family the representative induces satisfies the conjuncts `L0`, `L1`, `L2`,
  > `L3`, `L4d` and `L4n` of act 21's ladder, in act 21's wording, at evidence level 2. **So none of
  > those rungs, read of class maps, distinguishes act 25's family from any other bijection of the
  > classes at that configuration.** This is a statement about the exact declarations at the exact
  > configuration: it does not assert act 18's two standing hypotheses of any family, does not
  > assert or deny `L5`, touches act 24's cell `a4` in neither direction, and adopts nothing.
- **Outcome `A27-1-UNDECIDED`:**
  > Whether every bijection of the realizable classes at the single-carrier configuration admits
  > inverse lifts from one choice of base dilations with the induced family satisfying the named
  > rung conjuncts is undecided in this round, with the conjunct and the step named; the universal
  > lift having closed, the step is in the inverse composites or in the reading of act 21's rungs.
  > Neither the corollary nor its negation is claimed.
- **Outcome `A27-1-NOT-EXECUTED`:**
  > The reversible corollary was not executed, the universal lift not having reached its label; no
  > sentence of this round reports anything about it.

### The sub-questions

- **`A27-H`, `OBTAINED` (per shape, the shapes obtained named):**
  > The class map of each named shape of act 25's family admits the strictly natural lift the
  > universal theorem supplies, at evidence level 2; this is a statement about the class map and not
  > about act 25's formula as a map on tuples, act 20's lift of the relabelling stands as act 20
  > certified it, and it enters no label.
- **`A27-H`, `UNDECIDED` (per shape, the shapes undecided named):**
  > Whether the class map of the named shape admits the lift is undecided in this round, with the
  > step named; it enters no label.
- **`A27-T`, `OBTAINED`:**
  > No strictly natural map on dilations lifts act 20's relabelling transition at the named
  > permutation itself, exactly on every admissible dilation, at evidence level 2, refuted at one
  > named dilation, one named weak element and one named coordinate. **So the tuple-level reading
  > of representative-level naturality is not vacuous**: a given tuple-level formula can have no
  > strictly natural lift while its class map has one. This revises nothing of act 20, whose
  > verdict is about the lift act 20 built, and it enters no label.
- **`A27-T`, `UNDECIDED`:**
  > Whether the named relabelling formula has a strictly natural lift is undecided in this round,
  > with the step named; it enters no label.

### The outcome-vector table — every admissible headline under the gate

The headline is one row of this table, verbatim, in this rendering: **Outcome vector:** followed by
the two labels in target order, separated by ` · `. Each row is admissible; the execution selects
the row its verdicts and the gate compose and reports no other wording. **There are three rows and
no others**; every other combination of labels is excluded by failure rule 3.

| # | vector |
| --- | --- |
| 1 | **Outcome vector:** `A27-0-LIFTS` · `A27-1-REVERSIBLE` |
| 2 | **Outcome vector:** `A27-0-LIFTS` · `A27-1-UNDECIDED` |
| 3 | **Outcome vector:** `A27-0-UNDECIDED` · `A27-1-NOT-EXECUTED` |

**The predicted row is row 1.**

## The frozen post-round sentence for the `P0` row, per case

The `P0` row stays **OPEN** in every case and its label does not change. The execution appends,
after act 26's sentence in the same cell, exactly the sentence frozen for the vector reached.

**Case A — row 1 with both sub-questions obtained.** This is the case the freeze predicts.

> Act 27 tests, in one round with one universal target and its gated reversible corollary, whether every map of the realizable classes at the single-carrier configuration admits a tuple-level representative carrying a representative-level lift that is strictly natural in act 20's exact sense with both induced gauge maps the identity, and whether, for bijections of the classes, the construction supplies inverse lifts and the induced constant transition family satisfies the conjuncts of act 21's ladder before factorization other than the standing propagation hypotheses. Every such class map admits such a lift, proved universally at evidence level 2 by an explicit choice of representatives, so representative-level naturality at act 20's strength, read existentially over tuple-level representatives, imposes no restriction on class maps at that configuration; every bijection's induced family satisfies those rung conjuncts with inverse lifts, so none of those rungs, read of class maps, distinguishes act 25's family from any other bijection of the classes there; each of act 25's four shapes, as a class map, admits the lift; and one given tuple-level formula, act 20's relabelling transition at one permutation, admits no strictly natural lift, so the tuple-level reading of the rung is not vacuous and a given family's own lift remains a separate question, act 24's cell `a4` at the product configuration being untouched in either direction. Each verdict is of the exact frozen proposition at the exact configuration and of nothing in its neighbourhood; no verdict is inferred from another beyond the consumptions the freeze places; no condition is adopted; no lift is canonical, unique or continuous, and none is read as a symmetry, an antiunitary map, a time reversal, a unitary evolution or a dynamics; nothing is asserted at the product configuration, at any other ancilla cardinality, or about factorization; acts 12 through 26's own verdicts stand exactly as they state them; nothing here introduces continuity, composition in time or a generator; and deriving or recognising quantum evolution is out of scope by this round's own freeze. `P0`'s threading part is untouched, **no carrier is adopted as the physical one**, **no surviving law is adopted as the physical one**, and nothing here names, endorses or excludes a selection principle; factorization at the product configuration is named as the next condition to inspect, with its selecting power left open.

**Clauses of Case A vary with the outcome, and they vary independently within the gate**, each
replaced by the sentence its target's other outcomes fix:

| target or sub-question | clause | replacement |
| --- | --- | --- |
| `A27-0`, `UNDECIDED` | the sentence beginning "Every such class map admits such a lift" through "left open." | "Whether every such class map admits such a lift is recorded undecided, with the step named; no lift is claimed for any map, the reversible corollary was not executed, and nothing is asserted about what any rung does or does not distinguish. Each verdict is of the exact frozen proposition at the exact configuration and of nothing in its neighbourhood; no condition is adopted; nothing is asserted at the product configuration or at any other ancilla cardinality; acts 12 through 26's own verdicts stand exactly as they state them; and deriving or recognising quantum evolution is out of scope by this round's own freeze. `P0`'s threading part is untouched, **no carrier is adopted as the physical one**, **no surviving law is adopted as the physical one**, and nothing here names, endorses or excludes a selection principle." |
| `A27-1`, `UNDECIDED` | the clause "every bijection's induced family satisfies those rung conjuncts with inverse lifts, so none of those rungs, read of class maps, distinguishes act 25's family from any other bijection of the classes there" | "whether every bijection's induced family satisfies those rung conjuncts with inverse lifts is recorded undecided, with the conjunct named, and nothing is asserted about what those rungs distinguish" |
| `A27-H`, any shape `UNDECIDED` | the clause "each of act 25's four shapes, as a class map, admits the lift" | "the shapes of act 25's family obtained as class maps are those named, the others recorded undecided" |
| `A27-T`, `UNDECIDED` | the clause "one given tuple-level formula, act 20's relabelling transition at one permutation, admits no strictly natural lift, so the tuple-level reading of the rung is not vacuous and a given family's own lift remains a separate question" | "whether a given tuple-level formula can lack a strictly natural lift is recorded undecided, with the step named, and a given family's own lift remains a separate question" |

**The execution composes the sentence from these substitutions and reports no other wording. No
composition closes `P0`**, and none reports either of its two parts closed.

## Naming a lift or a map is not endorsing it, FROZEN

Act 21's four points at its lines 1868–1879 govern here, read of every class map, every
representative, every lift and every shape: naming is for testing; the round endorses none;
exclusion is only ever of the precise stated form; the lists are closed at this freeze.

### The non-adoption clause, FROZEN VERBATIM

**This is THE CLAUSE, and it is carried as a block quote at every place in this file and in every
artifact of this round where a lift's existence, a rung's vacuity or a family's standing could be
read as its adoption**, each carriage opening with one line naming where it is being carried. It is
act 21's clause with "Act 21" read as "Act 27", and nothing else changed.

> **THE CLAUSE, carried at this mention — the section that states it.**
> Act 27 classifies the cross-time laws a frozen ladder of conditions leaves standing, and adopts
> none. A law that survives every condition this freeze names is a law that survives **those**
> conditions, at the configuration frozen for it, and it is **not** a finding that it obtains in
> nature, **not** a finding that the programme requires it, and **not** an adoption of it as the
> physical law of evolution. **Surviving is not standing.** A rigidity verdict is a statement about
> the frozen ladder and about the frozen quotient list, and a family or wide verdict is not a licence
> to add one more condition, or to widen one more equivalence, until a plurality becomes a point.
> **No law gains physical status by surviving, no carrier and no principle is adopted as the physical
> one, and nothing here derives, recognises or approaches quantum evolution.**

**Read of the vacuity**: that a rung excludes no class map is a statement about the rung's exact
declaration at one configuration; it is never written as "naturality is empty", "the ladder is
empty", "the family is unconstrained" or "any evolution is allowed", and "every class map lifts"
is never written as "every class map is physical".

## What no outcome licenses

These are the forbidden sentences, in terms. None may be written in any artifact of this round, in
any paraphrase, in a summary line, an abstract, a table cell or a propagation line.

1. **"The lift is the unitary evolution", "the lift is a symmetry", "the induced maps being the
   identity means the dynamics commutes with the gauge", "the strict lift is the physical
   representative", or any statement that any lift, any representative, any class map or any shape
   is, resembles, approximates or points toward a quantum symmetry, an antiunitary map, a time
   reversal, unitarity, a dynamics or quantum evolution.** Each is named by its construction and as
   nothing else.
2. **"The family is selected", "the family is not selected", "the isometries are all physical",
   "naturality is the wrong condition", "the ladder selects nothing".** The non-adoption clause
   governs:
   > **THE CLAUSE, carried at this mention — the list of what no outcome licenses.**
   > Act 27 classifies the cross-time laws a frozen ladder of conditions leaves standing, and adopts
   > none. A law that survives every condition this freeze names is a law that survives **those**
   > conditions, at the configuration frozen for it, and it is **not** a finding that it obtains in
   > nature, **not** a finding that the programme requires it, and **not** an adoption of it as the
   > physical law of evolution. **Surviving is not standing.** A rigidity verdict is a statement about
   > the frozen ladder and about the frozen quotient list, and a family or wide verdict is not a licence
   > to add one more condition, or to widen one more equivalence, until a plurality becomes a point.
   > **No law gains physical status by surviving, no carrier and no principle is adopted as the physical
   > one, and nothing here derives, recognises or approaches quantum evolution.**
3. **"Every transition family satisfies `L4n`", "`L4n` is vacuous" without the qualification
   "read existentially over tuple-level representatives, at the single-carrier configuration",
   or any statement that a given tuple-level family has a lift from the universal theorem.** The
   universal theorem supplies a representative; `A27-T` shows a given formula can have none.
4. **"Act 24's cell `a4` is closed", "is now positive", "is now negative", "is narrowed", or any
   statement bearing on `a4` in either direction.** `a4` is at the product configuration and about
   a given family, and this round touches it by nothing.
5. **"The lift is canonical", "the natural lift", "the unique lift", "the continuous lift", "the
   lift is linear", or any regularity, uniqueness or canonicity claimed of any lift.** The theorem
   asserts existence through a disclosed choice.
6. **"Act 20's `STRICT-NO` is reversed", "act 20 was wrong", "the relabelling is strictly natural
   after all".** Act 20's verdict is about `RelabelLift`; `A27-H` supplies a different lift of the
   class map, and `A27-T` shows the formula itself has none; act 20's note stands as earned.
7. **"`L4n` is vacuous at every configuration", "at every `|A|`", "at the product
   configuration".** The reading that the gauge classes are phases is a reading at `A = Fin 1`.
8. **"The pre-`L5` rungs cannot distinguish `H` from `G`" written of act 26's enumerated group as
   a certified object.** The corollary quantifies over every bijection of the classes, and what
   it says about act 26's observation is that the observation's isometries are among them; the
   enumeration certifies nothing here and is not consumed.
9. **"`L5` selects", "`L5` does not select", or any statement about factorization beyond naming
   it as the next condition to inspect.**
10. **"Act 21's `L4n-RESTRICTS` is revised", "act 22's verdicts are revised", "act 24's twelve
    verdicts are revised", or any rewriting or reinterpretation of any earlier act's verdict,
    witness or obstruction.** They stand; this round adds a universal statement at its own
    configuration and re-earns, re-grades and reinterprets nothing.
11. **Any statement about the metric, the feature vector, the isometries or the affine hull as
    consumed by this round.** None enters any statement; act 26's observation is an observation.
12. **Any statement about the threading, the cross-time representative, the relative evolution or
    the relative candidate; about act 16's cancellation cell; about act 14's carriers; about act
    18's `D`-axis; about act 10's anchor axis; about Track I, Source B or Source C; or about the
    substratum Lemma 24.1 rounds.**
13. **"`P0` is closed", or "`P0`'s trajectory part is closed."**
14. **"The evolution is continuous", "smooth", "generated", "one-parameter", "composes in time"**,
    or any statement resting on structure the index type does not carry; in particular nothing
    about the constant family's iterates being a flow.
15. **"OI and QM are inequivalent."**
16. **A single-label headline.** The headline is the vector, selected verbatim from the table.
17. **"The class maps are the isometries", or any identification of the class maps this round
    quantifies over with the isometries of any geometry.** The class maps are every map with the two
    displayed hypotheses; the isometries are a subset the round does not name.
18. **A definition, described as "a convenience" or "harmless".** The budget is zero, and any
    declaration is a defect of the round.
19. **"The isometry group is the Wigner group", or any Wigner, antiunitary or time-reversal
    reading of any object of this round.** The word appears in this file only here and in the
    out-of-scope statement, and in no other artifact of the round.

## Named hazards

1. **Hazard one, restated as the round's own.** The predicate replaced: every statement names
   `StrictNatural` or `LadderConds`' conjuncts verbatim, quantified as their modules quantify them;
   forbidden sentences 3 and 7 exist for it.
   > **THE CLAUSE, carried at this mention — the hazard list.**
   > Act 27 classifies the cross-time laws a frozen ladder of conditions leaves standing, and adopts
   > none. A law that survives every condition this freeze names is a law that survives **those**
   > conditions, at the configuration frozen for it, and it is **not** a finding that it obtains in
   > nature, **not** a finding that the programme requires it, and **not** an adoption of it as the
   > physical law of evolution. **Surviving is not standing.** A rigidity verdict is a statement about
   > the frozen ladder and about the frozen quotient list, and a family or wide verdict is not a licence
   > to add one more condition, or to widen one more equivalence, until a plurality becomes a point.
   > **No law gains physical status by surviving, no carrier and no principle is adopted as the physical
   > one, and nothing here derives, recognises or approaches quantum evolution.**
2. **Hazard two, restated.** The class map confused with a tuple map: `Φ₀` is existentially
   quantified, `A27-T` certifies the contrast, `a4` is untouched; forbidden sentences 3, 4 and 6.
3. **Hazard three, restated.** The choice disguised: forbidden sentence 5; the section is recorded
   as a `Classical.choice` in the result note.
4. **Strictness proved on admissible dilations only.** `StrictNatural` quantifies over every
   matrix; a proof that establishes the two equations on admissible `U` only has proved a
   different theorem and earns `UNDECIDED` with that named. The gauge-invariance of
   non-admissibility is the lemma that closes the gap, and its absence is the named obstruction.
5. **Two sections in the reversible corollary.** Lifts built from different sections compose to a
   gauge move, not the identity; the route fixes one section, and a proof of the inverse equations
   only up to `GramPhaseEquiv` earns `A27-1-UNDECIDED`.
6. **A hypothesis added to the class map.** Surjectivity, injectivity or isometry added to `A27-0`'s
   hypotheses changes the theorem; `A27-1`'s two `L3` conjuncts are the only additions and are
   `A27-1`'s alone.
7. **The rung conjuncts paraphrased.** `A27-1` states `LadderConds`' conjuncts in `LadderConds`'
   wording; a conjunct restated in other words is a restated rung and a defect.
8. **The standing hypotheses asserted.** `ProperAt` and `PropagatesFrom` are not stated, not
   asserted and not denied of any family here; a note that writes "the family is an `L-PROP` law"
   has overstated the corollary.
9. **A verdict revealed before its commit.** The attestation set at every boundary and the
   partial-fact rule exist for it; a shared lemma exhibiting a lift or refuting one is a verdict in
   the wrong commit.
10. **A verdict commit carrying two targets.** One per executed target, in order; `A27-H` and
    `A27-T` inside `A27-0`'s.
11. **Executing a closed target "for information".** Failure rule 3.
12. **Reading any lift as a symmetry or a dynamics.** Forbidden sentences 1 and 14.
13. **The contrast's coordinate chosen after the fact.** `A27-T`'s dilation, weak element and
    coordinate are named here; the execution records the coordinate the kernel used if it differs
    from the reading.
14. **Consuming a sibling round's result because it is present at the mandated base.** The
    anti-contamination invariant governs.
15. **A landing without `P`**, **a stem both declared and recorded**, **a legacy constant
    written**, **editing this freeze after an outcome is known**, **a supersession outside the
    table** — each as act 21's hazards 28, 30, 31 and 34 state them, with `NLV` for `OLT`; the
    table here is empty, so any edit to a closed round's guard is a supersession outside it.
16. **A `B`-scoped precondition that the freeze's own text falsifies.** Every `B`-scoped row below
    is evaluated at `M` before the merge and is written so that the presence of this file in the
    tree does not fail it.
17. **A step of the freeze's reading that the kernel refuses.** Recorded as a discrepancy of the
    reading; the label is earned by what the kernel proves — in particular the shapes of the two
    gauge classes at one ancilla and the torsor lemma's exact hypotheses.

## Non-doings

The round does not: **derive, recognise, approach or claim progress toward quantum evolution**, in
any paraphrase; adopt any lift, any representative, any class map, the family or any shape as a
symmetry, a selector, a principle or a piece of physics, or identify any of them with any named
object of any other theory; read any lift as a unitary, an antiunitary, a time reversal or a
dynamics; claim canonicity, uniqueness, continuity or linearity of any lift; assert anything about
a given tuple-level family beyond the one contrast frozen; assert anything about act 24's cell
`a4`, in either direction; assert anything at the product configuration, at any `|A| > 1`, or at
any visible family other than the frozen one; assert or deny `L5` of anything; assert or deny act
18's standing hypotheses of any family; consume any metric, feature vector or isometry statement;
consume act 26's enumeration as anything but an observation; **restate, edit, add or remove any
rung, or change any conjunction**; paraphrase any notion of naturality; **introduce or widen an
equivalence**, or use one outside the frozen quotient list in any verdict; **define anything**;
add or remove a generator; test any configuration outside the single carrier; infer any target's
verdict from another's beyond the consumptions the freeze places; **re-prove or strengthen acts
7, 11, 12, 17, 18, 20, 21, 22, 23, 24, 25 or 26**; revise any merged label; rewrite, reinterpret or
grade any verdict, witness or obstruction of any earlier act's note; ask the threading question or
the cross-time representative question, in either direction; touch act 16's cancellation cell;
state anything about act 18's `D`-axis, act 10's anchor axis, act 14's carriers, Track I, Source B
or Source C, or the substratum Lemma 24.1 rounds; **introduce continuity, smoothness, composition
in time, a semigroup law or a generated evolution**; alter any existing manifest record, supersede
any closed round's contract, or write a legacy seal constant; edit any manuscript; close `P0` or
either of its parts.

**What is named as next, and left open.** Factorization, `L5` at act 21's frozen product
configuration, is the one condition of the ladder this round does not read, because it is stated
at a configuration this round does not touch. It is named here as the next condition to inspect
for its selecting power, and **nothing about that power is asserted or predicted**; a round on it
needs its own freeze, with the product configuration's classes described or the question posed
without that description.

### Deriving or recognising quantum evolution is EXPLICITLY OUT OF SCOPE

Act 21's statement at its lines 2130–2143 is carried in full: the question is not asked, not
bounded and not attempted; an execution that begins asking whether a lift looks like a unitary
evolution, whether the identity induced maps look like a commutation with a symmetry, or whether
the vacuity of a rung says anything about physics, has left the round's scope, and what it finds is
recorded as an observation and not executed. **"Resembles", "is consistent with", "is what one
would expect from" and "is a step toward" are forbidden sentences.** The word "Wigner" appears in
this file only in the forbidden list and in this statement, and appears in no other artifact of
the round.

### What act 27 does and does not change about `P0`, about act 24's `a4`, and about act 20's verdicts

**`P0` stays OPEN and two-part in every case, and its label does not change.** What act 27 can
change is the recorded status of one rung of act 21's ladder, read of class maps at the
single-carrier configuration, and the recorded status of the tuple-level contrast.

**On act 24's `a4`, stated once.** It is not decided, narrowed or reversed, for the reason the
locating control gives: its configuration and its object are not this round's. **On act 20's
`CLASS-TWISTED-NOT-STRICT` and `STRICT-NO`, stated once.** Both are about `RelabelLift`, the lift
act 20 built, and stand as earned; `A27-H`'s relabelling corollary supplies a different lift of the
class map, and `A27-T` supplies the statement that the formula itself has no strictly natural
lift, which agrees with act 20's verdict and revises nothing of it. **On act 21's
`L4n-RESTRICTS`, stated once.** It was earned by `ΦCTRL` at the product configuration against the
tuple-level reading of the rung, and stands; nothing here is at that configuration or about that
reading.

**The direct-branch statement is frozen exactly, and no more:** `D4a` positive on the direct branch;
`T1` **necessary, not sufficient**; `n = 3` properness at **evidence level 3**; **no claim about
what fraction of OI lies in the direct sector.** Act 7 layer 2's `D5` control stands **NOT
CERTIFIED**.

## Definition budget

**The execution introduces NO top-level Lean definition.** The budget is **zero**, stated as a
number so that it cannot drift: no `def`, `abbrev`, `structure`, `class`, `instance`, `axiom` or
`opaque`. Act 7's, act 11's, act 12's, act 20's, act 21's and act 25's declarations are consumed and
none is restated; every representative, section and lift is an existentially bound object or a
`Classical.choice` inside a proof. **Any definition requires its own append-only amendment**,
separately frozen and merged before the work it affects. Theorems are not budgeted.

## Evidence level

**Evidence level 2** — kernel-checked, every named result printing only
`[propext, Classical.choice, Quot.sound]`, with **no unproved declaration, no added axiom and no
kernel-bypassing decision procedure** — for `A27-0`, `A27-1`, each shape of `A27-H` and `A27-T`,
whichever label each reaches other than `UNDECIDED` or `NOT-EXECUTED`. `decide` over finite index
types is permitted; `native_decide` is not, and neither is `sorry`. `Classical.choice` is expected
wherever the section, `sh1_sufficiency`, the inverse class map or the classical branch of a
statement is used.

## The chronology control — through the manifest and never a constant

The execution's guard tag is **`R7-NLV`**, reserved here and created by the execution pull request.
The round's stem is **`NLV`**; its seal state is the prospective declaration during execution and
the record `verification/seals/NLV.json` from `P`, and **no constant**.

1. **This preregistration blob is merged into `main`, and its merge commit `B` certified by a fully
   green main-push run including the control-plane base check in mode `B`, before any
   execution-specific act 27 object enters the repository tree** — any Lean statement about a lift
   of any class map, any shared lemma of this round, any probe clause, any result artifact, any
   manifest record or declaration for `NLV`. **The single permitted exception is the analysis
   recorded inside this control-plane blob itself**, merged *as* the freeze, including the
   construction and the chronology simulation.
2. **The execution pull request's base must be exactly `B`.** The execution's first commit sets
   `_MANIFEST_PROSPECTIVE = {'NLV': B}` and `_MANIFEST_BASELINE = {'base': B, 'authorized':
   ('NLV',)}`, both outside the validator's marker-bounded regions, and nothing else.
3. **The execution guard pins this file's blob by content at this exact path, with a one-byte drift
   control**, fail-closed.
4. **The ancestry question is asked of the real execution head through the validator's prospective
   path** — one keyed call, `_si2_authority('NLV', tag='R7-NLV')` — with `pull_request.head.sha`
   from the Actions event payload as the target in pull-request continuous integration, `HEAD`
   otherwise, **never** the synthetic merge commit; an unresolvable head fails closed.
5. **The check excludes pre-freeze side history**: `B` ancestor-of `H`, and every commit in
   `git rev-list H ^B` a descendant of `B`, fail-closed — the validator's `EXECUTION` classification.
6. **The guard recovers whatever history it needs itself** and **fails** if recovery fails.
7. **Archive mode is the record.** At `L` the validator classifies `NLV` as `LANDED-PENDING-PIN`;
   `P` writes `verification/seals/NLV.json` with `base` = `B`, `sealed_head` = `E`, `merge` = `L`,
   removes the `NLV` entry from the prospective declaration, and touches nothing else; from `P` on
   the validator classifies `NLV` as `ARCHIVED`, each conjunct fail-closed.
8. **Existing manifest records are read with the integrity rule and never written.** The declared
   baseline holds the records at `B` against mutation, removal and any addition other than `NLV`.
9. **No `_NLV_BASE`, `_NLV_SEALED_HEAD` or `_NLV_MERGE` exists at any commit of the round**, and
   `SI-3`'s standing contract holds at every head.
10. **The ordering obligation's records are checked mechanically by `R7-NLV`**, each with a
    mutation control: (a) the module at every commit from the module commit to `E` contains no
    line beginning with `def `, `abbrev `, `structure `, `class `, `instance `, `axiom ` or
    `opaque `; (b) the module imports `OIBridge.OrbitGeometryRigidity`; (c) every statement that
    names the configuration binds `Γ₀` to `Matrix.of (fun _ _ => (1 / 4 : ℝ))`, the ancilla to
    `Fin 1` and the anchor to `(0 : Fin 1)`, and every naturality claim in a verdict statement is
    `StrictNatural (0 : Fin 1)` or `TwistedNatural (0 : Fin 1)` applied verbatim, with no other
    predicate of the same shape declared or stated; (d) the stage-A commit, the module commit and
    the verdict commits of the executed targets are on the first-parent chain from `B` to `E`, in
    that order, the module absent before the module commit and present from it on, and the module
    commit carrying no theorem whose name is a verdict name and no theorem whose statement carries
    `StrictNatural` in a conclusion; (e) each verdict theorem first appears at its own verdict
    commit and at no earlier commit; (f) the gate: a verdict commit for `A27-1` exists only if the
    result note carries `A27-0-LIFTS`, and `a27_1_reversible_lift` exists only then. A declaration,
    a synthetic edit to the configuration's equation or to a predicate's name, a verdict theorem
    inserted into the module commit's text, a fabricated SHA off the chain, and an `A27-1` theorem
    present without `A27-0-LIFTS` each **fail** the control.
11. **Content contracts hold the result note to this freeze's distinctions**, each mutation-tested:
    the round's shape as sealing with `E` → `L` → `P`; the declaration table with **no definition**
    and the eight records; the prior-knowledge disclosure **once, before the first span**, and the
    **three** attestation answers, one per span, each reporting only what that span acquired; the
    sentence that no rung was restated, no notion was paraphrased, no hypothesis was added, no
    definition was introduced and nothing was imported; each target's label carried with its frozen
    sentence verbatim; `A27-H` per shape and `A27-T` each with its label and frozen sentence; **the
    outcome vector, equal verbatim to one of the three rows of the table**; **the gate record**;
    **the statement that no verdict was inferred from another beyond the consumptions the freeze
    places**; **the statement that no lift is read as a symmetry, an antiunitary map, a time
    reversal, a unitary evolution or a dynamics, and none is called canonical, unique or
    continuous**; **the statement that act 24's cell `a4` is untouched in either direction and that
    act 20's verdicts stand**; **the statement that every earlier act's historical verdicts stand
    unchanged**; the route-authorization matrix reported as honoured; THE CLAUSE carried complete at
    every mention with its count; and the frozen `P0` sentence for the case reached present in
    `verification/ROADMAP.md` verbatim, after act 26's.

### The contracts this round supersedes, named in advance — none

**Under `§A.37`'s closed-round rule, `AGENTS.md` lines 1055–1061, a closed round's contract that
reads the current round's declaration would be superseded here. The measurement recorded above
found none**: at `D` every closed round's guard reads its own record, and the placeholder
declaration failed nothing. **The execution edits no contract of any closed round.** Should the
execution find that a closed round's contract fails on an act 27 head for a reason that has nothing
to do with act 27's result, that is a discrepancy recorded in the result note, and the disposition
is an append-only amendment to this freeze, separately merged, and not an edit made during
execution.

### What must have merged before the execution begins, checkable mechanically

Each row below names its scope — at `D`, at `B`, or from `D` to `B` — and the block after the table
is the machine-checkable form of the same rows, which the release gate lints and the workflow's
`control-plane-base-check` job evaluates in mode `M` against the candidate merge of this pull
request and, after the merge, in mode `B` against the actual merge commit.

| # | scope | precondition | mechanical check |
| --- | --- | --- | --- |
| 1 | `D` | The names were free when chosen | `git grep -l -- 'R7-NLV' D`, `git grep -l -- '_NLV' D`, `git grep -l -- 'NLV' D`, `git grep -l -- 'StrictNaturalLift' D`, `git grep -l -- 'strict-natural-lift' D`, `git grep -l -- 'act-27' D`, `git grep -l -- 'A27-' D` and `git grep -l -- 'a27_' D` each return nothing |
| 2 | `D` | The seals tree at `D` is the pinned one | `git rev-parse D:verification/seals` is `73927a618072453213fd27189aceff76b6b499e5`, thirty-one records, twenty-five `sealed` and six `base-only` |
| 3 | `D` | The guard at `D` is green and carries no legacy constant | ninety `R7-*` tags, all `PASS`, on main-push run 35507975235; `_SI2_LEGACY_RE` finds zero assignment statements |
| 4 | `D → B` | `D` is an ancestor of `B` | `git merge-base --is-ancestor D B` succeeds |
| 5 | `D → B` | The blobs this round consumes are unchanged | each of the first thirty-six paths of the start-state table has at `B` the blob the table names (the `frozen-blob` lines of the block) |
| 6 | `B` | No act 27 execution object exists | the guard file at `B` contains no `R7-NLV` and no `_NLV`; no `verification/seals/NLV.json`; no `verification/lean-mathlib/OIBridge/StrictNaturalLift.lean`; the round directory holds nothing but `preregistration.md` and, if any, `amendments/amendment-*.md` |
| 7 | `B` | No round is executing at `B` | the guard file at `B` carries `_MANIFEST_PROSPECTIVE = {}` |
| 8 | `B` | Act 26 is sealed at `B` | `verification/seals/CGR.json` at `B` carries `round` `CGR`, `kind` `sealed`, `base` `e0be0ab6dca7b6661a008a9f5e1f3a29ea736003`, `sealed_head` `2b30604ccb5b3a8187357a6f720ee104bfe1826e`, `merge` `7564ab67809d56d5047e27a4b214f99c2effc9c8`; the guard file at `B` carries the `R7-CGR` check |
| 9 | `B` | Act 26's module is wired | `OIBridge.lean` at `B` imports `OIBridge.OrbitGeometryRigidity` |
| 10 | `B` | This control plane is in the tree at its path | `verification/programmes/oi-qm/track-b/act-27-strict-natural-lift/preregistration.md` exists at `B`; its blob is the one the `R7-NLV` clause pins, which the execution's first act verifies by `git hash-object` and the block cannot state of itself |

**No sibling lane's merge is a precondition of this round**, and the execution does not wait for one.
Sibling results present at `B` are not inputs. **The claim is scoped to the repository record.**

```control-plane-preconditions
d: 22de675ed6a6dd3b98313e6908107404e988352d
merged: false
frozen-blob: verification/lean-mathlib/OIBridge/TwoSidedGauge.lean 4bba2040c33424fafbc6d31c0d63b86dff33691a
frozen-blob: verification/lean-mathlib/OIBridge/CoherentLiftGauge.lean 8d17177799327d648bbbd001cf237e1ac37bd3fc
frozen-blob: verification/lean-mathlib/OIBridge/DilationChoice.lean 7e3a8222cedf530f3c109662e7174d72b6358063
frozen-blob: verification/lean-mathlib/OIBridge/GramTrajectorySelection.lean afc22cfc93b244c80e1c55a273dcfda1ddebb121
frozen-blob: verification/lean-mathlib/OIBridge/IntermediateCrossTimeStructure.lean cb14c43b0becfe1a379ae3615d5553723ede9163
frozen-blob: verification/lean-mathlib/OIBridge/RepresentativeNaturality.lean 4c1137f35600320b9273c857ec62271341b05cd0
frozen-blob: verification/lean-mathlib/OIBridge/OrbitLawRigidityTwisted.lean 860daac4eb20dbe92c35c2b3ca7aaa1ed798e7b8
frozen-blob: verification/lean-mathlib/OIBridge/OrbitLawNaturalityFactorization.lean d41b157a3f38d4ebedbe11ad9682a8693836a383
frozen-blob: verification/lean-mathlib/OIBridge/OrbitLawGaps.lean 5ed0dad78d87314dfd9e1a8ec241f479ded1e3e1
frozen-blob: verification/lean-mathlib/OIBridge/OrbitGeometrySelector.lean ce9d1aa05dfdedfb5cac171cfe6379681942195f
frozen-blob: verification/lean-mathlib/OIBridge/OrbitGeometryIsometries.lean 954fbddaa7511713a26c316b3b2e0f29497e81d2
frozen-blob: verification/lean-mathlib/OIBridge/OrbitGeometryRigidity.lean 3e15384196203939d348f2a313a873818ec4b684
frozen-blob: verification/programmes/oi-qm/track-b/act-26-orbit-geometry-rigidity/preregistration.md 521b63ccde0056453f42035ba9d7970be66cf6f9
frozen-blob: verification/programmes/oi-qm/track-b/act-26-orbit-geometry-rigidity/result.md 76ebf0ffa512ce2e53868e529d42fbf9895a39d8
frozen-blob: verification/programmes/oi-qm/track-b/act-25-orbit-geometry-isometries/preregistration.md f4892f64e1667244fc262a01fb46b27f9b1ba1ba
frozen-blob: verification/programmes/oi-qm/track-b/act-25-orbit-geometry-isometries/result.md e4441e9a6f121f96089dc2c9e0ca2e2b9a7a33d9
frozen-blob: verification/programmes/oi-qm/track-b/act-24-orbit-geometry-selector/preregistration.md 3b61d6c90fe3f05bbf5e791f74d7106e9be2f94a
frozen-blob: verification/programmes/oi-qm/track-b/act-24-orbit-geometry-selector/result.md a2719d5f63c4ce517590ec7fbe8e61bafe04c3a7
frozen-blob: verification/programmes/oi-qm/track-b/act-21-orbit-law-rigidity-twisted/preregistration.md 316d635a31f91faebeeebef7688b30002d24b4ca
frozen-blob: verification/programmes/oi-qm/track-b/act-21-orbit-law-rigidity-twisted/amendments/amendment-1.md d140978e6f0031063e7aa4b9bbe3e960d9b7f8e1
frozen-blob: verification/programmes/oi-qm/track-b/act-21-orbit-law-rigidity-twisted/result.md bb02ef41eb221696ffa45c9281b69553c8279cbb
frozen-blob: verification/programmes/oi-qm/track-b/act-20-representative-naturality/preregistration.md 131783f48ac492fdfdc46072aee3f39278973622
frozen-blob: verification/programmes/oi-qm/track-b/act-20-representative-naturality/result.md 6f2d2c6a70eda2806e41b88b3b9fe5d4292f46db
frozen-blob: verification/programmes/oi-qm/track-b/act-12-two-sided-gauge/preregistration.md 5850238f290f0424ff677ff6d5cc2c039b1f58c2
frozen-blob: verification/programmes/oi-qm/track-b/act-12-two-sided-gauge/result.md 467d8be147b6ebd91f2eed12404566af74ac779f
frozen-blob: verification/programmes/oi-qm/track-b/act-13-cross-time-invariants/result.md 2c38dbf1c79a0aa3eb654a40d6cb527e99349b4a
frozen-blob: verification/programmes/oi-qm/track-b/act-18-intermediate-cross-time-structure/result.md 14a2cd8c54946bf0078329402e6f853107b31d9d
frozen-blob: verification/programmes/oi-qm/track-b/act-22-orbit-law-naturality-factorization/result.md b0f9ae48dbb2044de353efe1a0f5a1fab4f88c9e
frozen-blob: verification/programmes/oi-qm/track-b/act-23-orbit-law-gaps/result.md 174e790d2cc5c663f9f54ae7daaeca90b51f96e1
frozen-blob: verification/seals/CGR.json 5a1c18831a6c215c182c30952f7f9d3fc3b16c95
frozen-blob: verification/seals/OGC.json 6674357ff7a51df416b6ec0d747d5325b9a3756a
frozen-blob: verification/seals/OGS.json 5436e01852e9999483dd4aff9f605e47575b7415
frozen-blob: verification/seals/RNT.json 546965414aa47fa9b9554448e9c5d13d52f81109
frozen-blob: verification/seals/OLT.json 8ed0ef5391410db3a112cbe845d27536b7c1ab9b
frozen-blob: verification/seals/OLN.json 1552065eeae26b5e07ad1cfe1cdb97b76aefa1bb
frozen-blob: verification/seals/OLG.json 8a058df23c07e2b7571672c039a5a7b4f911a339
# row 1: name freedom, a drafting-time fact
{"id": "d1-tag-free", "scope": "D", "check": "git grep -l -- 'R7-NLV' $D", "expect": "empty"}
{"id": "d1-stem-free", "scope": "D", "check": "git grep -l -- '_NLV' $D", "expect": "empty"}
{"id": "d1-bare-free", "scope": "D", "check": "git grep -l -- 'NLV' $D", "expect": "empty"}
{"id": "d1-module-free", "scope": "D", "check": "git grep -l -- 'StrictNaturalLift' $D", "expect": "empty"}
{"id": "d1-dir-free", "scope": "D", "check": "git grep -l -- 'strict-natural-lift' $D", "expect": "empty"}
{"id": "d1-act-free", "scope": "D", "check": "git grep -l -- 'act-27' $D", "expect": "empty"}
{"id": "d1-target-free", "scope": "D", "check": "git grep -l -- 'A27-' $D", "expect": "empty"}
{"id": "d1-theorem-prefix-free", "scope": "D", "check": "git grep -l -- 'a27_' $D", "expect": "empty"}
# row 2: the seals tree at D
{"id": "d2-seals-tree", "scope": "D", "check": "test \"$(git rev-parse $D:verification/seals)\" = 73927a618072453213fd27189aceff76b6b499e5", "expect": "exit0"}
# row 4: provenance
{"id": "db4-ancestor", "scope": "D->B", "check": "git merge-base --is-ancestor $D $REF", "expect": "exit0"}
# row 6: no execution object; the names occur in this file, so the guard, the seals, the modules and the tree are read directly and never through git grep
{"id": "b6-guard-clean", "scope": "B", "check": "git show $REF:verification/lean/edge_rigidity_probe.py | grep -e 'R7-NLV' -e '_NLV'", "expect": "empty"}
{"id": "b6-no-record", "scope": "B", "check": "git ls-tree --name-only $REF verification/seals/ | grep -e 'NLV.json'", "expect": "empty"}
{"id": "b6-no-module", "scope": "B", "check": "git ls-tree -r --name-only $REF verification/lean-mathlib/ | grep -e 'StrictNaturalLift'", "expect": "empty"}
{"id": "b6-dir-control-plane-only", "scope": "B", "check": "git ls-tree -r --name-only $REF verification/programmes/oi-qm/track-b/act-27-strict-natural-lift/ | grep -v -e '/preregistration.md$' -e '/amendments/amendment-[0-9][0-9]*.md$'", "expect": "empty"}
# row 7: no round executing
{"id": "b7-no-prospective", "scope": "B", "check": "git show $REF:verification/lean/edge_rigidity_probe.py | grep -e '^_MANIFEST_PROSPECTIVE = {}$'", "expect": "nonempty"}
# row 8: act 26 sealed
{"id": "b8-cgr-sealed", "scope": "B", "check": "git show $REF:verification/seals/CGR.json | tr -d ' \\n' | grep -e '\"round\":\"CGR\",\"kind\":\"sealed\",\"base\":\"e0be0ab6dca7b6661a008a9f5e1f3a29ea736003\",\"sealed_head\":\"2b30604ccb5b3a8187357a6f720ee104bfe1826e\",\"merge\":\"7564ab67809d56d5047e27a4b214f99c2effc9c8\"'", "expect": "nonempty"}
{"id": "b8-cgr-guard", "scope": "B", "check": "git show $REF:verification/lean/edge_rigidity_probe.py | grep -e \"check('R7-CGR'\"", "expect": "nonempty"}
# row 9: act 26's module wired
{"id": "b9-import-cgr", "scope": "B", "check": "git show $REF:verification/lean-mathlib/OIBridge.lean | grep -e '^import OIBridge.OrbitGeometryRigidity$'", "expect": "nonempty"}
# row 10: this control plane at its path
{"id": "b10-self-present", "scope": "B", "check": "git cat-file -e $REF:verification/programmes/oi-qm/track-b/act-27-strict-natural-lift/preregistration.md", "expect": "exit0"}
```

## Execution discipline

- Freeze by exact commit SHA **and blob SHA** before any execution. **Blob identity is
  authoritative**, and the path this file sits at is pinned with it.
- Once frozen, immutable; execution-affecting corrections are append-only amendments, separately
  frozen and merged before the work they affect, each repeating the `M`-then-`B` certification and
  each moving `B` to its own certified merge.
- **This pull request carries this file alone.**
- **The execution branches from `B` and from nothing else**, only after `B`'s main-push
  certification is fully green, and **its first act is to verify that the preregistration at `B`
  carries the blob this freeze names**, recorded in the result note.
- **The stage-A commit comes first**, then **the module commit** with the shared lemmas, no verdict
  and no lift of any class map, then **the verdict commits of the executed targets in the order
  `A27-0`, `A27-1`**, one per executed target, `A27-H` and `A27-T` inside `A27-0`'s, the gate read
  between them; their SHAs are recorded, the prior knowledge is disclosed once before the spans,
  and the attestation set is answered for each of the three spans.
- **Then exactly one execution pull request**, based on `B`, carrying the Lean module, the result
  note, the `R7-NLV` guard clause, the two declarations, the `ROADMAP` propagation and the census
  entry. **No manuscript changes. No edit to any closed round's guard.**
- **Before certification the execution never absorbs later `main`.** The certification of record is
  the run whose `head_sha` is `E`.
- Exact-head review after execution is complete, with full continuous integration green.
- **No merge without an explicit owner direction after exact-head review, naming the exact head SHA.**
- **The landing is `E` → `L` → `P`, on the execution pull request, in that order**, with `P`
  mandatory and pin-only. Landing conflicts are resolved **in `L`, never in `E`**, by merits. Full
  continuous integration must pass again on `P` before the pull request merges, and the resulting
  `main` build must be green before the next round's landing is constructed.

## Allowed final report

1. **The round's shape**, restated: sealing under the manifest protocol, `E` → `L` → `P`, the
   declarations as set, the record `P` writes named field by field, no existing manifest record
   altered, no closed round's contract edited, no legacy constant written, and the base-blob
   verification recorded;
2. **the ordering obligation's eight records**, the prior-knowledge disclosure made once before the
   spans, and the attestation set's **three** answers, one per span, with the freeze-supplied facts
   listed;
3. **`A27-0`** — the label with its frozen sentence, the theorem named, the five conjuncts reported
   separately, the hypotheses reported as the two class-map hypotheses alone, the section reported
   as a `Classical.choice`, and **the statement that no hypothesis was added and the predicate is
   act 20's verbatim**;
4. **`A27-1`** — the label with its frozen sentence, the theorem named or the gate's closure, the
   inverse equations and each rung conjunct reported separately, and **the statement that act
   18's standing hypotheses and `L5` are not asserted**;
5. **`A27-H`** — each shape with its outcome and its theorem or its step, and the statement that
   act 20's lift and verdicts stand;
6. **`A27-T`** — its outcome, its theorem or its step, the dilation, weak element and coordinate
   used;
7. **the outcome vector**, one of the three rows verbatim, **the gate record**, and **the statement
   that no verdict was inferred from another beyond the consumptions the freeze places**;
8. **the route-authorization matrix as honoured**;
9. **the scope boundary as honoured**: nothing derives, recognises or approaches quantum
   evolution; no continuity, composition or generator; no lift called canonical, unique or
   continuous, none read as a symmetry, an antiunitary map, a time reversal, a unitary evolution or
   a dynamics; no metric, feature vector or isometry consumed; act 24's `a4` untouched in either
   direction; every earlier act's verdicts untouched; the threading, act 16's cell, act 18's
   `D`-axis, act 10's anchor axis and act 14's carriers untouched;
10. **the non-adoption clause carried verbatim at each mention**, with the count of carriages;
11. the frozen `P0` sentence for the case reached, appended after act 26's, the row's label
    unchanged, with `L5` named as next and its power left open;
12. what no outcome licenses, in this file's wording, and the status rule as honoured;
13. the relation to acts 7, 11, 12, 17, 18, 20, 21, 22, 23, 24, 25 and 26 — every merged label
    consumed, none revised;
14. the definition count against the budget of zero;
15. the chronology certification, naming the property certified, the ten preconditions with their
    scopes and the block's rows as the base check reported them at `M` and at `B`, the validator's
    classification of `NLV` at `E`, `L` and `P`, the empty supersession table reported as honoured,
    and `SI-3`'s standing contract reported as holding;
16. the axiom table with one line per named result;
17. the discrepancies, if any, recorded and not repaired — including any step of the freeze's
    reading the kernel refused;
18. the observation, if any, for a later round on `L5`, on a class map without a lift, or on a
    given tuple-level family, stated once and narrowly as the non-doings section fixes it, and the
    observations the anti-expansion rule collected.

## Owner settlements before immutability

Recorded before this freeze merges and becomes immutable under `§A.37`. **Every item below is a call
already made**, and the body of this freeze is written to them throughout. **This freeze carries no
open decision.**

1. **The invariant-stabilizer target is withdrawn.** The premise that act 24 froze twelve invariant
   families was a conflation of its twelve transition families with its one invariant family; the
   owner withdrew that target and the retarget to representative-level naturality was approved.
2. **The round is the one-target vacuity round, option 1 of the two the owner was offered**: the
   universal strict-lift theorem, its reversible corollary, the four-shape instantiation and the
   tuple-level contrast; the sixteen-coset selection test is not run, the universal theorem
   covering every class map without depending on the enumeration's exhaustiveness.
3. **The predicate is act 20's exact `StrictNatural`**, quantified as its module quantifies it, with
   the identity induced maps; a normalizer or intertwining formulation earns nothing without a
   proved connection to it.
4. **The four obligations are frozen**: classes identified with two-sided orbits through act 12's
   theorem; well-definedness through the common stabilizer `{(λI, λ⁻¹I) : |λ| = 1}`; naturality on
   the full domain with the identity extension validated by the gauge-invariance of admissibility
   and its complement, the choice of representatives disclosed and existence alone claimed; the
   reversible case from one fixed section with descent and reversibility established together.
5. **The construction is recorded as pre-freeze hand reasoning**, with a positive prediction at
   evidence level 2, disclosed so that the attestation set measures what the execution acquires
   beyond it.
6. **Act 24's cell `a4` is untouched in either direction**, its configuration and its object being
   separate obligations; the correction that selection would not have closed `a4` positively, and
   the further correction that this round's class-level theorem decides `a4` in neither direction,
   are both recorded in the locating control.
7. **The scope sentences are the three the owner fixed**: `L4n` imposes no restriction on class maps
   at this configuration under existential lifting; with the reversible corollary the relevant
   pre-`L5` rungs cannot distinguish act 25's family from any other bijection of the classes
   there; act 24's `a4` remains unchanged.
8. **`L5` at the product configuration is named as the next candidate to inspect, with its
   selecting power left open.**
9. **The definition budget is zero**, every object a bound variable or a written-out proposition.
10. **The lifecycle is `§A.37`'s under the manifest protocol**: the control plane merges alone; its
    certified merge is `B`; the execution branches from `B` and nothing else; the landing is `E` →
    `L` → `P` with `P` mandatory; no merge without an explicit direction naming the exact head SHA.
