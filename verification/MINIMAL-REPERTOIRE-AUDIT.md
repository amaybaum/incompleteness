# The minimal-repertoire audit — how little control selects quantum mechanics

`OIBridge/MinimalRepertoire.lean` (the round's kernel), `verification/lean/repertoire_lie_probe.py`
(the exact Lie-algebra computation that fixed the hypothesis before the kernel work),
`INVERSE-CLAUSE-AUDIT.md` (the package this round compresses), `LieRankSource.lean` (the
elementary repertoire and its `su(D)` derivation), `EmbeddedObservation.lean` and
`AncillaClosure.lean` (the level machinery the descent step consumes); guard `R7-MIN` in
`verification/lean/edge_rigidity_probe.py`.

**Status: Outcome A of the preregistered fork, proved.** Implementation locality, phase-free
richness and embedded observation are equivalent to exact finite endomorphic operational quantum
mechanics on every nonempty finite carrier (`carrier_general_oiPlusMin`); the quarter phase and
all but one driven pair leave the repertoire; the discrete part of the repertoire is one full cycle
and one adjacent exchange (`phaseFree_of_cyclic`); and the parity obstruction that excludes "one
drive and one cycle" on even carriers is a theorem (`not_hControl_evenCycle`). Twenty-three named
results, each printing only `propext`, `Classical.choice`, `Quot.sound`. The fork below is
recorded as preregistered, with the one finding that preceded the kernel and fixed the hypothesis.

## The question

The exact characterization is `OIPlusPos`: implementation locality, elementary transition richness,
embedded observation. Elementary transition richness (`ElementaryTransitionRichness`) asks, at
every level `n` on the carrier `A × Fin n` of size `D`, for three things: every pair of distinct
states continuously driven (`flow (transition a b) t` available for every real `t`), every
exchange available (`permMatrix (Equiv.swap a b)`), and a quarter phase on every state
(`phaseGate a`). It is the one conjunct carrying the nonclassical selection, and it has not been
shown minimal.

> How much of the repertoire is needed? Is one driven transition enough? Is the quarter phase
> derivable? Is the full set of exchanges needed?

## The finding that precedes the kernel

The Lie algebra generated by the drives `−i(E_ab + E_ba)` on the edges of a graph on `D` states
is `su(D)` when the graph is connected and **not bipartite**, and a conjugate of `so(D)` when the
graph is bipartite: a diagonal phase `Λ = diag(i on one colour, 1 on the other)` carries every
drive on a bichromatic edge to a real antisymmetric matrix, and the real antisymmetric matrices are
a Lie algebra with no diagonal element. `repertoire_lie_probe.py` computes the generated algebra
exactly over `ℚ`:

| edge set | `D` | generated dimension | `su(D)` |
|---|---|---|---|
| `D`-cycle | 3, 5, 7 | 8, 24, 48 | 8, 24, 48 |
| `D`-cycle | 4, 6, 8 | 6, 15, 28 | 15, 35, 63 |
| path | 3, 4, 5, 6 | 3, 6, 10, 15 | 8, 15, 24, 35 |
| complete graph | 3, 4, 5 | 8, 15, 24 | 8, 15, 24 |
| cycle plus one chord | 4, 6 | 15, 35 | 15, 35 |

So "one driven transition plus one cyclic permutation" generates full control exactly when `D` is
odd. Every doubled level and every level over an even carrier is even, so that primitive cannot
be the theory-level one. The orbit of one driven pair under the exchanges is the complete graph,
which has a triangle, so the primitive attacked here is:

> **Phase-free richness.** At every level with two or more states, some pair is continuously
> driven; at every level, every exchange is available. No quarter phase.

The exchanges themselves are generated by one full cycle and one adjacent exchange
(`Equiv.Perm.closure_cycle_adjacent_swap`), so the discrete part of the primitive is two
elements; the parity obstruction says the adjacent exchange cannot be dropped.

## The fork

**Outcome A — phase-free richness suffices.** Implementation locality, phase-free richness and
embedded observation are equivalent to exact finite endomorphic operational QM on every nonempty
finite carrier. The route has three parts, each stated in advance:

1. **Local theorem, `D ≥ 3`.** One driven transition and the permutations generate `su(D)`:
   the drives on all pairs are conjugates of the one drive; the bracket of two drives sharing a
   state is the imaginary transition on the third pair, which the quarter phase used to supply;
   the bracket of the real and imaginary transitions on a pair is the population difference; and
   the round-59 decomposition of a traceless skew-Hermitian matrix finishes. No phase.
2. **Local sharpness, `D = 2`.** The drive on the one pair and the swap commute; their Lie algebra
   is one-dimensional and contains no population-difference direction. This is the `D = 2`
   instance of the bipartite obstruction, stated for any bichromatic drive and any set of
   colour-compatible permutations, and it also covers the even cycle. It is an algebraic
   countercontrol, not a theory-level independence theorem.
3. **Descent.** At the levels with `D ≤ 2` — `(|A|, n) ∈ {(1,1), (1,2), (2,1)}` — control is
   inherited from a level with `D ≥ 3`: the unitary `U ⊗ 1` on `(A × Fin n) × Fin 3`, transported
   to level `3n`, is available by the local theorem there; iterated ancilla closure, which
   embedded observation derives, returns its uniform-attach-then-discard to level `n`; and the
   discard of a conjugation by `U ⊗ 1` is the conjugation by `U`. Classical coarse-graining
   (`availExt_coarse`) plays no part: it changes outcome labels at a fixed level and does not move
   operations between carrier dimensions.

**Outcome B — the qubit phase is genuine.** The local theorem holds, descent fails, and a
`FiniteOperationalTheory` carries implementation locality, phase-free richness and embedded
observation while missing a qubit `Z`-rotation. The package then keeps a `D = 2`-only phase
clause.

**Excluded before the kernel.** The cyclic variant, one drive and one cycle with nothing else:
the parity obstruction above. The round proves the obstruction as a theorem rather than leaving
it as a computation.

## The results

| result | statement |
|---|---|
| `bracket_XX` | two drives sharing a state bracket to the imaginary transition on the third pair |
| `hControl_of_XYZ` | the round-59 decomposition, isolated: real and imaginary transitions on every pair and the population differences give every traceless skew-Hermitian matrix |
| `hControl_perm` | on `D ≥ 3` states, one driven transition and the permutations give `𝔏 ⊇ su(D)` |
| `colourAlg`, `transition_mem_colourAlg`, `controlLie_le_colourAlg` | a bichromatic drive with colour-compatible permutations generates inside a conjugate of the real antisymmetric matrices |
| `diag_zero_of_mem_controlLie`, `popDiff_notMem_controlLie`, `not_hControl_of_colourCompatible` | that algebra reaches no population difference, so `𝔏 ⊇ su(D)` fails on two or more states |
| `not_hControl_two` | the qubit: its drive and every permutation of its two states fail `𝔏 ⊇ su(2)` |
| `not_hControl_evenCycle` | one drive and the powers of a full cycle on `2m + 2` states fail `𝔏 ⊇ su(D)` |
| `phaseFree_of_elementary`, `avail_perm_of_ne` | the elementary repertoire gives phase-free richness; the exchanges of distinct states give every permutation on two or more states |
| `control_at_level` | phase-free richness gives full control at every level with `D ≥ 3`, through `universalReachability_of_lieRank_positive` |
| `tensorOf_one_isometry`, `discard_tensorOf_one`, `descend` | `U ⊗ 1` is an isometry; the discard of `conj (U ⊗ 1)` is `conj U`; control at level `n(m+1)` descends to level `n` through iterated ancilla closure |
| `control_of_phaseFree` | phase-free richness and closure give full composite unitary control on every nonempty carrier |
| `oiPlusMin_iff_qm`, `oiPlusMin_iff_oiPlusPos`, `carrier_general_oiPlusMin` | the package, equivalent to QM and to `OIPlusPos` |
| `perm_avail_of_cycle_swap`, `phaseFree_of_cyclic` | one full cycle and one adjacent exchange give every permutation; cyclic richness implies phase-free richness |

The descent runs from level `3n` rather than `2n`, so that the carrier there has at least three
states even when `|A| · n = 1`. The two falsifiers named for A — a discard that is not `conj U`,
or a transport along `shiftIdx` that misses the level of the local theorem — did not fire:
`discard_tensorOf_one` is an identity of linear maps, and `descend` consumes the closure rule at
exactly the level `n · (m + 1)` at which `control_at_level` applies.

## What the package says

`OIPlusMin := ImplementationLocality ∧ PhaseFreeRichness ∧ EmbeddedObservation`. Phase-free
richness is: at every level with two or more states, some pair is continuously driven and every
exchange of two distinct states is available. Through `phaseFree_of_cyclic` the exchange clause may
be read as one full cycle and one adjacent exchange. Everything else in the earlier packages —
validity, spectator independence, the seam, closure, observer recursion, Lie-rank richness, full
control, inverse accessibility, the quarter phase, the drives on all other pairs — is derived. The
one continuously driven transition is the sole nonclassical resource the characterization consumes.

## What this note does not claim

That one driven transition is minimal in any stronger sense — whether the *driven* pair can be replaced
by a discrete resource is exactly the physical question of the substratum audit, and this round
does not touch it. That the empirical addition of the mixing resource is supplied by the OI
substratum. That anything here bears on the OI-N freeze, the concrete-cut freeze, or CT3.
